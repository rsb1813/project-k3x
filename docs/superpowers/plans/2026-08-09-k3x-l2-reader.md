# K3X Milestone 6 Independent L2 Reader Implementation Plan

**Goal:** Add an exact ordered batch reader with independent `pread|io_uring` and `buffered|direct` axes, then measure it without claiming WSL2 logical reads as physical NVMe traffic.

**Architecture:** Reader metadata verification stays portable and buffered. A Reader-owned data-plane source keeps one descriptor, serves exact ordered batches, and exposes logical versus submitted/completed storage accounting. The expert loader batches six native MXFP4 extents. Optional Linux liburing and direct-I/O paths remain runtime switches with explicit capability failure.

**Toolchain:** C++20, CMake/Ninja, Python 3.12/pytest, optional liburing on Linux, optional CUDA 13.3 `sm_120`, Compute Sanitizer, JSON/CSV benchmark tooling.

## Task 1: Freeze contracts and failing tests

- [x] Add `L2IoEngine`, `L2CacheMode`, `ReaderOptions`, `ExtentRequest`, and expanded counters without changing default behavior.
- [x] Add tests for parsing, defaults, invalid queue depth, unsupported modes, exact ordered batches, overflow, out-of-range, short reads, and failure atomicity.
- [x] Add a model test requiring one six-extent expert batch and exact token/routing parity.
- [x] Commit the contract and red tests.

## Task 2: Persistent buffered pread baseline

- [x] Add a Reader-owned portable data-plane interface and Linux persistent-descriptor `pread` implementation.
- [x] Delegate single tensor/auxiliary reads through the ordered batch path.
- [x] Preserve exact CRC/error behavior and logical counter semantics.
- [x] Pass CPU and CUDA reader/model regressions.
- [x] Commit the buffered baseline.

## Task 3: Batch native expert extents

- [x] Add record lookup that collects gate/up/down packed and scale requests before any data-plane read.
- [x] Submit one ordered six-request batch and reconstruct exact `ExpertMlpPayload` values.
- [x] Preserve atomic L1 admission and transient bypass behavior.
- [x] Verify disabled/static L1 parity and reduced batch submissions without changing logical bytes.
- [x] Commit the expert batch integration.

## Task 4: Optional io_uring engine

- [x] Add `K3X_ENABLE_IO_URING` with Linux/liburing discovery and an unchanged default build.
- [x] Implement bounded SQE submission, stable request identity, complete CQE reaping, negative-result handling, and out-of-order mapping.
- [x] Add deterministic tests through a completion-order seam plus real-kernel smoke tests when supported.
- [x] Verify requested unsupported mode fails before model execution.
- [x] Commit the io_uring engine.

## Task 5: Explicit O_DIRECT cache mode

- [x] Query `STATX_DIOALIGN` and reject missing/unsupported capability without fallback.
- [x] Implement overflow-safe aligned intervals and aligned bounce-buffer ownership for both engines.
- [x] Distinguish logical bytes from aligned submitted/completed storage bytes.
- [x] Test misalignment, EOF, amplification, unsupported filesystem, and exact slice reconstruction.
- [x] Commit the direct-I/O mode.

## Task 6: Runtime, profiler, and B-0007 runner

- [x] Add CLI options and benchmark schema fields for both axes, queue depth, alignment, batches/completions/failures, and process I/O deltas.
- [x] Add a deterministic synthetic aligned I/O fixture without full Kimi weights.
- [x] Cross four cases only where capabilities exist and record skipped modes explicitly.
- [x] Enforce exact token/routing and logical-byte parity across supported cases.
- [x] Commit the runner and schema.

## Task 7: Verification and measurement

- [x] Run CPU and CUDA full suites.
- [x] Run all affected CUDA Compute Sanitizer targets.
- [x] Run WSL2 correctness/smoke measurements and label them non-authoritative.
- [x] Record native-Linux P44 Pro warm/cold B-0007 as unavailable in this environment; do not fabricate it.
- [x] Keep `pread + buffered` as default because native evidence is unavailable.

## Task 8: TITAN Ledger, review, and publication

- [x] Update `ARCHITECTURE.md`, `DECISIONS.md`, `BENCHMARKS.md`, `README.md`, `checklist.md`, and `context-notes.md`; update `PROJECT_STATE.md` last.
- [ ] Request one Terra high read-only Critical/Important review and apply evidence-backed findings once.
- [ ] Commit results and ledger, push the milestone branch, open a draft PR, require Linux CI, ancestry-check, fast-forward public `main`, and confirm post-merge CI.
- [ ] Preserve the worktree for the next milestone.
