# K3X 컨텍스트 노트

## 2026-08-08

- 워크스페이스는 시작 시 완전히 비어 있었고 Git 저장소가 아니었다.
- 사용자는 Python/PyTorch reference와 C++20 CPU runtime, 후속 CUDA backend 조합을 승인했다.
- 첫 마일스톤의 실행 범위는 텍스트 디코더이다. MoonViT-V2는 아키텍처 연결 경계만 문서화하고 실행 구현은 연기한다.
- 첫 마일스톤은 KDA, Gated MLA, Attention Residual, Stable LatentMoE, router, native MXFP4 decode, incremental state 및 greedy generation의 correctness를 검증한다.
- 합성 모델은 4개 decoder layer를 `KDA, KDA, KDA, MLA` 순서로 배치한다. 실제 모델의 3 KDA 뒤 1 MLA 패턴을 최소 크기로 재현하기 위한 결정이다.
- 합성 Attention Residual block size는 여러 depth source를 작은 모델에서 실제로 테스트하기 위해 2로 축소한다. 실제 checkpoint 값은 12이며 문서에서 명확히 구분한다.
- Native MXFP4 expert는 dequantize 후 requantize하지 않는다. 합성 converter도 payload와 scale의 byte-exact 보존을 검증한다.
- K3X v1은 4 KiB superblock, little-endian fixed-width records, aligned extents, per-extent CRC32C와 최종 SHA-256, required/optional feature bits를 사용한다.
- 미완성 변환은 `.partial` artifact와 resume manifest에 남기고, 모든 extent 검증 후 원자적으로 최종 이름으로 바꾼다.
- 확인한 공식 Kimi K3 config의 주요 값은 hidden 7168, 93 layers, 69 KDA, 24 MLA, 96 heads, latent MoE 3584, 896 experts, Top-16, 2 shared experts, AttnRes block 12, KDA short convolution 4이다.
- 조사 시점의 source HEAD는 설계 명세의 source ledger에 고정한다. 이후 upstream 변경은 자동으로 현재 설계의 근거가 되지 않는다.
- 사용자가 작성된 첫 마일스톤 설계 명세를 승인했다.
- 구현 계획 작성 시 기본 환경에서 Python 3.14.6만 확인되었고 PyTorch, CMake, C++ compiler 및 WSL은 없었다. Codex 번들 Python 3.12.13은 사용할 수 있지만 PyTorch는 설치되어 있지 않다.
- 2026-08-08 PyPI index에서 확인한 계획상 Python dependency version은 torch 2.13.0, numpy 2.5.1, safetensors 0.8.0, pytest 9.1.1, google-crc32c 1.8.0, psutil 7.2.2이며 build backend는 setuptools 83.0.0이다. 설치 전에는 사용자에게 package-install 권한을 별도로 확인한다.
- 첫 마일스톤 구현은 연결된 correctness graph이므로 기본 실행 방식은 주 agent의 inline execution으로 둔다. 독립 agent 사용은 사용자가 명시적으로 요청할 때만 선택한다.
- 사용자가 로컬 dependency 설치와 전체 구현 진행을 승인했다. 격리 worktree `feat/milestone-zero`에서 작업한다.
- 저장소 전용 `.venv`에 Python 3.12.13, torch 2.13.0, numpy 2.5.1, safetensors 0.8.0, pytest 9.1.1, google-crc32c 1.8.0, psutil 7.2.2, CMake 4.4.2, Ninja 1.13.0을 설치했다. 시스템에는 MSVC 19.51이 이미 있어 LLVM 설치는 진행하지 않았다.
- 합성 config 계약 테스트는 의도한 `ModuleNotFoundError`로 RED를 확인한 뒤 2개가 통과했다. `pip check`도 문제 없음으로 통과했다.
- 고정한 PipeNetwork revision의 vendored text reference와 MLX port를 재검증한 결과 K3 MLA는 `mla_use_nope=true`이며 `qk_rope_head_dim`으로 명명된 64차원 subspace에도 rotary embedding을 적용하지 않는다. 사용자 승인에 따라 설계와 계획에서 이를 shared extra-key NoPE 경로로 정정했다.
- FP32 RMSNorm, SiTU-GLU, native MXFP4 E2M1/E8M0 decode와 reference matmul 테스트 6개가 통과했다. Nibble 순서를 의도적으로 뒤집었을 때 literal test가 실패함을 확인하고 low-nibble-first 구현을 복원했다.
- KDA reference는 causal depthwise ShortConv history, head-wise `A_log`, channel-wise forget logits, sigmoid beta, delta write 후 read, full-rank output gate를 명시적 state로 구현했다. Pre-update state를 읽도록 변이했을 때 literal recurrence test가 실패함을 확인했다.
- Gated MLA reference는 q-LoRA와 normalized KV latent, main key/value, shared extra NoPE key, output gate를 명시적으로 저장한다. 길이 1, 2, 5에서 KDA와 MLA 모두 prefill과 token-by-token decode 출력 및 최종 state가 exact match했다.
- 전체 그래프의 full-prefix와 incremental 경로는 layer 0부터 GEMM batch shape가 달라 최대 `9.31e-9`, 최종 logits에서 최대 `3.58e-7`의 정상적인 FP32 반올림 차이가 측정됐다. 따라서 cross-path 및 cross-language state는 `atol=rtol=1e-6` 수치 비교를 사용하고, byte-exact digest는 같은 runtime의 반복 실행에만 사용한다.
- 공식 vendored `config.json`에서 SiTU beta 4.0, linear beta 25.0, MLA output gate 활성화, routed scaling factor 1.0을 확인해 합성 config와 K3X model metadata에 반영했다.
- Router의 correction bias는 선택에만 사용하고 unbiased sigmoid score를 normalize한다. Stable LatentMoE는 native MXFP4 routed branch와 원래 hidden-space shared branch를 분리하며, AttnRes는 normalized key로 score한 뒤 raw source를 혼합한다.
- Controlled graph는 prompt `[1,7,3,9]`에서 full/incremental 모두 token 5를 6회 생성했다. Seeded graph도 두 mode의 greedy token이 exact match했고, source shard와 golden fixture를 두 번 생성한 SHA-256 manifest가 동일했다.
- K3X v1 writer는 safetensors header만 읽어 tensor extent를 찾고, 설정된 chunk 크기 이하로 payload를 복사한다. native MXFP4 packed code와 scale은 별도 aligned extent로 byte-for-byte 보존한다.
- 각 extent는 flush와 fsync 뒤 partial 파일에서 다시 읽어 CRC32C를 확인한 후에만 원자적 resume ledger에 기록한다. source fingerprint가 달라진 resume는 거부한다.
- 4 KiB superblock literal layout, 128-byte tensor record, extent 정렬·중복·절단 검증, payload corruption, unknown required feature, source mutation, 중단 재개, CLI dry-run 및 native MXFP4 byte parity를 포함해 Python 전체 35개 테스트가 3.49초에 통과했다.
- C++20 runtime은 외부 ML library 없이 portable CRC32C/SHA-256, strict K3X reader, RMSNorm, SiTU, native MXFP4와 합성 KDA/MLA/Attention Residual/Stable LatentMoE graph를 구현한다. Root SHA-256은 1 MiB heap buffer로 증분 검증해 artifact 전체를 RAM에 올리지 않는다.
- C++ reader는 Python writer artifact를 열고 payload CRC corruption, unknown required feature, truncation을 동일한 안정적 오류 코드로 거부했다. C++ full/incremental greedy 결과는 PyTorch의 `[43,32,28,49,9,28]`과 모두 exact match했다.
- 첫 cross-language 실행의 Windows `0xC00000FD`는 reader 함수의 1 MiB stack SHA buffer 때문임을 확인했다. heap buffer로 이동한 뒤 같은 parity test가 통과했다.
- C++ runtime에 prefill, decode, layer별 단조 시계 계측을 추가하고 별도 benchmark driver에서 프로세스 RSS, 논리적 artifact read bytes, KDA/MLA state 크기를 수집한다. 첫 토큰은 prefill logits에서 나오므로 decode tok/s 계산에서는 이후 5개 토큰만 사용한다.
- 최종 리뷰에서 첫 token의 argmax 시계 구간과 실행기 무결성 검증 생략을 발견해 바로잡았다. 2026-08-08 Windows 11 AMD64, MSVC Debug, 3 warmup과 20회 재측정에서 합성 모델 prefill 405.11 tok/s, 실제 5회 incremental forward decode 558.89 tok/s, strict 전체 artifact 검증을 포함한 process-level TTFT 중앙값 86.20 ms, peak child RSS 6,270,976 bytes, logical tensor read 110,936 bytes/generated token을 측정했다. 이는 합성 correctness harness 결과이며 실제 Kimi K3 또는 RTX 5080 성능 근거가 아니다.
- 공개 config 차원으로 계산한 native MXFP4 routed expert는 17,547,264 bytes이다. cache reuse가 전혀 없는 natural Top-16의 92개 MoE layer expert traffic은 25,829,572,608 bytes/token이다. P44 Pro 공개 최대 7.0 GB/s만 적용한 expert-only ceiling은 0.27 tok/s이므로 다음 실제 병목은 NVMe expert traffic 회피율이다.
- 공식 차원으로 계산한 BF16 text trunk 추정치는 약 85.72 GB이며 전체 checkpoint scan 결과가 아니다. 96 GB RAM에서 OS와 expert bank까지 확보하려면 sensitivity-aware trunk quantization이 필요하다는 capacity planning 근거로만 사용한다.
- GitHub Actions는 Linux에서 C++ build/CTest 뒤 Python 및 cross-language suite를 실행한다. 테스트 runner 경로는 Windows `.exe`와 POSIX binary를 모두 찾도록 정정했다.
- 최종 diff 리뷰에서 발견한 실행기 `metadata_only`, 불완전한 C++ layer/expert directory parser, final rename 직후 stale ledger, token-only C++ parity를 모두 회귀 테스트와 함께 수정했다. 실행기는 생성 전에 artifact 전체 무결성을 검증하고, C++ 진단 mode는 prompt logits, 네 layer 출력, canonicalized KDA/MLA state를 PyTorch와 `1e-6` 허용오차로 비교한다.

## 2026-08-08 Milestone 1

- 사용자는 CPU exact runtime과 profiler를 먼저 확장하고, 이어서 RTX 5080용 basic CUDA backend를 만드는 순서를 승인했다.
- custom CUDA only, cuBLASLt only, hybrid baseline을 비교했고 사용자는 cuBLASLt 기준 경로와 custom MXFP4 경로를 함께 두는 hybrid 설계를 승인했다.
- 실제 개발 PC에서 NVIDIA GeForce RTX 5080 16,303 MiB, compute capability 12.0, driver 591.86, CUDA toolkit 13.3과 nvcc 13.3.73을 확인했다.
- 설치된 nvcc는 `compute_120`과 `sm_120`을 지원한다. CUDA 13.3 local header만 보면 E2M1과 UE8M0/32 mode가 함께 보이지만, NVIDIA 공식 cuBLAS 문서는 FP4에 UE4M3/16을 요구하고 UE8M0/32를 FP8 mode로 규정한다. K3의 E2M1+E8M0/32 native MXFP4와 직접 호환되지 않으므로 cuBLASLt FP4 expert path는 기각하고 cuBLASLt는 dense baseline에만 사용한다.
- 격리 worktree `feat/milestone-one-runtime`을 만들고 Python 3.12.13 및 `pyproject.toml`의 고정 dependencies를 새 `.venv`에 설치했다.
- baseline의 첫 Python 실행은 C++ build 전이라 cross-language runner 7개가 없어서 실패했다. MSVC Developer Command 환경과 `.venv`의 Ninja 경로를 명시해 Release build와 CTest 2/2 통과를 확인했다.
- 새 `k3x_run.exe` 실행은 Windows Smart App Control policy `{0283ac0f-fff1-49ae-ada1-8a933130cad6}`에 의해 차단됐다. Code Integrity event 3033과 3077에서 unsigned executable이 Enterprise signing level을 충족하지 못했다는 원인을 확인했다. Python suite는 이 환경 제약으로 cross-language 5개가 차단되고 나머지 41개가 통과했다.
- K3X는 Smart App Control을 자동으로 끄거나 신뢰 저장소를 수정하지 않는다. CUDA runtime 완료 판정은 Linux native 또는 사용자가 별도로 승인한 WSL2 GPU 환경에서 수행한다.
- 사용자는 Milestone 1 hybrid 설계 명세를 승인하고 TITAN LEDGER 지속성 프로토콜을 추가했다. `PROJECT_CHARTER.md`는 안정된 헌장, `ARCHITECTURE.md`는 상태가 표시된 실제 구조, `DECISIONS.md`는 선택 근거, `BENCHMARKS.md`는 측정 원장, `PROJECT_STATE.md`는 마지막에 갱신하는 현재 상태로 분리한다.
- APOLLO, TITAN COUNCIL, AURORA, PROMETHEUS-X, MERCURY, ORBIT, HELIOS, SHADOW, PHOENIX, VAULT, VEILBREAK, AUTO는 구현·benchmark 전까지 proposed이다. ATLAS, CHRONOS, BLACKSTAR는 역할 정의가 제공되지 않아 reserved proposed/undefined로 기록했다.
- Windows에는 WSL distribution이 설치되어 있지 않다. WSL2 설치는 시스템 변경과 재부팅 가능성이 있으므로 명시적 사용자 승인 전에는 실행하지 않는다.
- Milestone 1 구현 계획은 Linux GPU 실행 환경을 선행 관문으로 두고 profiler, CPU backend extraction, optional CUDA shell, cuBLASLt dense, custom MXFP4, end-to-end profiling, 최종 측정 순서로 작성했다.
- 사용자가 inline execution과 WSL2 Ubuntu 24.04 설치를 명시적으로 승인했다. 비관리자 `wsl --install`은 기능 비활성 상태에서 exit 1로 종료됐고, 관리자 DISM으로 `Microsoft-Windows-Subsystem-Linux`와 `VirtualMachinePlatform`만 `/NoRestart` 활성화했다. 두 기능 모두 `InstallState=1`을 확인했으며 CBS/PendingFileRename 재부팅 대기 상태라 자동 재부팅 없이 중단했다.
- 재부팅 후 WSL 2.7.11.0과 Ubuntu 24.04.4 LTS를 설치했다. Linux kernel은 6.18.33.2이며 RTX 5080 16,303 MiB, compute capability 12.0, Windows driver 591.86이 WSL에 정상 노출된다.
- NVIDIA WSL 지침에 따라 Linux display driver는 설치하지 않고 WSL 저장소의 `cuda-toolkit-13-3`만 설치했다. 실제 package version은 13.3.1-1, nvcc는 13.3.73이며 `sm_120`을 제공한다.
- Ubuntu 기본 개발 사용자는 비관리자 `jolib`로 설정했다. Python 환경은 `/home/jolib/.venvs/k3x-m1`, native build는 Git에서 무시되는 `build-linux`를 사용한다.
- 생산 코드 변경 전 Linux Release baseline은 CTest 2/2와 cross-language 제외 pytest 39/39가 통과했다.
- `K3X_BUILD_DIR` resolver의 RED ImportError를 확인한 뒤 공통 native binary resolver를 구현했다. targeted cross-language 8/8, 전체 pytest 47/47, CTest 2/2가 통과했고 커밋은 `b6d900f`다.
- deterministic profiler는 명시적 event만 소유하며 clock, thread, JSON, CUDA 의존성을 갖지 않는다. 실패 event는 실패 횟수만 증가시키고 시간·byte 합계에서는 제외하며 H2D와 D2H bytes를 분리한다. unknown target RED 후 CTest 3/3과 pytest 47/47을 확인했고 커밋은 `f06ce97`이다.
- exact CPU backend는 기존 double 누산 dense와 native MXFP4 matvec만 격리한다. graph는 명시적 backend와 prefill/decode phase를 전달하고 LM head는 global layer로 기록한다. literal backend RED/GREEN, CTest 4/4, cross-language parity 5/5, 전체 pytest 47/47을 확인했고 커밋은 `b439e25`이다.
- Release `NDEBUG`가 최근 `assert` 기반 profiler/backend test 표현식을 제거해 unit checks가 실행되지 않은 문제를 발견했다. 기존 suite 방식인 explicit return codes로 교체해 실제 RED와 GREEN을 재확인했고 커밋은 `d1b52d4`이다.
- optional CUDA shell은 OFF에서 typed unavailable stub만 링크하고 CUDA dependency가 없으며, ON에서 CUDA 13.3, native `sm_120`, capability 12.0 gate, nonblocking stream, cuBLASLt handle을 RAII로 소유한다. CPU/CUDA CTest 각각 5/5, CPU 전체 pytest 47/47, CUDA cross-language parity 5/5를 확인했고 커밋은 `5b6d1e7`이다. CUDA matrix compute는 아직 미구현이다.
- cuBLASLt dense baseline은 row-major FP32 host API를 유지하고 FP32 또는 BF16-rounded GPU operand를 zero-workspace heuristic으로 실행하며 FP32로 누산·반환한다. CUDA 13.3의 정규 행렬 계약은 BF16 `Atype/Btype`을 함께 요구하므로 BF16 mode는 입력과 가중치를 모두 RNE 반올림해 staging한다. literal test에서 FP32 H2D/D2H는 36/8 bytes, BF16-rounded는 18/8 bytes이며 두 mode 모두 독립 oracle과 일치한다. `compute-sanitizer` memcheck 0 errors, CUDA CTest 6/6, CPU CTest 5/5, CPU pytest 47/47, CUDA cross-language parity 5/5를 확인했고 구현 커밋은 `c4b612a`다. 이는 correctness 측정이며 throughput benchmark는 아니다.
- exact custom MXFP4 baseline은 K3X low-nibble-first E2M1과 E8M0/32 payload를 재패킹 없이 읽고, output row당 256-thread block 하나에서 FP32 shared-memory reduction을 수행한다. 3-row/64-column literal은 두 scale group, sign, exponent, high/low nibble을 검증하고 320-column mutation fixture는 두 번째 thread stride를 제거하면 실패한다. invalid extent, non-32 group, reserved `0xFF` scale을 거부한다. 승인된 comparison 계약에 맞춰 `cuda_dense`는 CPU MXFP4 oracle을 유지하며 H2D와 device time이 0이고, custom kernel은 `cuda_custom`에서만 실행된다. `compute-sanitizer` memcheck 0 errors, CUDA CTest 7/7, CPU CTest 5/5, CPU pytest 47/47, CUDA cross-language parity 5/5를 확인했다. 구현 커밋은 `ea730c5`, cuda-dense oracle 정정은 `7d4ade6`이다. archive의 `mxfp4.cu.o`에는 native `sm_120` cubin이 있다. 처리량은 아직 측정하지 않았다.
- 명시적 `cpu`, `cuda-dense`, `cuda-custom` CLI와 FP32/BF16 precision 선택, backend/device/transfer/kernel/VRAM profiler JSON, benchmark JSON/CSV 집계를 TDD로 연결했다. CUDA-disabled 요청은 `BACKEND_UNAVAILABLE`로 실패하며 CPU로 fallback하지 않는다. 구현 커밋은 `c92f498`이다.
- 최종 그래프 검증은 CPU CTest 5/5와 pytest 53/53, CUDA CTest 7/7과 pytest 59/59가 통과했다. FP32와 BF16 두 CUDA backend 모두 합성 token `[43,32,28,49,9,28]`을 보존한다.
- 계획 문서의 artifact 명령은 생성기 출력 상위 디렉터리를 converter에 넘겼지만 실제 생성기는 그 아래 `source/`에 checkpoint manifest를 만든다. 실패 traceback에서 `source-manifest.json` 부재를 확인한 뒤 실제 계약대로 `artifacts/synthetic-source/source`를 변환했다. 코드 회귀가 아니라 계획 예시 경로 불일치다.
- 커밋 `c92f498`에서 3 warmup과 20 sample로 측정했다. CPU, FP32 cuda-dense, FP32 cuda-custom decode는 각각 19.4858, 11.6682, 10.1118 tok/s다. BF16 cuda-dense/custom은 11.4957/10.1235 tok/s이며 H2D와 backend peak VRAM은 FP32의 약 절반이다.
- CUDA-event kernel 누적은 전체 run당 11.56~14.52 ms에 불과하지만 end-to-end 실행은 수백 ms다. 현재 합성 병목은 per-operation allocation, staging, synchronous copy/synchronization과 CPU-resident graph다. 다음 구현은 kernel 미세 최적화보다 persistent device buffers와 layer/block batching을 우선한다.
- 사용자는 Milestone 2 권장안인 bounded static weight residency, reusable scratch buffers, grouped projection 조합을 승인했다. 세 기능은 각각 독립 switch와 ablation을 가지며 기존 per-operation/transient/scalar 경로를 reference로 보존한다.
- Resident cache는 tensor ID와 representation/shape metadata를 key로 사용한다. capacity를 넘는 entry는 exact transient staging으로 실행하고 bypass를 기록하며, 이 단계에서는 LRU/LFU/Least-Stale eviction을 도입하지 않는다.
- 첫 grouped call site는 dependency가 없는 KDA Q/K/V, dense/shared gate-up, routed MXFP4 expert gate-up으로 제한한다. 전체 layer GPU executor, async storage, pinned transfer와 tiered cache는 측정 후 다음 경계로 남긴다.
- 사용자가 written Milestone 2 spec을 승인했다. 구현 계획 self-review에서 B-0002 reference가 descriptor/event reuse로 오염되지 않도록 `per-operation`은 buffer, descriptor, heuristic, event lifetime까지 Milestone 1 동작을 보존하고 `reused`에서만 재사용하도록 명확히 했다.
- 구현 계획은 두 CUDA backend의 FP32 8개 switch 조합과 fully-enabled BF16, disjoint grouped output arena, member별 event pool, capacity bypass와 split H2D accounting을 명시한다.
- Milestone 2 Task 1은 새 CUDA 옵션의 reference 기본값과 12개 runtime counter 계약을 TDD로 고정했다. `ComputeBackend`의 새 순수 가상 계약 때문에 계획에 명시된 CPU 파일뿐 아니라 CUDA 구현체에도 옵션·통계 accessor를 최소 연결했으며, CPU CTest 5개와 CUDA CTest 7개가 모두 통과했다.
- Milestone 2 Task 2는 네 CUDA CLI 옵션의 허용값과 CPU/CUDA 조합 규칙을 backend 생성 전에 검증하고, 옵션·runtime counter를 runtime JSON과 benchmark JSON/CSV에 보존한다. 자체 검토에서 CUDA 분리 H2D를 0 placeholder로 기록하는 안을 폐기하고 backend가 실제 성공한 복사를 weight/activation으로 즉시 누적하게 했다. Task 10에서는 profiler event 분류를 추가해 이 backend 계측과 교차 검증한다.
- Task 2 최종 검증은 CPU CTest 5/5와 Python 23 passed/7 skipped, CUDA CTest 7/7과 Python 29 passed/1 skipped이다. `ruff`는 승인된 venv에 설치되어 있지 않아 실행하지 않았고, Python 파일은 pytest import/실행으로 검증했다.
- Milestone 2 Task 3은 dense/MXFP4 weight view에 stable tensor ID와 shape metadata를 추가했다. `Engine`의 모든 dense 연산은 이름에서 계산한 K3X tensor ID를 명시적으로 전달하고, expert는 owned packed/scale byte vector의 lifetime 안에서 view를 구성하므로 host pointer를 cache identity로 사용하지 않는다. CPU group oracle은 전체 member를 먼저 검증한 뒤 기존 scalar 산술을 순서대로 재사용하며 invalid member가 있으면 profiler event를 하나도 추가하지 않는다.
- Task 3 검증은 CPU CTest 5/5와 전체 pytest 63 passed/7 skipped, CUDA CTest 7/7과 CUDA 관련 Python 29 passed/1 skipped이다. CUDA의 현재 group method는 동일 계약의 선검증 후 scalar 실행이며 실제 shared-input batching은 후속 Task 8에서 구현한다.
- Milestone 2 Task 4는 backend 밖의 internal CUDA memory primitive로 `DeviceAllocation`과 `ScratchBuffer`를 추가했다. Scratch growth는 replacement allocation 성공 후 swap하여 실패 시 기존 pointer/capacity/live-byte 상태를 보존하고, 교체 순간의 실제 old+new device memory는 peak VRAM에 포함한다. Fake allocator 실패 테스트와 실제 GPU test가 통과했고 Compute Sanitizer memcheck는 0 errors였다.
- Milestone 2 Task 5는 reference `per-operation` 경로를 tracked local allocation으로 유지하고 `reused`에서 dense 3개, native MXFP4 4개의 grow-only scratch slot을 backend 수명 동안 보존한다. Reused 경로는 timing event와 dense shape/precision별 cuBLASLt descriptor·zero-workspace heuristic도 재사용한다. 동일 shape 두 번째 호출의 allocation 증가는 0이었고, larger dense/MXFP4 shape는 각각 실제로 커진 2개/3개 slot만 교체했다.
- Task 5 검증은 CUDA CTest 8/8, CUDA benchmark schema 4 passed/4 deselected, dense와 MXFP4 Compute Sanitizer 각각 0 errors이다. Reference runtime counter는 allocation/free 균형과 scratch 0을, reused counter는 live scratch와 synchronization 횟수를 실제 기록한다.

## 2026-08-09 Milestone 2

- Task 6 bounded dense residency는 stable tensor ID, representation, rows, cols, group size 전체를 key로 사용한다. 동일 tensor의 FP32/BF16 표현은 별도 entry로 허용하지만 shape/group 충돌은 `invalid_extent`로 거부한다. Hard capacity에 맞지 않는 entry는 miss와 bypass를 기록하고 기존 exact transient path로 실행한다.
- Dense residency 검증은 `cuda_residency`와 `cuda_dense` CTest 통과, `test_cuda_residency` Compute Sanitizer 0 errors이다. 24-byte capacity에서 첫 FP32 weight는 miss/upload, 두 번째 사용은 hit/zero extra weight H2D, 두 번째 24-byte weight는 exact bypass였고 resident bytes는 24를 넘지 않았다.
- Task 7은 native MXFP4 packed E2M1 bytes와 E8M0 scales를 한 논리 key 아래 두 tracked device extent로 보존한다. 102-byte entry는 한 번만 upload되고 다음 호출은 hit이며, 101-byte capacity는 exact transient bypass를 선택한다. Repacking, dequantization, pruning은 수행하지 않는다.
- Task 7 최종 검증은 CUDA CTest 9/9와 `test_cuda_residency`, `test_cuda_mxfp4` Compute Sanitizer 각각 0 errors이다. `cuda-dense` MXFP4 CPU oracle은 resident table을 조회하지 않아 hit/miss/bypass/resident counter가 모두 0으로 유지됐다.
- Task 8 grouped dense는 모든 member를 선검증하고 shared activation을 한 번 upload한 뒤, ordered cuBLASLt matmul과 disjoint output arena D2H를 한 stream에 enqueue하여 group당 한 번만 synchronize한다. KDA Q/K/V, dense MLP gate/up, shared expert gate/up이 grouped mode에서 이 경로를 사용하며 scalar mode는 기존 호출 순서를 유지한다.
- Task 8 검증은 `cuda_dense` CTest 통과와 CUDA parity 24 passed/1 skipped이다. `cuda-dense`와 `cuda-custom` grouped FP32 모두 exact token `[43, 32, 28, 49, 9, 28]` 및 기존 logit/layer/state tolerance를 유지했고 group counter가 실제 증가했다.
- Task 9 native MXFP4 grouped 경로는 routed expert gate/up의 동일 activation을 한 번만 H2D 전송하고, ordered native kernel과 분리된 output arena를 한 stream에 enqueue한 뒤 group당 한 번만 synchronize한다. Expert down projection은 SiTU-GLU 의존성 때문에 scalar로 유지한다.
- Task 9 검증은 CUDA CTest 9/9, CUDA graph parity 36 passed/1 skipped, CPU CTest 5/5, CPU pytest 62 passed/23 skipped, `test_cuda_mxfp4` Compute Sanitizer 0 errors이다. CUDA graph parity에는 두 backend의 FP32 allocation/weight/batching 16개 조합과 fully-enabled BF16 2개 조합이 포함되며 exact token `[43, 32, 28, 49, 9, 28]`을 유지한다.
- Task 10 profiler는 successful H2D event를 immutable weight와 activation으로 분류하고 두 분류의 합을 기존 total H2D로 집계한다. 실패 event는 byte와 시간 집계에서 제외되며 failure count만 증가한다.
- Task 10 ablation runner는 reference, reuse, residency, grouped 네 단계를 고정 순서로 한 process씩 측정하고 단계별 JSON/CSV와 인접 단계 counter delta를 담은 `summary.json`을 쓴다. 1-sample synthetic dry run에서 네 단계 옵션 identity와 `weight_h2d_bytes + activation_h2d_bytes == host_to_device_bytes`가 모두 유지됐다. 이 dry run 수치는 성능 결론으로 사용하지 않는다.
- Task 11 최종 검증은 CPU CTest 5/5와 pytest 65 passed/23 skipped, CUDA CTest 9/9와 pytest 87 passed/1 skipped, 네 CUDA test의 Compute Sanitizer 0 errors이다.
- B-0003에서 FP32 `cuda-dense` reference/reuse/residency/grouped decode는 12.1261/17.4560/18.0041/17.9018 tok/s이고, `cuda-custom`은 12.2647/17.1425/17.2723/16.8348 tok/s이다. Grouping은 activation H2D와 synchronization을 줄였지만 scalar residency보다 느려 기본값으로 채택하지 않는다.
- Fully-enabled BF16은 `cuda-dense` 17.6861 tok/s와 `cuda-custom` 17.0032 tok/s이며 최대 절대 오차 0.00402409와 exact token을 유지했다. FP32 scalar residency보다 빠르지 않고 일반 품질 benchmark가 없으므로 opt-in을 유지한다.

## 2026-08-09 Milestone 3 설계

- 사용자는 Cloud Run 및 유료 자원 단계 전까지 기존 헌장과 안전 범위 안의 단순 승인 작업은 별도 질문 없이 계속 진행하도록 승인했다. 비용, 파괴적 변경, 보안 경계 변경은 이 승인에 포함되지 않는다.
- B-0003 이후 다음 경계로 generic device tensor와 full decoder layer 대신 dependency-closed FFN block을 선택했다. gate/up, strict FP32 SiTU-GLU, down을 한 CUDA call과 최종 synchronization 안에서 실행하며 기존 operation 경로를 reference로 유지한다.
- `ffn-block` runtime mode는 `cuda-custom` 전용으로 제한한다. `cuda-dense`에서 routed MXFP4를 CPU로 내부 처리하면 advertised boundary 안에 silent CPU execution이 섞이므로 지원하지 않는다.
- routed expert block은 선택된 Top-K triplet 전체를 한 group으로 받아 latent input을 한 번만 upload하고 expert 출력만 host로 반환한다. Router score, selection, output mixing은 CPU exact graph에 남는다.
- Sol 읽기 전용 자문은 같은 경계를 권장했고 fast-math 금지, preflight validation, 기존 resident table 재사용, 최종 sync 뒤 event 집계가 핵심 불변식이라고 확인했다. 자문 전후 작업 트리에는 예상 밖 변경이 없었다.
- 구현 계획은 경계 옵션, portable oracle, strict SiTU, dense/shared block, exact MXFP4 expert group, graph 연결, 계측/ablation, B-0004와 원장 갱신의 여덟 의미 단위로 나눴다.
- 연결된 구현의 일관성을 위해 primary agent가 계획을 직접 실행하며, Cloud Run 또는 유료 자원 생성 직전까지 단순 승인 질문 없이 진행한다.
- Milestone 3 Task 1은 `operation|ffn-block` boundary 계약과 두 block 성공 계수를 runtime, CLI, JSON/CSV benchmark schema에 추가했다. `ffn-block`은 backend 생성 전에 `cuda-custom` 외 조합을 거부한다. RED는 enum/counter 컴파일 부재, unknown CLI, dataclass field 부재로 확인했고 GREEN은 CPU backend CTest 2/2와 Python 28 passed/23 skipped, CUDA backend CTest 1/1과 Python 50 passed/1 skipped다.
- Milestone 3 Task 2는 `DenseMlpView`, `Mxfp4MlpView`와 두 block API를 정의하고 CPU backend에서 기존 scalar matvec과 portable `situ_glu()`를 결정론적으로 조합한다. 모든 expert triplet을 먼저 검증하므로 뒤쪽 expert가 잘못돼도 부분 profiler event가 남지 않는다. RED는 새 타입과 method 부재 컴파일 오류로 확인했고 GREEN은 CPU CTest 5/5와 pytest 68 passed/23 skipped다.
- Milestone 3 Task 3은 strict 식 순서를 유지하는 `launch_situ_glu()`와 FP32/BF16 출력 경로를 추가했다. 첫 CUDA build는 Task 2 순수 가상 API의 CUDA override 부재를 드러내 최소 실패 stub으로 계약을 복구했고, 두 번째 RED는 `situ.cuh` 부재에서 확인했다. literal/guard test는 통과했고 Compute Sanitizer는 0 errors, 실제 NVCC flags에는 `--use_fast_math`가 없다.
- Milestone 3 Task 4는 dense/shared FFN triplet을 한 stream에서 input upload, gate/up cuBLASLt, strict SiTU, down cuBLASLt, final output download 순으로 실행한다. warm resident test에서 3 hits, 추가 weight H2D 0, activation H2D 12 bytes, D2H 8 bytes, sync 1회, block call 1회를 확인했다. BF16은 input/weight와 activation을 RNE로 저장하며 독립 rounded oracle 허용오차를 통과했다. `cuda_dense|cuda_ffn` CTest 2/2와 FFN memcheck 0 errors다.
- Milestone 3 Task 5는 선택된 exact native MXFP4 expert triplet 전체를 preflight한 뒤 shared latent를 한 번만 upload하고 ordered expert output을 한 번의 최종 sync로 반환한다. warm group은 six resident hits, 추가 weight H2D 0, activation H2D 128 bytes, D2H 8 bytes, block 1회와 expert 2개를 기록했다. 두 번째 expert reserved scale은 copy, cache counter, profiler event 전에 거부되며 2209-byte capacity에서는 적어도 한 weight를 exact bypass해 CPU oracle과 같은 출력을 냈다. `cuda_mxfp4|cuda_ffn` CTest 2/2와 FFN memcheck 0 errors다.
- Milestone 3 Task 6은 dense, shared, routed expert FFN만 `cuda_boundary=ffn-block`으로 분기하고 router, stable Top-K, routing score, expert order, CPU mixing, residual, KDA/MLA state는 유지한다. diagnostics에 prefill routed expert trace를 추가해 operation과 block의 선택이 같음을 직접 검증했다. FP32/BF16과 scalar/grouped 4조합이 exact token과 routing trace를 보존했다. CPU parity 22 passed/22 skipped, CUDA CTest 11/11, CUDA graph parity 43 passed/1 skipped다.
- Milestone 3 Task 7은 `situ_glu` profiler event, benchmark token IDs, exact 4-case FFN boundary runner를 추가했다. runner는 provenance와 token parity를 검증하고 operation counter 0, block counter 양수, matching case 대비 D2H/sync 감소를 요구한다. 실제 1-sample FP32 dry run은 네 case 모두 exact token이었고 scalar D2H 111600→83952 bytes, sync 630→423, grouped D2H 111600→83952, sync 486→369를 측정했다. 이 결과는 dry run이며 B-0004가 아니다. CUDA benchmark schema는 12/12 통과했다.
- Milestone 3 최종 검증은 CPU CTest 5/5와 pytest 70 passed/26 skipped, CUDA CTest 11/11과 pytest 95 passed/1 skipped다. `test_cuda_dense`, `test_cuda_mxfp4`, `test_cuda_memory`, `test_cuda_residency`, `test_cuda_situ`, `test_cuda_ffn`의 Compute Sanitizer는 각각 0 errors였고 actual NVCC flags에는 fast math가 없다.
- B-0004는 코드 commit `0f6bbdd`, WSL2 Ubuntu 24.04.4, RTX 5080, 3 warmup과 20 sample로 FP32/BF16 각각 operation/ffn-block × scalar/grouped 네 경로를 측정했다. 모든 경로가 exact token `[43, 32, 28, 49, 9, 28]`을 보존했다. FP32 scalar block은 operation 대비 decode 16.3576→17.0713 tok/s, activation H2D 126144→92736 bytes, D2H 111600→83952 bytes, sync 630→423을 기록했다. BF16 scalar block은 16.3874→16.9847 tok/s였지만 최대 절대오차 0.00402409이고 FP32 block보다 느려 opt-in을 유지한다.
- B-0004 compact manifest 8개 레코드는 원본 JSON의 decode, prefill, TTFT, H2D, D2H, synchronization, peak VRAM, kernel time, 최대 절대오차와 token IDs를 자동 대조해 exact match를 확인했다. 합성 artifact SHA-256은 `59c1f83f571fb59dcdad27ef80da8d42b03176dfb5fa63ae5195717c141775ed`다.
- FP32 FFN block scalar는 합성 실험 추천 경로로만 채택하고 reference default는 `operation`으로 유지한다. block 경계가 activation traffic과 synchronization을 줄였지만 kernel time은 증가했고 decode 향상은 4.36%에 그쳐 다음 병목은 CPU 중심 KDA/MLA, routing, residual 및 비-FFN 경계와 launch orchestration이다.
- 최종 Terra high 읽기 전용 리뷰는 FFN-block preflight가 kernel의 고정 E8M0/32 계약과 달리 임의 group size metadata를 허용하는 Important 문제를 발견했다. 64-column fixture의 group size 16/64가 payload 길이 검증을 통과하도록 만든 RED 테스트가 기존 코드에서 실패했고, gate/up/down 모두 `group_size == 32`를 side effect 전에 요구하는 최소 수정으로 GREEN이 됐다. 수정 commit은 `3df8d3f`다.
- 수정 후 CPU CTest 5/5와 pytest 70 passed/26 skipped, CUDA CTest 11/11과 pytest 95 passed/1 skipped, `test_cuda_ffn` Compute Sanitizer 0 errors를 확인했다. FP32/BF16 각 1-sample four-case smoke도 exact token과 traffic/counter 계약을 통과했다. valid group-32 실행부는 바뀌지 않아 B-0004 측정 commit과 수치는 보존하며 smoke 수치를 새 성능 측정으로 사용하지 않는다.

## 2026-08-09 Milestone 4 설계

- 공개 `main`의 Milestone 3 완료 상태에서 `codex/milestone-four-async-l0-l1` 격리 브랜치를 만들었다. baseline은 CPU CTest 5/5와 pytest 70 passed/26 skipped, CUDA CTest 11/11과 pytest 95 passed/1 skipped다.
- 현재 `Reader`는 선택 expert extent를 매번 pageable vector로 동기 읽고, resident admission과 transient H2D는 compute stream에 enqueue된다. 따라서 기존 `cudaMemcpyAsync` 표기만으로 L1→L0 copy/compute overlap을 증명할 수 없다.
- CUDA 13.3 공식 문서에 따라 bounded reusable page-locked slab, 별도 non-default transfer stream, readiness event와 compute-stream wait를 필수 계약으로 선택했다. 무제한 pinning, per-request `cudaHostRegister`, default-stream copy는 배제한다.
- 세 대안 중 router 이후 exact payload를 prepare하고 routed-down projection과 H2D를 겹친 뒤 single-use token으로 consume하는 two-phase 경계를 선택했다. Expert 내부 pipeline은 첫 transfer를 숨기지 못하고, 범용 worker scheduler는 L2/cache/predictor를 성급히 결합하므로 연기했다.
- 첫 async 조합은 `cuda-custom + ffn-block + reused + transient`로 제한하고 synchronous를 기본값으로 보존한다. Static residency와 async admission 결합, L1 persistent cache, L2 NVMe async read, eviction, predictor는 다음 독립 milestone이다.
- 상세 TDD 계획은 공개 계약, 고정 pinned slab, single-flight transfer pipeline, prepared FFN, graph overlap, 계측, B-0005와 원장 갱신의 여덟 의미 단위로 나눴다. 자체 검토에서 copyable opaque token과 backend-enforced single-use를 선택하고, 모든 async counter를 첫 계약 task로 이동했으며, CUDA 임시 override와 synchronous-plus-capacity 거부 검증을 명시했다.
- Milestone 4 Task 1은 synchronous/prefetch 옵션, pinned capacity, opaque token, `INVALID_STATE`, 두 단계 backend API와 12개 zero-default 통계를 TDD로 고정했다. RED는 새 C++ 계약 부재 컴파일 오류와 accepted capability 조합을 제거한 mutation에서 확인했고, GREEN은 CPU backend CTest 2/2, CUDA backend CTest 1/1과 transfer CLI targeted pytest를 통과했다. CUDA prepared API는 후속 pipeline 전까지 명시적으로 `BACKEND_UNAVAILABLE`을 반환한다.
- Milestone 4 Task 2는 backend 밖의 단일 할당 `PinnedBuffer`를 추가했다. 0 byte와 재할당을 거부하고 `cudaHostAlloc` 성공 뒤에만 current/peak pinned byte를 반영하며, 소유한 `cudaFreeHost` 성공 시 current를 0으로 되돌린다. 타입 부재 RED 후 real RTX 5080 CTest가 통과했고 Compute Sanitizer memcheck는 0 errors였다.
- Milestone 4 Task 3은 고정 pinned/device slab, 별도 nonblocking transfer stream, transfer/readiness/wait event와 한 pending request를 소유하는 `AsyncMxfp4Pipeline`을 구현했다. 전체 expert triplet을 native group-32/E8M0 규칙으로 선검증하고 router 순서의 여섯 extent를 padding 없이 한 slab으로 복사한다. 두 pipeline의 첫 token 충돌 RED를 추가로 확인해 process-global monotonic opaque ID로 foreign token도 거부하며, duplicate/stale/layer/phase/capacity/scale/extent 실패가 valid pending request와 성공 counter를 보존한다. 실제 device byte order CTest와 Compute Sanitizer memcheck는 통과했고 0 errors였다.
- Milestone 4 Task 4는 valid prefetch 옵션에서만 pipeline을 생성하고 CUDA 13.3의 실제 `asyncEngineCount`와 `cudaDevAttrGpuOverlap`을 기록한다. Prepared FFN은 activation upload 뒤 readiness wait를 enqueue하고 staged device view로 gate/up, strict SiTU, down을 실행해 expert output만 반환한다. Prepare 뒤 모든 원본 payload를 변조한 RED가 temporary `BACKEND_UNAVAILABLE`에서 실패했고, GREEN은 CPU pre-mutation oracle과 일치했다. 한 final sync, expert 2개, exact 2210-byte weight H2D, cache 무사용, foreign/repeated token과 invalid beta/input의 pending 보존을 확인했으며 FFN/pipeline CTest와 prepared FFN memcheck 0 errors를 통과했다.
- Milestone 4 Task 5는 synchronous MoE 순서를 보존하고 prefetch 모드에서만 routing trace 뒤 exact payload load/prepare, routed-down projection, prepared consume 순으로 재배치했다. Graph acceptance에서 async 실행 여부를 직접 판별해야 하므로 Task 6의 runner 직렬화 중 pinned current, prefetch calls/bytes, wait count 네 필드만 앞당겨 노출했고 benchmark dataclass/CSV와 나머지 계측은 그대로 Task 6에 남겼다. FP32/BF16 및 scalar/grouped에서 operation reference, synchronous transient block, prefetch transient block 모두 token `[43, 32, 28, 49, 9, 28]`과 routing trace를 보존했다. Targeted pytest 6개와 CUDA CTest 14/14가 통과했으며 prefetch는 host synchronization count를 늘리지 않았다.
- Milestone 4 Task 6은 transfer mode/capacity, current/peak pinned bytes, prefetch calls/bytes, ready/late, wait count, staging/transfer/stall time와 device overlap capability를 runtime JSON, `BenchmarkRecord`, JSON/CSV에 연결했다. Ready/late는 sample마다 합이 call 수인지 검증하고 중앙 ready count와 그 보수로 보고해 timing 변동에도 count invariant를 유지한다. CPU schema 8 passed/5 skipped, CUDA schema 13/13이 통과했으며 1-sample real prefetch 측정은 양수인 transfer/pinned counter와 exact token quality를 확인하는 schema 검증일 뿐 성능 결론으로 사용하지 않는다.
- Milestone 4 Task 7은 synchronous/prefetch × scalar/grouped 네 경로만 고정하는 B-0005 runner를 추가했다. 각 raw JSON/CSV 존재, 옵션과 provenance, token/routing parity, H2D 합계, synchronous zero counters, prefetch positive/invariant counters, host synchronization 비증가를 실행 중 강제한다. 단위 테스트 13개와 CPU/CUDA benchmark schema 8 passed/5 skipped 및 13/13이 통과했다. 실제 RTX 5080 FP32 1-sample smoke도 네 경로에서 exact token과 routing을 보존하고 prefetch call/wait 27회, ready-before-use 27회, pinned 1 MiB를 기록했지만 성능 결론으로 사용하지 않는다.
- Milestone 4 전체 검증은 CPU CTest 5/5와 pytest 94 passed/27 skipped, CUDA CTest 14/14와 pytest 120 passed/1 skipped다. CUDA test binary 10개 모두 Compute Sanitizer memcheck `ERROR SUMMARY: 0 errors`를 기록했다. 새 seeded source shard는 Milestone 3 source와 byte-identical이고 converter maximum source read는 257 bytes다. K3X superblock의 crash-resume UUID는 매 변환마다 새로 생성되므로 이번 artifact SHA-256 `e245c52759dffcfaccfe182bbba56fa069288d99f0d70a1cd779169bb51e6993`을 측정 identity로 고정했다.
- B-0005는 commit `99cf1e4`, WSL2 Ubuntu 24.04.4, RTX 5080, 3 warmup/20 samples로 FP32/BF16 synchronous/prefetch × scalar/grouped를 측정했다. 모든 row가 token `[43, 32, 28, 49, 9, 28]`과 24-entry routing trace를 보존했다. Matched decode delta는 FP32 scalar -1.03%, grouped +0.51%, BF16 scalar -0.38%, grouped +0.90%다. Prefetch는 H2D와 host sync를 바꾸지 않고 27/27 request를 use 전에 ready로 만들었지만 1 MiB pinned/device slab과 0.198--0.312 ms exposed stall이 있어 opt-in을 유지한다.
- B-0005 compact manifest의 8개 record는 raw JSON의 throughput, TTFT, RSS, traffic, VRAM, sync, kernel, error, pinned/prefetch/event timing fields와 token/routing/H2D invariants를 PowerShell로 자동 대조해 일치했다. 다음 병목은 synchronous file-to-host materialization과 persistent L1 expert cache 부재이며, L2 NVMe async I/O와 deadline scheduling은 아직 구현되지 않았다.
- Milestone 4 final Terra high 읽기 전용 리뷰는 stored-only use sequence, prepared failure side effect 우려, matched-pair H2D/sync 검증 공백을 Important로 보고했다. 실제 backend는 token/layer/phase/input/parameter를 이미 scratch와 activation H2D 전에 검사하므로 wait를 activation upload 앞으로 옮기지 않았다. 대신 token에 use sequence를 포함해 backend/pipeline 양쪽에서 검증하고, foreign/sequence 실패가 allocation, device memory, activation H2D, wait, profiler H2D를 바꾸지 않으며 valid pending을 보존하는 회귀를 추가했다. Ablation은 matched total/weight/activation H2D와 sync의 exact equality를 강제한다.
- Review fix commit `190459b` 뒤 CPU CTest 5/5와 pytest 98 passed/27 skipped, CUDA CTest 14/14와 pytest 124 passed/1 skipped가 통과했다. `test_cuda_async_pipeline`과 `test_cuda_async_ffn` Compute Sanitizer는 각각 0 errors였고 FP32/BF16 각 1-sample four-case smoke가 exact token/routing 및 강화된 traffic/sync 계약을 통과했다. Valid B-0005 실행 순서는 바뀌지 않아 3×20 측정을 보존한다.
- PR #4의 첫 push/PR correctness run `31298666191`/`31298678056`은 CPU-only CI의 build directory가 `build`인데 테스트가 이름 `build-cpu`만 CPU build로 인식해 1건씩 실패했다. Runtime은 올바르게 `BACKEND_UNAVAILABLE` code 4를 반환했다. Commit `c961026`은 directory 이름 추론을 제거하고 CPU 전용 `test_backend_unavailable` binary 존재로 capability를 판별한다. 로컬 CPU targeted 2/2와 전체 98 passed/27 skipped, CUDA targeted 1 passed/1 skipped를 확인했다.
- 수정 push/PR correctness run `31298872275`/`31298874190`은 모두 성공했다. PR #4는 ancestry 확인 뒤 merge commit 없이 `c961026`으로 public `main`에 fast-forward되어 GitHub에서 MERGED가 됐고, main correctness run `31298966035`도 성공했다. Worktree와 feature branch는 다음 milestone을 위해 보존한다.

## 2026-08-09 Milestone 5 설계

- Public main closure `04e9cbc`와 correctness run `31299092196` 성공 상태에서 `codex/milestone-five-l1-cache` 브랜치를 만들었다. Milestone 4의 prefetch는 `Model::load_expert`가 매 선택마다 수행하는 six synchronous extent reads와 temporary pageable vectors 뒤에서 시작하므로 다음 경계는 persistent L1 source-byte residency다.
- Reader-level tensor cache는 expert-atomic admission과 통계를 잃고 trunk/expert를 섞으며, CUDA backend-owned host cache는 storage를 compute backend에 결합한다. Model과 Reader 사이의 immutable whole-expert store를 선택해 CPU, synchronous CUDA, prepared CUDA가 같은 exact native MXFP4 payload handle을 소비하게 한다.
- 첫 L1 모드는 `disabled|static`만 지원한다. Static은 complete gate/up/down triplet이 hard capacity에 맞을 때만 no-eviction admission하고, 공간이 없으면 exact transient bypass한다. First-observation admission은 storage primitive 검증용이며 LRU/LFU/Least-Stale, task/session prior, transition prediction, eviction, L2 async I/O는 후속 milestone이다.
- B-0006은 fixed `cuda-custom + ffn-block + reused + transient + scalar`에서 L1 disabled/static과 L1-to-L0 synchronous/prefetch를 교차한다. Static은 exact token/routing과 matched GPU traffic을 보존하면서 positive hit/miss, bounded residency, zero selected-capacity bypass, reduced Reader calls/bytes를 보여야 한다. Logical Reader bytes는 physical NVMe traffic으로 표기하지 않는다.
- Milestone 5 runtime은 `disabled|static` L1 옵션, immutable complete-expert shared handle, overflow-guarded hard capacity, exact transient bypass를 구현했다. CPU operation, CUDA synchronous FFN-block, prepared prefetch가 같은 handle 표현을 소비하며 1-byte capacity는 모든 expert를 정확히 bypass한다.
- Full verification은 CPU CTest 6/6와 pytest 106 passed/34 skipped, CUDA CTest 15/15와 pytest 139 passed/1 skipped다. CUDA 바이너리 10개 모두 Compute Sanitizer memcheck 0 errors였다.
- B-0006은 measurement commit `616c857`, seeded chunk-257 artifact SHA-256 `077e10a3ba478e83ac8dfd2509ea51a6ea2bfdfe670b60fcadc7f74b97ff810c`, FP32/BF16 각 3 warmup/20 samples로 측정했다. Static은 18 misses 뒤 36 hits, 29,376 resident bytes, zero bypass를 기록하고 logical Reader calls/bytes를 428/665,616에서 212/606,864로 줄였다.
- Matched disabled/static decode는 FP32 synchronous 16.6714/50.5246, prefetch 16.9078/49.4904, BF16 synchronous 16.5688/47.2476, prefetch 16.7753/50.9757 tok/s다. 모든 행이 exact token `[43,32,28,49,9,28]`, routing, H2D/D2H, FFN count, synchronization을 보존했다. 이 큰 차이는 tiny repeated-route WSL2 graph의 synchronous pageable read 제거 효과이므로 full-model TPS로 해석하지 않고 static을 opt-in으로 유지한다.
- Final Terra high review는 캐시가 generation 호출마다 재생성되어 session warm-cache 계약을 충족하지 못하는 점과 native expert admission 검증이 불충분한 점을 Important로 보고했다. Commit `2a0cb27`은 `RuntimeSession`에 store 수명을 두고 one-shot 호환 API를 유지하며, group-32 packed/scales 크기, reserved E8M0, triplet shape를 admission 전에 검증한다. 연속 generation 회귀에서 두 번째 호출은 misses가 늘지 않고 hits가 54 증가하며 Reader delta가 감소했고, malformed payload는 통계와 residency를 바꾸지 않았다.
- 검토 수정 뒤 B-0006을 같은 artifact와 3 warmup/20 samples 조건으로 다시 측정했다. 위 최초 수치는 이 재측정으로 대체된다. 새 disabled/static decode는 FP32 synchronous 16.5587/47.6845, prefetch 16.7636/50.6235, BF16 synchronous 16.4052/47.7956, prefetch 16.5073/47.6198 tok/s다. 모든 cache, logical Reader, token, routing, GPU traffic 불변식은 동일하게 통과했다.
- Public PR #5의 push/PR correctness run `31301870446`과 `31301880423`이 모두 성공했다. `origin/main`이 `dac4ed0`의 조상임을 확인한 뒤 fast-forward했고, post-merge main correctness run `31301960103`도 성공했다.

## 2026-08-09 Milestone 6 설계

- Public main `a7e8acf`에서 `codex/milestone-six-l2-reader`를 시작했다. 현재 hot path는 extent마다 새 `std::ifstream`을 열고 seek/read하며, L1 miss의 native expert 하나가 여섯 번 이 경로를 호출한다.
- Engine과 page-cache policy를 한 enum에 합치지 않고 `pread|io_uring`과 `buffered|direct` 두 축으로 분리한다. Single-read wrapper와 ordered batch를 함께 두고 expert loader가 gate/up/down packed/scale 여섯 extent를 한 batch로 요청한다.
- `io_uring`은 optional Linux capability로 두며 first comparison에서 registered buffers, SQPOLL, IOPOLL을 제외한다. `O_DIRECT`는 `statx(STATX_DIOALIGN)`으로 확인된 alignment만 사용하고 silent buffered fallback을 금지한다.
- 현재 WSL2 source path는 9p/DrvFS이고 liburing development package가 설치되지 않았다. WSL2는 correctness/smoke에만 쓰며 P44 Pro native-Linux default 결정 근거로 사용하지 않는다. Logical/syscall/process I/O counters도 physical NVMe GB/token으로 표기하지 않는다.
- Milestone 6 Task 1 첫 RED는 `ReaderOptions`, L2 enums, `ExtentRequest`, expanded counters, ordered batch API 부재로 compile 실패했고 CLI는 `--l2-io`를 unknown argument로 거부했다. 최소 계약을 추가한 뒤 exact ordered read, empty batch no-op, invalid range prevalidation, default CLI가 통과했다. CPU 전체는 CTest 7/7과 pytest 122 passed/34 skipped다. 실제 hot data plane은 아직 `std::ifstream` 기준선이므로 persistent `pread` 구현을 주장하지 않는다.
- Task 2 RED는 Reader open 뒤 artifact 경로를 rename하면 첫 tensor read가 실패해 extent마다 path를 다시 여는 사실을 재현했다. Reader-owned data plane은 Linux에서 `O_RDONLY|O_CLOEXEC` descriptor 하나를 유지하고 `pread`의 EINTR, partial progress, EOF를 처리한다. Windows fallback은 기존 portable exact read를 유지한다. CUDA build와 CTest 16/16, Reader/L1 targeted pytest 5개가 통과했다.
- Task 3 RED는 first-generation Reader calls와 batch submissions가 동일해 native expert의 six extents가 개별 batch였음을 재현했다. Loader는 세 projection record를 모두 확인한 뒤 packed/scale 여섯 요청을 한 ordered batch로 제출하고 payload를 원래 gate/up/down 순서로 재구성한다. 18 cache misses에서 logical calls는 유지하면서 batch submissions가 정확히 90 감소했고, CPU 전체 CTest 7/7과 pytest 123 passed/34 skipped, CUDA session 및 synchronous/prefetch L1 targeted 7개가 통과했다.
- Task 4는 WSL Ubuntu에 `liburing-dev` 2.5를 설치하고 `K3X_ENABLE_IO_URING` optional Linux build를 추가했다. Default build는 liburing을 링크하지 않으며 requested unbuilt mode는 `STORAGE_UNAVAILABLE`로 fail closed한다. Bounded ring은 explicit offsets, stable operation index, negative CQE, short read, out-of-order completion, 0x7ffff000-byte operation chunking을 처리하고 `IORING_OP_READ` support를 probe한다. Deterministic completion seam과 real-kernel tensor/session tests가 통과했으며 optional build 전체는 CTest 8/8과 pytest 125 passed/34 skipped다.
- Task 5 RED는 direct와 io-uring-direct 요청이 `STORAGE_UNAVAILABLE`인 상태를 확인했다. Linux direct path는 `STATX_DIOALIGN`의 실제 memory/offset alignment가 모두 있을 때만 `O_DIRECT`를 열고, aligned interval과 move-only bounce buffer에서 정확한 logical slice를 복원한다. Small scale extent, final EOF extent, aligned-byte amplification, full expert/session parity를 두 engine에서 검증했다. WSL root ext4에서는 pread-direct와 io-uring-direct가 통과했고 `/mnt/c` 9p/DrvFS는 alignment unavailable로 명시적으로 거부됐다. Direct-enabled default는 CTest 8/8과 pytest 127 passed/38 skipped, liburing build는 CTest 8/8과 pytest 130 passed/35 skipped다.
- Task 6는 runtime JSON과 benchmark schema에 두 L2 축, queue depth, direct alignment, batch/completion/error, logical/aligned storage byte, Reader storage elapsed time, Linux `/proc/self/io`의 `rchar/read_bytes` delta를 추가했다. B-0007 runner는 네 조합을 독립 교차하고 `STORAGE_UNAVAILABLE`만 capability skip으로 기록하며 다른 오류는 숨기지 않는다. Supported case는 token, 실제 24-entry routing trace, logical calls/bytes parity와 성공 accounting을 강제한다. io_uring 부분 제출은 실제 제출 바이트만 기록하고 제출된 작업을 모두 completion 처리한 뒤 오류를 반환하도록 보강했다. WSL2 ext4 1-sample smoke에서 네 조합 모두 exact였고 logical 665,616 bytes, 428 calls, 158 batches를 보존했으며 direct는 756,736 aligned bytes였다. 이는 capability/correctness smoke이며 성능 기본값 근거가 아니다. 최종 targeted tests는 19 passed/5 skipped, 전체 Python은 default CPU 135 passed/40 skipped, liburing/direct build 136 passed/39 skipped다.
- Task 7 최신 검증은 CPU CTest 8/8와 pytest 135 passed/40 skipped, liburing/direct CTest 8/8와 pytest 136 passed/39 skipped, CUDA CTest 17/17와 pytest 168 passed/7 skipped다. CUDA 바이너리 10개 모두 Compute Sanitizer memcheck 0 errors다. Commit `5049f26`, WSL2 ext4, queue depth 8, 3 warmup/20 samples B-0007은 네 모드에서 exact token/routing과 428 logical calls, 158 batches, 665,616 logical bytes를 보존했다. Buffered는 665,616 bytes, direct는 756,736 bytes를 제출했다. Decode는 pread/buffered 5,870.8082, io_uring/buffered 5,616.1034, pread/direct 163.3491, io_uring/direct 428.8471 tok/s였으나 tiny CPU graph와 WSL2 ext4 capability 측정이므로 native P44 Pro default나 full-model TPS 근거로 쓰지 않는다.
- Final Terra high read-only review는 io_uring submit/wait 오류가 outstanding 요청 또는 stale SQE를 batch buffer 수명 밖에 남길 수 있는 Important 결함과, 아직 없는 multi-megabyte fixture의 현재형 문서 주장 한 건을 찾았다. `IoRingBatchGuard`는 모든 early return에서 buffer보다 먼저 ring을 닫고 Reader를 fail-closed시키며 `EINTR` wait를 재시도한다. 실제 ring lifetime regression, liburing ASan/UBSan CTest 9/9, post-fix 네 조합 exact smoke를 통과했고 설계 문서는 fixture를 proposed로 정정했다. Post-review 전체는 CPU CTest 8/8와 pytest 136/40, liburing CTest 9/9와 pytest 137/39, CUDA CTest 17/17와 pytest 169/7이다. Successful path는 바뀌지 않아 B-0007 3×20 raw 측정은 유지한다.
- Public PR #6의 push/PR correctness run `31305244311`과 `31305256170`이 모두 성공했다. `origin/main`이 `1f2853e`의 조상임을 확인한 뒤 merge commit 없이 public `main`에 fast-forward했고 GitHub에서 PR #6이 MERGED로 확인됐다. Post-merge main correctness run `31305351800`도 성공했다. Worktree와 feature branch는 다음 milestone을 위해 보존한다.
- Milestone 6 publication 기록 commit `11e1d9a`는 branch/main correctness run `31305484133`/`31305487562`에서 모두 성공했다. 보존된 격리 worktree를 public main에서 `codex/milestone-seven-bounded-slice`로 전환했다. Baseline CPU CTest는 8/8이다. 첫 pytest 실행은 `K3X_BUILD_DIR` 미지정으로 존재하지 않는 `build/k3x_run`을 찾아 51건 실패했고, 실제 원인을 확인한 뒤 `K3X_BUILD_DIR=build-cpu`로 재실행하여 136 passed/40 skipped를 확인했다.
- Milestone 7은 실제 released expert 한 개의 17,547,264-byte MXFP4 payload만 물리적으로 생성하는 non-executable storage fixture를 선택했다. Sparse holes는 I/O 증거를 왜곡하고 full-dimension executable graph는 bounded scope를 벗어나므로 거부했다. Optional feature bit로 fixture를 명시하고 전용 expert-load benchmark만 허용한다. 사용자의 Cloud Run 전 단순 승인 사전 허가에 따라 별도 승인 질문 없이 명세와 계획을 진행한다.
- Milestone 7 구현 계획은 streaming source fixture, converter optional identity/resume, C++ execution guard, exact six-extent benchmark, B-0008 ablation, 측정/ledger/publication의 여섯 독립 검증 단위로 고정했다. 각 production behavior는 실제 artifact를 이용한 RED-GREEN test를 먼저 거치며, token benchmark schema를 storage benchmark에 재사용하지 않는다.
- Milestone 7 Task 1 RED는 `k3x_ref.storage_fixture` 부재로 collection 실패했다. GREEN은 safetensors shard를 `.partial`에서 bounded chunks로 물리 생성하고 fsync/atomic replace한 뒤 deterministic manifest를 기록한다. Targeted pytest 2 passed이고 1 MiB CLI 생성은 payload 17,547,264 bytes, 최대 chunk 1,048,576 bytes, shard SHA-256 `1e310ebdcdd7a8a7ec124fac1e59ca44bbdf5da1ef279502c81a8b70f06379f5`를 보고했다. 첫 독립 검사 명령은 PowerShell/WSL quoting 오류로 시작되지 않았고, 분리한 실제 CLI와 pytest inspect 경계로 재검증했다.
- Milestone 7 Task 2 RED는 storage optional constant 부재로 collection 실패했다. GREEN은 `k3-storage-slice-v1`의 kind/config/ID/세 matrix shape와 17,547,264-byte payload를 fail-closed 검증하고 optional bit 0을 K3X에 기록한다. 실제 slice conversion, malformed source, two-extent resume, changed-source rejection을 포함한 targeted 21건과 전체 CPU pytest 146 passed/40 skipped가 통과했다. 자체 검토에서 synthetic resume fingerprint 호환성을 보존하도록 optional feature가 0이 아닐 때만 feature identity를 fingerprint에 추가했고 resume/format 18건을 재검증했다.
- Milestone 7 Task 3 RED는 C++ Reader가 storage optional bit를 노출하지 않아 test mode가 10을 반환했고 runtime이 `INVALID_EXTENT: missing FP32 tensor: model.embeddings`까지 진입했다. GREEN은 optional bit 0을 C++ superblock에 보존하고 모든 generation overload가 공유하는 경계에서 `NON_EXECUTABLE_ARTIFACT`를 반환한다. Targeted 2 passed, CPU CTest 8/8, 전체 pytest 148 passed/40 skipped다.
- Milestone 7 Task 4 첫 RED는 `k3x_storage_bench` binary 부재로 2건이 명시적으로 실패했다. 구현은 metadata-only Reader로 gate/up/down data+scale 여섯 extent를 한 batch에 읽고 ordered SHA-256, exact bytes, latency percentiles, Reader/storage/process-I/O counters만 JSON으로 출력하며 token field를 만들지 않는다. Targeted 2 passed, CPU CTest 8/8, liburing build targeted 2 passed다. 자체 검토에서 storage fixture가 alphabetic `down/gate/up` 물리 순서로 pack되는 결함과 2-sample p95가 median보다 작아지는 percentile 결함을 각각 재현했고, execution order `gate/up/down`과 nearest-rank percentile로 수정한 뒤 targeted 3 passed와 CPU CTest 8/8을 재확인했다.
- Milestone 7 Task 5 RED는 B-0008 module 부재로 collection 실패했다. GREEN runner는 네 Reader 조합을 실제 executable로 호출하고 identity/digest/logical bytes/counters/direct alignment/percentile/process-I/O를 검증하며 `STORAGE_UNAVAILABLE`만 skip한다. WSL2 ext4 1-sample smoke는 네 모드 모두 digest `e5fb7939474a57ab9263a791999d76ba078bd767cc3f155f3522b1bec576c7e4`, 17,547,264 bytes, six completions, zero failures를 보존했고 actual dimensions가 512-byte 정렬에 맞아 direct byte amplification은 0이었다. 이 수치는 smoke이며 ledger benchmark가 아니다. 전체는 CPU CTest 8/8와 pytest 152/40, liburing CTest 9/9와 pytest 157/35, CUDA CTest 17/17와 pytest 185/7, ASan/UBSan liburing CTest 9/9와 storage targeted pytest 4 passed다.
- B-0008은 code commit `9198ed2`, WSL2 ext4 artifact, queue depth 8, 3 warmup/20 samples로 측정했다. 네 row 모두 ordered digest `e5fb7939474a57ab9263a791999d76ba078bd767cc3f155f3522b1bec576c7e4`, 120 completions, 350,945,280 logical/submitted bytes, zero failure/short read를 보존했다. Median wall/Reader ms는 pread-buffered 50.685/4.661, io_uring-buffered 51.592/4.579, pread-direct 60.402/14.832, io_uring-direct 56.426/11.633이다. Direct amplification은 0%다. Artifact SHA-256은 `b14610fd2b405dd97c09004fb29157f5b318522591546337bce89e7e8a6a2b65`이며 raw/summary 교차 확인은 exact다. 이는 allocation+hash를 포함한 WSL2 expert-load 측정이며 tok/s, cold-cache, P44 Pro, full-model 증거가 아니다.
- 첫 Compute Sanitizer loop 두 번은 외부 PowerShell이 Bash `$target`을 제거해 target-invalid 메시지를 냈지만 도구가 0을 반환했으므로 성공으로 세지 않았다. 각 CUDA binary를 이름으로 명시해 WSL ext4 `/tmp/k3x-csan-m7`에 복사하고 개별 실행한 최종 검증에서 10개 모두 `ERROR SUMMARY: 0 errors`를 보고했다.
- Milestone 7 Terra high final review는 Critical 0건, Important 2건으로 병합 불가 판정했다. 원인은 storage manifest의 shard/tensor SHA-256이 생성만 되고 변환 경계에서 검증되지 않는 점과, resume ledger가 완료 extent의 canonical prefix, 길이, offset, 현재 source CRC를 검증하지 않는 점이다. 두 문제를 별도 실패 재현 테스트로 확인한 뒤 최소 수정한다.
- 리뷰 수정의 RED 단계에서 source 변조·digest 불일치·manifest 게시 실패 3건과 resume ledger 손상 5건이 모두 재현됐다. Content-addressed shard와 manifest-last 게시, bounded shard/tensor SHA-256 검증, manifest가 참조하는 shard만의 source fingerprint, canonical prefix 및 source/partial 이중 CRC 검증으로 닫았다. 대상 스위트 32/32, CPU CTest 8/8 및 pytest 161/40, liburing CTest 9/9 및 pytest 162/39, CUDA CTest 17/17 및 pytest 194/7이 통과했다. B-0008 payload/Reader/benchmark code는 바뀌지 않아 측정 수치를 재작성하지 않았다.
- Public PR #7은 branch/push correctness run `31308034386`과 PR run `31308046877` 성공 후 ready로 전환했고, `e9a5d4d`를 공개 `main`에 fast-forward하여 GitHub가 merged로 기록했다. Post-merge main correctness run `31308147456`도 성공했다. Merge commit은 별도로 만들지 않았다.
- Milestone 8은 Reader native async API나 미래-layer predictor를 선행하지 않는다. Blocking Reader를 worker 내부에서 재사용하는 bounded deadline queue를 만들고, 현재 layer의 정확한 natural Top-K가 결정된 뒤 routed/shared 독립 계산과 expert load를 겹친다. N+1/N+2 lookahead는 scheduler API의 후속 소비자이며 이번 구현 상태로 주장하지 않는다.
- Task 1 scheduler는 single bounded worker와 absolute latest-start priority를 사용한다. Controlled blocker 뒤에 요청을 쌓는 deterministic test가 smaller slack 우선, stable sequence tie, queue-full fail-closed, loader failure, ticket single-consumption, resident inline completion, destructor drain을 검증했고 5회 반복 통과했다.
- Task 2 RED에서 동일 L1 key의 동시 miss는 loader를 8회 실행했고, shared Reader 병렬 pread는 payload는 성공했지만 `calls` counter가 손실됐다. `HostExpertStore::get_or_load`와 `Reader::read_extents`의 data-plane/counter 구간만 각각 mutex로 직렬화해 pread/io_uring 동시성 테스트를 통과했다. TSan 빌드는 성공했지만 WSL2에서 `unexpected memory mapping`으로 실행 불가했고, ASan/UBSan liburing에서 scheduler, L1, pread/io_uring 병렬 경로가 통과했다.
- Task 3 deadline mode는 router가 확정한 current-layer natural Top-K만 제출하고, resident hit를 inline 처리한 뒤 non-resident load를 worker에 넣는다. Main thread는 routed-down 및 shared FFN을 계산하고 expert use 직전에 ticket을 기다린다. Blocking mode의 순서와 기본값은 그대로다. Static L1 session parity에서 tokens, routing, hit/miss, Reader calls/requested/completed bytes가 exact였고 scheduler submissions/completions가 일치했다. Pending callable은 Engine이 아니라 Reader와 session-owned store만 캡처해 failure unwind 수명을 보존한다.
- Task 4는 `--l2-schedule blocking|deadline`을 추가하고 blocking을 기본으로 유지한다. JSON은 submissions, resident inline, completions, ready/late, estimated misses, requested bytes, queue high-water, worker time, exposed wait를 기록한다. Blocking/deadline CLI parity와 invalid `fifo` rejection, benchmark schema tests 9/5가 통과했다. N+1/N+2 lookahead는 여전히 명시적으로 미구현이다.
- Task 5 B-0009 runner는 L1 static 65,536 bytes에서 `blocking|deadline × pread|io_uring × buffered|direct` 8개 조합을 측정한다. `STORAGE_UNAVAILABLE`만 pair 단위 capability skip으로 허용하고, 모든 supported row에서 exact tokens/routing/L1 counters/logical Reader calls와 bytes를 강제한다. Blocking scheduler counters는 전부 0이어야 하고 deadline은 submissions/completions/readiness, inline-resident hit, requested bytes, queue activity, worker time 불변식을 만족해야 한다. RED는 module 부재로 collection 실패했고 GREEN targeted tests는 10/10, 실제 build 경로를 지정한 benchmark/parity 묶음은 68 passed/36 skipped, CPU CTest는 9/9다.
- B-0009은 review-fix measurement code commit `68b3e54`, artifact SHA-256 `392b9237274e5580b665cf95afbda9a09e8d01ba7484bed00cf83a4ae99eb4fa`, WSL2 ext4 warm 상태, 3 warmup/20 samples로 교체 측정했다. 8개 row 모두 exact tokens/routing, 606,864 logical bytes, 212 completions, zero failure/short read를 보존했다. Deadline row는 54 submissions/completions와 36 inline hits를 기록했다. Decode tok/s는 blocking/deadline 순서로 pread-buffered 6,508.251/5,112.555, io_uring-buffered 6,234.853/4,971.308, pread-direct 808.171/768.502, io_uring-direct 1,966.491/1,766.937이었다. 이 tiny warm WSL2 결과에서는 deadline이 모두 느려 기본 활성화 근거가 없으며 native P44 Pro, cold-cache, full-model 성능 증거가 아니다. Raw/summary 교차검증 test는 11/11이다.
- Milestone 8 Terra high final review는 Reader counters와 L1 stats의 잠금 없는 참조, 예외 반환 뒤 raw Reader/store capture가 남을 수 있는 수명 문제를 Important 3건으로 판정했다. Snapshot API를 잠금 아래 값 반환으로 바꾸고, per-layer fetch estimate를 첫 submit 전에 한 번 캡처했으며, scheduler idle barrier를 generation 성공·예외 반환 양쪽에 연결했다. API 반환형과 동시 snapshot, idle barrier 회귀를 추가했고 targeted pytest 56/35 및 CPU CTest 9/9가 통과했다. Estimate capture 시점이 바뀌어 B-0009를 `68b3e54`에서 교체 측정했고 raw/summary 교차검증 11/11이 통과했다.
- 동일 Terra high 재검토는 이전 Important 3건이 모두 종결됐고 새 Critical/Important가 없다고 판정했다. Post-review 전체 검증은 CPU CTest 9/9와 pytest 175/41, liburing/direct CTest 10/10과 pytest 177/39, CUDA CTest 18/18과 pytest 208/8, ASan/UBSan liburing CTest 10/10과 targeted pytest 69/33이다. CUDA test binary 10개는 모두 Compute Sanitizer 0 errors다. TSan은 build 성공 후 WSL2 runtime의 `unexpected memory mapping`으로 실행 불가하여 결과를 주장하지 않는다.
- Public PR #8은 branch push correctness run `31310497619`와 PR run `31310504588` 성공 후 ready로 전환했고, `a289172`를 공개 `main`에 fast-forward하여 GitHub가 MERGED로 기록했다. Post-merge main correctness run `31310608231`도 성공했다. Merge commit은 별도로 만들지 않았다.
- Milestone 8 publication record `07dd36b`는 branch/main correctness run `31310722378`/`31310723374`에서 모두 성공했다. 보존 worktree를 public main에서 `codex/milestone-nine-expert-cache-policies`로 전환했다.
- SpecMD arXiv 2602.03921의 Least-Stale는 이전 forward cycle의 stale queue를 current queue보다 먼저 퇴거하고 각 큐에서 layer-position FIFO를 사용하는 spatial-temporal 정책이다. 논문은 공개 구현을 아직 `open-sourcing underway`로 표시하고 author-linked repository가 확인되지 않아 K3X는 code port가 아닌 paper reproduction으로 기록한다. Milestone 9는 disabled/static reference를 보존하며 exact LRU/LFU/Least-Stale, selected-set protection, collision miss accounting, B-0010만 범위에 둔다.
- Milestone 9 Task 1은 동일 크기 expert trace로 LRU의 최근 사용, LFU의 사용 빈도, Least-Stale의 stale-cycle 후 layer-position 순서를 결정적으로 검증한다. 현재 natural Top-K selected set은 퇴거 후보에서 제외하고, 같은 forward cycle에서 퇴거된 expert를 다시 요청할 때 collision miss를 기록한다. 새 정책 단위 테스트와 CPU CTest 9/9가 통과했다.
- Task 2는 `lru`, `lfu`, `least-stale` CLI identity를 추가하고 disabled 기본값을 보존했다. Forward cycle은 재사용되는 `RuntimeSession`이 원자적으로 발급하여 generation 간 cycle ID 재사용을 막고, Engine은 각 MoE layer의 natural Top-K 전체를 admission 전에 표시한다. 1-expert 용량 테스트가 selected-set exact bypass를 검증했고, blocking/deadline 경로의 새 정책들은 token, routing, logits, Reader byte 계약을 유지했다. CPU CTest 9/9와 `test_cpp_parity.py` 56 passed/31 skipped가 통과했다.
- Task 3 telemetry는 eviction과 same-forward collision miss를 C++ JSON, deterministic benchmark sample, `BenchmarkRecord`, JSON/CSV에 연결했다. SpecMD 원문의 `left` 정의와 upcoming-layer 보호를 재대조해 Least-Stale가 stale current/future를 절대 layer 오름차순으로 퇴거하던 초기 comparator를 폐기했다. 수정 comparator는 이미 처리한 left layer를 먼저, 불가피한 future fallback에서는 가장 먼 layer를 먼저 퇴거하며 deterministic trace에서 LRU collision 1, Least-Stale 0을 검증했다. B-0010 runner는 disabled 기준선과 4개 정책을 2·8·16-expert 용량에서 교차하며 exact token/routing·numerical error과 cache/Reader accounting을 강제한다. 관련 pytest는 76 passed/36 skipped, CPU CTest는 9/9이다.
- Milestone 9 final review의 Important 1건은 동일 `RuntimeSession`의 동시 generation이 store의 단일 active cycle/layer/selected set을 덮어쓰는 경계였다. 전체 generation을 session mutex로 직렬화하고 별도 thread의 generation이 guard 해제 전에 완료되지 않는 회귀 테스트를 추가했다. 리뷰의 B-0010 collision 9 모순 지적은 기각했다. 이전 token에서 잔류한 future-layer expert가 현재 token의 앞 layer에서 퇴거되고 뒤 layer에서 재요청되는 것이 same-forward collision이며, 기존 결정적 trace가 이 경로를 직접 재현한다.
