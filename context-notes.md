# K3X 컨텍스트 노트

## 2026-08-08

- 워크스페이스는 시작 시 완전히 비어 있었고 Git 저장소가 아니었다.
- 사용자는 Python/PyTorch reference와 C++20 CPU runtime, 후속 CUDA backend 조합을 승인했다.
- 첫 마일스톤의 실행 범위는 텍스트 디코더이다. MoonViT-V2는 아키텍처 연결 경계만 문서화하고 실행 구현은 연기한다.
- 첫 마일스톤은 KDA, Gated MLA, Attention Residual, Stable LatentMoE, router, native MXFP4 decode, incremental state 및 greedy generation의 correctness를 검증한다.
- 합성 모델은 4개 decoder layer를 `KDA, KDA, KDA, MLA` 순서로 배치한다. 실제 모델의 3 KDA 뒤 1 MLA 패턴을 최소 크기로 재현하기 위한 결정이다.
- 합성 Attention Residual block size는 여러 depth source를 작은 모델에서 실제로 테스트하기 위해 2로 축소한다. 실제 checkpoint 값은 12이며 문서에서 명확히 구분한다.
- Native MXFP4 expert는 dequantize 후 requantize하지 않는다. 합성 converter도 payload와 scale의 byte-exact 보존을 검증한다.
- K3X v1은 4 KiB superblock, little-endian fixed-width records, aligned extents, per-extent CRC32C와 최종 SHA-256, required/optional feature bits를 사용한다.
- 미완성 변환은 `.partial` artifact와 resume manifest에 남기고, 모든 extent 검증 후 원자적으로 최종 이름으로 바꾼다.
- 확인한 공식 Kimi K3 config의 주요 값은 hidden 7168, 93 layers, 69 KDA, 24 MLA, 96 heads, latent MoE 3584, 896 experts, Top-16, 2 shared experts, AttnRes block 12, KDA short convolution 4이다.
- 조사 시점의 source HEAD는 설계 명세의 source ledger에 고정한다. 이후 upstream 변경은 자동으로 현재 설계의 근거가 되지 않는다.
- 사용자가 작성된 첫 마일스톤 설계 명세를 승인했다.
- 구현 계획 작성 시 기본 환경에서 Python 3.14.6만 확인되었고 PyTorch, CMake, C++ compiler 및 WSL은 없었다. Codex 번들 Python 3.12.13은 사용할 수 있지만 PyTorch는 설치되어 있지 않다.
- 2026-08-08 PyPI index에서 확인한 계획상 Python dependency version은 torch 2.13.0, numpy 2.5.1, safetensors 0.8.0, pytest 9.1.1, google-crc32c 1.8.0, psutil 7.2.2이며 build backend는 setuptools 83.0.0이다. 설치 전에는 사용자에게 package-install 권한을 별도로 확인한다.
- 첫 마일스톤 구현은 연결된 correctness graph이므로 기본 실행 방식은 주 agent의 inline execution으로 둔다. 독립 agent 사용은 사용자가 명시적으로 요청할 때만 선택한다.
- 사용자가 로컬 dependency 설치와 전체 구현 진행을 승인했다. 격리 worktree `feat/milestone-zero`에서 작업한다.
- 저장소 전용 `.venv`에 Python 3.12.13, torch 2.13.0, numpy 2.5.1, safetensors 0.8.0, pytest 9.1.1, google-crc32c 1.8.0, psutil 7.2.2, CMake 4.4.2, Ninja 1.13.0을 설치했다. 시스템에는 MSVC 19.51이 이미 있어 LLVM 설치는 진행하지 않았다.
- 합성 config 계약 테스트는 의도한 `ModuleNotFoundError`로 RED를 확인한 뒤 2개가 통과했다. `pip check`도 문제 없음으로 통과했다.
- 고정한 PipeNetwork revision의 vendored text reference와 MLX port를 재검증한 결과 K3 MLA는 `mla_use_nope=true`이며 `qk_rope_head_dim`으로 명명된 64차원 subspace에도 rotary embedding을 적용하지 않는다. 사용자 승인에 따라 설계와 계획에서 이를 shared extra-key NoPE 경로로 정정했다.
- FP32 RMSNorm, SiTU-GLU, native MXFP4 E2M1/E8M0 decode와 reference matmul 테스트 6개가 통과했다. Nibble 순서를 의도적으로 뒤집었을 때 literal test가 실패함을 확인하고 low-nibble-first 구현을 복원했다.
- KDA reference는 causal depthwise ShortConv history, head-wise `A_log`, channel-wise forget logits, sigmoid beta, delta write 후 read, full-rank output gate를 명시적 state로 구현했다. Pre-update state를 읽도록 변이했을 때 literal recurrence test가 실패함을 확인했다.
- Gated MLA reference는 q-LoRA와 normalized KV latent, main key/value, shared extra NoPE key, output gate를 명시적으로 저장한다. 길이 1, 2, 5에서 KDA와 MLA 모두 prefill과 token-by-token decode 출력 및 최종 state가 exact match했다.
- 전체 그래프의 full-prefix와 incremental 경로는 layer 0부터 GEMM batch shape가 달라 최대 `9.31e-9`, 최종 logits에서 최대 `3.58e-7`의 정상적인 FP32 반올림 차이가 측정됐다. 따라서 cross-path 및 cross-language state는 `atol=rtol=1e-6` 수치 비교를 사용하고, byte-exact digest는 같은 runtime의 반복 실행에만 사용한다.
- 공식 vendored `config.json`에서 SiTU beta 4.0, linear beta 25.0, MLA output gate 활성화, routed scaling factor 1.0을 확인해 합성 config와 K3X model metadata에 반영했다.
- Router의 correction bias는 선택에만 사용하고 unbiased sigmoid score를 normalize한다. Stable LatentMoE는 native MXFP4 routed branch와 원래 hidden-space shared branch를 분리하며, AttnRes는 normalized key로 score한 뒤 raw source를 혼합한다.
- Controlled graph는 prompt `[1,7,3,9]`에서 full/incremental 모두 token 5를 6회 생성했다. Seeded graph도 두 mode의 greedy token이 exact match했고, source shard와 golden fixture를 두 번 생성한 SHA-256 manifest가 동일했다.
- K3X v1 writer는 safetensors header만 읽어 tensor extent를 찾고, 설정된 chunk 크기 이하로 payload를 복사한다. native MXFP4 packed code와 scale은 별도 aligned extent로 byte-for-byte 보존한다.
- 각 extent는 flush와 fsync 뒤 partial 파일에서 다시 읽어 CRC32C를 확인한 후에만 원자적 resume ledger에 기록한다. source fingerprint가 달라진 resume는 거부한다.
- 4 KiB superblock literal layout, 128-byte tensor record, extent 정렬·중복·절단 검증, payload corruption, unknown required feature, source mutation, 중단 재개, CLI dry-run 및 native MXFP4 byte parity를 포함해 Python 전체 35개 테스트가 3.49초에 통과했다.
- C++20 runtime은 외부 ML library 없이 portable CRC32C/SHA-256, strict K3X reader, RMSNorm, SiTU, native MXFP4와 합성 KDA/MLA/Attention Residual/Stable LatentMoE graph를 구현한다. Root SHA-256은 1 MiB heap buffer로 증분 검증해 artifact 전체를 RAM에 올리지 않는다.
- C++ reader는 Python writer artifact를 열고 payload CRC corruption, unknown required feature, truncation을 동일한 안정적 오류 코드로 거부했다. C++ full/incremental greedy 결과는 PyTorch의 `[43,32,28,49,9,28]`과 모두 exact match했다.
- 첫 cross-language 실행의 Windows `0xC00000FD`는 reader 함수의 1 MiB stack SHA buffer 때문임을 확인했다. heap buffer로 이동한 뒤 같은 parity test가 통과했다.
- C++ runtime에 prefill, decode, layer별 단조 시계 계측을 추가하고 별도 benchmark driver에서 프로세스 RSS, 논리적 artifact read bytes, KDA/MLA state 크기를 수집한다. 첫 토큰은 prefill logits에서 나오므로 decode tok/s 계산에서는 이후 5개 토큰만 사용한다.
- 최종 리뷰에서 첫 token의 argmax 시계 구간과 실행기 무결성 검증 생략을 발견해 바로잡았다. 2026-08-08 Windows 11 AMD64, MSVC Debug, 3 warmup과 20회 재측정에서 합성 모델 prefill 405.11 tok/s, 실제 5회 incremental forward decode 558.89 tok/s, strict 전체 artifact 검증을 포함한 process-level TTFT 중앙값 86.20 ms, peak child RSS 6,270,976 bytes, logical tensor read 110,936 bytes/generated token을 측정했다. 이는 합성 correctness harness 결과이며 실제 Kimi K3 또는 RTX 5080 성능 근거가 아니다.
- 공개 config 차원으로 계산한 native MXFP4 routed expert는 17,547,264 bytes이다. cache reuse가 전혀 없는 natural Top-16의 92개 MoE layer expert traffic은 25,829,572,608 bytes/token이다. P44 Pro 공개 최대 7.0 GB/s만 적용한 expert-only ceiling은 0.27 tok/s이므로 다음 실제 병목은 NVMe expert traffic 회피율이다.
- 공식 차원으로 계산한 BF16 text trunk 추정치는 약 85.72 GB이며 전체 checkpoint scan 결과가 아니다. 96 GB RAM에서 OS와 expert bank까지 확보하려면 sensitivity-aware trunk quantization이 필요하다는 capacity planning 근거로만 사용한다.
- GitHub Actions는 Linux에서 C++ build/CTest 뒤 Python 및 cross-language suite를 실행한다. 테스트 runner 경로는 Windows `.exe`와 POSIX binary를 모두 찾도록 정정했다.
- 최종 diff 리뷰에서 발견한 실행기 `metadata_only`, 불완전한 C++ layer/expert directory parser, final rename 직후 stale ledger, token-only C++ parity를 모두 회귀 테스트와 함께 수정했다. 실행기는 생성 전에 artifact 전체 무결성을 검증하고, C++ 진단 mode는 prompt logits, 네 layer 출력, canonicalized KDA/MLA state를 PyTorch와 `1e-6` 허용오차로 비교한다.

## 2026-08-08 Milestone 1

- 사용자는 CPU exact runtime과 profiler를 먼저 확장하고, 이어서 RTX 5080용 basic CUDA backend를 만드는 순서를 승인했다.
- custom CUDA only, cuBLASLt only, hybrid baseline을 비교했고 사용자는 cuBLASLt 기준 경로와 custom MXFP4 경로를 함께 두는 hybrid 설계를 승인했다.
- 실제 개발 PC에서 NVIDIA GeForce RTX 5080 16,303 MiB, compute capability 12.0, driver 591.86, CUDA toolkit 13.3과 nvcc 13.3.73을 확인했다.
- 설치된 nvcc는 `compute_120`과 `sm_120`을 지원한다. CUDA 13.3 local header만 보면 E2M1과 UE8M0/32 mode가 함께 보이지만, NVIDIA 공식 cuBLAS 문서는 FP4에 UE4M3/16을 요구하고 UE8M0/32를 FP8 mode로 규정한다. K3의 E2M1+E8M0/32 native MXFP4와 직접 호환되지 않으므로 cuBLASLt FP4 expert path는 기각하고 cuBLASLt는 dense baseline에만 사용한다.
- 격리 worktree `feat/milestone-one-runtime`을 만들고 Python 3.12.13 및 `pyproject.toml`의 고정 dependencies를 새 `.venv`에 설치했다.
- baseline의 첫 Python 실행은 C++ build 전이라 cross-language runner 7개가 없어서 실패했다. MSVC Developer Command 환경과 `.venv`의 Ninja 경로를 명시해 Release build와 CTest 2/2 통과를 확인했다.
- 새 `k3x_run.exe` 실행은 Windows Smart App Control policy `{0283ac0f-fff1-49ae-ada1-8a933130cad6}`에 의해 차단됐다. Code Integrity event 3033과 3077에서 unsigned executable이 Enterprise signing level을 충족하지 못했다는 원인을 확인했다. Python suite는 이 환경 제약으로 cross-language 5개가 차단되고 나머지 41개가 통과했다.
- K3X는 Smart App Control을 자동으로 끄거나 신뢰 저장소를 수정하지 않는다. CUDA runtime 완료 판정은 Linux native 또는 사용자가 별도로 승인한 WSL2 GPU 환경에서 수행한다.
- 사용자는 Milestone 1 hybrid 설계 명세를 승인하고 TITAN LEDGER 지속성 프로토콜을 추가했다. `PROJECT_CHARTER.md`는 안정된 헌장, `ARCHITECTURE.md`는 상태가 표시된 실제 구조, `DECISIONS.md`는 선택 근거, `BENCHMARKS.md`는 측정 원장, `PROJECT_STATE.md`는 마지막에 갱신하는 현재 상태로 분리한다.
- APOLLO, TITAN COUNCIL, AURORA, PROMETHEUS-X, MERCURY, ORBIT, HELIOS, SHADOW, PHOENIX, VAULT, VEILBREAK, AUTO는 구현·benchmark 전까지 proposed이다. ATLAS, CHRONOS, BLACKSTAR는 역할 정의가 제공되지 않아 reserved proposed/undefined로 기록했다.
- Windows에는 WSL distribution이 설치되어 있지 않다. WSL2 설치는 시스템 변경과 재부팅 가능성이 있으므로 명시적 사용자 승인 전에는 실행하지 않는다.
- Milestone 1 구현 계획은 Linux GPU 실행 환경을 선행 관문으로 두고 profiler, CPU backend extraction, optional CUDA shell, cuBLASLt dense, custom MXFP4, end-to-end profiling, 최종 측정 순서로 작성했다.
- 사용자가 inline execution과 WSL2 Ubuntu 24.04 설치를 명시적으로 승인했다. 비관리자 `wsl --install`은 기능 비활성 상태에서 exit 1로 종료됐고, 관리자 DISM으로 `Microsoft-Windows-Subsystem-Linux`와 `VirtualMachinePlatform`만 `/NoRestart` 활성화했다. 두 기능 모두 `InstallState=1`을 확인했으며 CBS/PendingFileRename 재부팅 대기 상태라 자동 재부팅 없이 중단했다.
- 재부팅 후 WSL 2.7.11.0과 Ubuntu 24.04.4 LTS를 설치했다. Linux kernel은 6.18.33.2이며 RTX 5080 16,303 MiB, compute capability 12.0, Windows driver 591.86이 WSL에 정상 노출된다.
- NVIDIA WSL 지침에 따라 Linux display driver는 설치하지 않고 WSL 저장소의 `cuda-toolkit-13-3`만 설치했다. 실제 package version은 13.3.1-1, nvcc는 13.3.73이며 `sm_120`을 제공한다.
- Ubuntu 기본 개발 사용자는 비관리자 `jolib`로 설정했다. Python 환경은 `/home/jolib/.venvs/k3x-m1`, native build는 Git에서 무시되는 `build-linux`를 사용한다.
- 생산 코드 변경 전 Linux Release baseline은 CTest 2/2와 cross-language 제외 pytest 39/39가 통과했다.
- `K3X_BUILD_DIR` resolver의 RED ImportError를 확인한 뒤 공통 native binary resolver를 구현했다. targeted cross-language 8/8, 전체 pytest 47/47, CTest 2/2가 통과했고 커밋은 `b6d900f`다.
- deterministic profiler는 명시적 event만 소유하며 clock, thread, JSON, CUDA 의존성을 갖지 않는다. 실패 event는 실패 횟수만 증가시키고 시간·byte 합계에서는 제외하며 H2D와 D2H bytes를 분리한다. unknown target RED 후 CTest 3/3과 pytest 47/47을 확인했고 커밋은 `f06ce97`이다.
- exact CPU backend는 기존 double 누산 dense와 native MXFP4 matvec만 격리한다. graph는 명시적 backend와 prefill/decode phase를 전달하고 LM head는 global layer로 기록한다. literal backend RED/GREEN, CTest 4/4, cross-language parity 5/5, 전체 pytest 47/47을 확인했고 커밋은 `b439e25`이다.
- Release `NDEBUG`가 최근 `assert` 기반 profiler/backend test 표현식을 제거해 unit checks가 실행되지 않은 문제를 발견했다. 기존 suite 방식인 explicit return codes로 교체해 실제 RED와 GREEN을 재확인했고 커밋은 `d1b52d4`이다.
- optional CUDA shell은 OFF에서 typed unavailable stub만 링크하고 CUDA dependency가 없으며, ON에서 CUDA 13.3, native `sm_120`, capability 12.0 gate, nonblocking stream, cuBLASLt handle을 RAII로 소유한다. CPU/CUDA CTest 각각 5/5, CPU 전체 pytest 47/47, CUDA cross-language parity 5/5를 확인했고 커밋은 `5b6d1e7`이다. CUDA matrix compute는 아직 미구현이다.
- cuBLASLt dense baseline은 row-major FP32 host API를 유지하고 FP32 또는 BF16-rounded GPU operand를 zero-workspace heuristic으로 실행하며 FP32로 누산·반환한다. CUDA 13.3의 정규 행렬 계약은 BF16 `Atype/Btype`을 함께 요구하므로 BF16 mode는 입력과 가중치를 모두 RNE 반올림해 staging한다. literal test에서 FP32 H2D/D2H는 36/8 bytes, BF16-rounded는 18/8 bytes이며 두 mode 모두 독립 oracle과 일치한다. `compute-sanitizer` memcheck 0 errors, CUDA CTest 6/6, CPU CTest 5/5, CPU pytest 47/47, CUDA cross-language parity 5/5를 확인했고 구현 커밋은 `c4b612a`다. 이는 correctness 측정이며 throughput benchmark는 아니다.
- exact custom MXFP4 baseline은 K3X low-nibble-first E2M1과 E8M0/32 payload를 재패킹 없이 읽고, output row당 256-thread block 하나에서 FP32 shared-memory reduction을 수행한다. 3-row/64-column literal은 두 scale group, sign, exponent, high/low nibble을 검증하고 320-column mutation fixture는 두 번째 thread stride를 제거하면 실패한다. invalid extent, non-32 group, reserved `0xFF` scale을 거부한다. 승인된 comparison 계약에 맞춰 `cuda_dense`는 CPU MXFP4 oracle을 유지하며 H2D와 device time이 0이고, custom kernel은 `cuda_custom`에서만 실행된다. `compute-sanitizer` memcheck 0 errors, CUDA CTest 7/7, CPU CTest 5/5, CPU pytest 47/47, CUDA cross-language parity 5/5를 확인했다. 구현 커밋은 `ea730c5`, cuda-dense oracle 정정은 `7d4ade6`이다. archive의 `mxfp4.cu.o`에는 native `sm_120` cubin이 있다. 처리량은 아직 측정하지 않았다.
- 명시적 `cpu`, `cuda-dense`, `cuda-custom` CLI와 FP32/BF16 precision 선택, backend/device/transfer/kernel/VRAM profiler JSON, benchmark JSON/CSV 집계를 TDD로 연결했다. CUDA-disabled 요청은 `BACKEND_UNAVAILABLE`로 실패하며 CPU로 fallback하지 않는다. 구현 커밋은 `c92f498`이다.
- 최종 그래프 검증은 CPU CTest 5/5와 pytest 53/53, CUDA CTest 7/7과 pytest 59/59가 통과했다. FP32와 BF16 두 CUDA backend 모두 합성 token `[43,32,28,49,9,28]`을 보존한다.
- 계획 문서의 artifact 명령은 생성기 출력 상위 디렉터리를 converter에 넘겼지만 실제 생성기는 그 아래 `source/`에 checkpoint manifest를 만든다. 실패 traceback에서 `source-manifest.json` 부재를 확인한 뒤 실제 계약대로 `artifacts/synthetic-source/source`를 변환했다. 코드 회귀가 아니라 계획 예시 경로 불일치다.
- 커밋 `c92f498`에서 3 warmup과 20 sample로 측정했다. CPU, FP32 cuda-dense, FP32 cuda-custom decode는 각각 19.4858, 11.6682, 10.1118 tok/s다. BF16 cuda-dense/custom은 11.4957/10.1235 tok/s이며 H2D와 backend peak VRAM은 FP32의 약 절반이다.
- CUDA-event kernel 누적은 전체 run당 11.56~14.52 ms에 불과하지만 end-to-end 실행은 수백 ms다. 현재 합성 병목은 per-operation allocation, staging, synchronous copy/synchronization과 CPU-resident graph다. 다음 구현은 kernel 미세 최적화보다 persistent device buffers와 layer/block batching을 우선한다.
- 사용자는 Milestone 2 권장안인 bounded static weight residency, reusable scratch buffers, grouped projection 조합을 승인했다. 세 기능은 각각 독립 switch와 ablation을 가지며 기존 per-operation/transient/scalar 경로를 reference로 보존한다.
- Resident cache는 tensor ID와 representation/shape metadata를 key로 사용한다. capacity를 넘는 entry는 exact transient staging으로 실행하고 bypass를 기록하며, 이 단계에서는 LRU/LFU/Least-Stale eviction을 도입하지 않는다.
- 첫 grouped call site는 dependency가 없는 KDA Q/K/V, dense/shared gate-up, routed MXFP4 expert gate-up으로 제한한다. 전체 layer GPU executor, async storage, pinned transfer와 tiered cache는 측정 후 다음 경계로 남긴다.
- 사용자가 written Milestone 2 spec을 승인했다. 구현 계획 self-review에서 B-0002 reference가 descriptor/event reuse로 오염되지 않도록 `per-operation`은 buffer, descriptor, heuristic, event lifetime까지 Milestone 1 동작을 보존하고 `reused`에서만 재사용하도록 명확히 했다.
- 구현 계획은 두 CUDA backend의 FP32 8개 switch 조합과 fully-enabled BF16, disjoint grouped output arena, member별 event pool, capacity bypass와 split H2D accounting을 명시한다.
- Milestone 2 Task 1은 새 CUDA 옵션의 reference 기본값과 12개 runtime counter 계약을 TDD로 고정했다. `ComputeBackend`의 새 순수 가상 계약 때문에 계획에 명시된 CPU 파일뿐 아니라 CUDA 구현체에도 옵션·통계 accessor를 최소 연결했으며, CPU CTest 5개와 CUDA CTest 7개가 모두 통과했다.
- Milestone 2 Task 2는 네 CUDA CLI 옵션의 허용값과 CPU/CUDA 조합 규칙을 backend 생성 전에 검증하고, 옵션·runtime counter를 runtime JSON과 benchmark JSON/CSV에 보존한다. 자체 검토에서 CUDA 분리 H2D를 0 placeholder로 기록하는 안을 폐기하고 backend가 실제 성공한 복사를 weight/activation으로 즉시 누적하게 했다. Task 10에서는 profiler event 분류를 추가해 이 backend 계측과 교차 검증한다.
- Task 2 최종 검증은 CPU CTest 5/5와 Python 23 passed/7 skipped, CUDA CTest 7/7과 Python 29 passed/1 skipped이다. `ruff`는 승인된 venv에 설치되어 있지 않아 실행하지 않았고, Python 파일은 pytest import/실행으로 검증했다.
- Milestone 2 Task 3은 dense/MXFP4 weight view에 stable tensor ID와 shape metadata를 추가했다. `Engine`의 모든 dense 연산은 이름에서 계산한 K3X tensor ID를 명시적으로 전달하고, expert는 owned packed/scale byte vector의 lifetime 안에서 view를 구성하므로 host pointer를 cache identity로 사용하지 않는다. CPU group oracle은 전체 member를 먼저 검증한 뒤 기존 scalar 산술을 순서대로 재사용하며 invalid member가 있으면 profiler event를 하나도 추가하지 않는다.
- Task 3 검증은 CPU CTest 5/5와 전체 pytest 63 passed/7 skipped, CUDA CTest 7/7과 CUDA 관련 Python 29 passed/1 skipped이다. CUDA의 현재 group method는 동일 계약의 선검증 후 scalar 실행이며 실제 shared-input batching은 후속 Task 8에서 구현한다.
- Milestone 2 Task 4는 backend 밖의 internal CUDA memory primitive로 `DeviceAllocation`과 `ScratchBuffer`를 추가했다. Scratch growth는 replacement allocation 성공 후 swap하여 실패 시 기존 pointer/capacity/live-byte 상태를 보존하고, 교체 순간의 실제 old+new device memory는 peak VRAM에 포함한다. Fake allocator 실패 테스트와 실제 GPU test가 통과했고 Compute Sanitizer memcheck는 0 errors였다.
- Milestone 2 Task 5는 reference `per-operation` 경로를 tracked local allocation으로 유지하고 `reused`에서 dense 3개, native MXFP4 4개의 grow-only scratch slot을 backend 수명 동안 보존한다. Reused 경로는 timing event와 dense shape/precision별 cuBLASLt descriptor·zero-workspace heuristic도 재사용한다. 동일 shape 두 번째 호출의 allocation 증가는 0이었고, larger dense/MXFP4 shape는 각각 실제로 커진 2개/3개 slot만 교체했다.
- Task 5 검증은 CUDA CTest 8/8, CUDA benchmark schema 4 passed/4 deselected, dense와 MXFP4 Compute Sanitizer 각각 0 errors이다. Reference runtime counter는 allocation/free 균형과 scratch 0을, reused counter는 live scratch와 synchronization 횟수를 실제 기록한다.

## 2026-08-09 Milestone 2

- Task 6 bounded dense residency는 stable tensor ID, representation, rows, cols, group size 전체를 key로 사용한다. 동일 tensor의 FP32/BF16 표현은 별도 entry로 허용하지만 shape/group 충돌은 `invalid_extent`로 거부한다. Hard capacity에 맞지 않는 entry는 miss와 bypass를 기록하고 기존 exact transient path로 실행한다.
- Dense residency 검증은 `cuda_residency`와 `cuda_dense` CTest 통과, `test_cuda_residency` Compute Sanitizer 0 errors이다. 24-byte capacity에서 첫 FP32 weight는 miss/upload, 두 번째 사용은 hit/zero extra weight H2D, 두 번째 24-byte weight는 exact bypass였고 resident bytes는 24를 넘지 않았다.
- Task 7은 native MXFP4 packed E2M1 bytes와 E8M0 scales를 한 논리 key 아래 두 tracked device extent로 보존한다. 102-byte entry는 한 번만 upload되고 다음 호출은 hit이며, 101-byte capacity는 exact transient bypass를 선택한다. Repacking, dequantization, pruning은 수행하지 않는다.
- Task 7 최종 검증은 CUDA CTest 9/9와 `test_cuda_residency`, `test_cuda_mxfp4` Compute Sanitizer 각각 0 errors이다. `cuda-dense` MXFP4 CPU oracle은 resident table을 조회하지 않아 hit/miss/bypass/resident counter가 모두 0으로 유지됐다.
- Task 8 grouped dense는 모든 member를 선검증하고 shared activation을 한 번 upload한 뒤, ordered cuBLASLt matmul과 disjoint output arena D2H를 한 stream에 enqueue하여 group당 한 번만 synchronize한다. KDA Q/K/V, dense MLP gate/up, shared expert gate/up이 grouped mode에서 이 경로를 사용하며 scalar mode는 기존 호출 순서를 유지한다.
- Task 8 검증은 `cuda_dense` CTest 통과와 CUDA parity 24 passed/1 skipped이다. `cuda-dense`와 `cuda-custom` grouped FP32 모두 exact token `[43, 32, 28, 49, 9, 28]` 및 기존 logit/layer/state tolerance를 유지했고 group counter가 실제 증가했다.
- Task 9 native MXFP4 grouped 경로는 routed expert gate/up의 동일 activation을 한 번만 H2D 전송하고, ordered native kernel과 분리된 output arena를 한 stream에 enqueue한 뒤 group당 한 번만 synchronize한다. Expert down projection은 SiTU-GLU 의존성 때문에 scalar로 유지한다.
- Task 9 검증은 CUDA CTest 9/9, CUDA graph parity 36 passed/1 skipped, CPU CTest 5/5, CPU pytest 62 passed/23 skipped, `test_cuda_mxfp4` Compute Sanitizer 0 errors이다. CUDA graph parity에는 두 backend의 FP32 allocation/weight/batching 16개 조합과 fully-enabled BF16 2개 조합이 포함되며 exact token `[43, 32, 28, 49, 9, 28]`을 유지한다.
- Task 10 profiler는 successful H2D event를 immutable weight와 activation으로 분류하고 두 분류의 합을 기존 total H2D로 집계한다. 실패 event는 byte와 시간 집계에서 제외되며 failure count만 증가한다.
- Task 10 ablation runner는 reference, reuse, residency, grouped 네 단계를 고정 순서로 한 process씩 측정하고 단계별 JSON/CSV와 인접 단계 counter delta를 담은 `summary.json`을 쓴다. 1-sample synthetic dry run에서 네 단계 옵션 identity와 `weight_h2d_bytes + activation_h2d_bytes == host_to_device_bytes`가 모두 유지됐다. 이 dry run 수치는 성능 결론으로 사용하지 않는다.
- Task 11 최종 검증은 CPU CTest 5/5와 pytest 65 passed/23 skipped, CUDA CTest 9/9와 pytest 87 passed/1 skipped, 네 CUDA test의 Compute Sanitizer 0 errors이다.
- B-0003에서 FP32 `cuda-dense` reference/reuse/residency/grouped decode는 12.1261/17.4560/18.0041/17.9018 tok/s이고, `cuda-custom`은 12.2647/17.1425/17.2723/16.8348 tok/s이다. Grouping은 activation H2D와 synchronization을 줄였지만 scalar residency보다 느려 기본값으로 채택하지 않는다.
- Fully-enabled BF16은 `cuda-dense` 17.6861 tok/s와 `cuda-custom` 17.0032 tok/s이며 최대 절대 오차 0.00402409와 exact token을 유지했다. FP32 scalar residency보다 빠르지 않고 일반 품질 benchmark가 없으므로 opt-in을 유지한다.

## 2026-08-09 Milestone 3 설계

- 사용자는 Cloud Run 및 유료 자원 단계 전까지 기존 헌장과 안전 범위 안의 단순 승인 작업은 별도 질문 없이 계속 진행하도록 승인했다. 비용, 파괴적 변경, 보안 경계 변경은 이 승인에 포함되지 않는다.
- B-0003 이후 다음 경계로 generic device tensor와 full decoder layer 대신 dependency-closed FFN block을 선택했다. gate/up, strict FP32 SiTU-GLU, down을 한 CUDA call과 최종 synchronization 안에서 실행하며 기존 operation 경로를 reference로 유지한다.
- `ffn-block` runtime mode는 `cuda-custom` 전용으로 제한한다. `cuda-dense`에서 routed MXFP4를 CPU로 내부 처리하면 advertised boundary 안에 silent CPU execution이 섞이므로 지원하지 않는다.
- routed expert block은 선택된 Top-K triplet 전체를 한 group으로 받아 latent input을 한 번만 upload하고 expert 출력만 host로 반환한다. Router score, selection, output mixing은 CPU exact graph에 남는다.
- Sol 읽기 전용 자문은 같은 경계를 권장했고 fast-math 금지, preflight validation, 기존 resident table 재사용, 최종 sync 뒤 event 집계가 핵심 불변식이라고 확인했다. 자문 전후 작업 트리에는 예상 밖 변경이 없었다.
- 구현 계획은 경계 옵션, portable oracle, strict SiTU, dense/shared block, exact MXFP4 expert group, graph 연결, 계측/ablation, B-0004와 원장 갱신의 여덟 의미 단위로 나눴다.
- 연결된 구현의 일관성을 위해 primary agent가 계획을 직접 실행하며, Cloud Run 또는 유료 자원 생성 직전까지 단순 승인 질문 없이 진행한다.
- Milestone 3 Task 1은 `operation|ffn-block` boundary 계약과 두 block 성공 계수를 runtime, CLI, JSON/CSV benchmark schema에 추가했다. `ffn-block`은 backend 생성 전에 `cuda-custom` 외 조합을 거부한다. RED는 enum/counter 컴파일 부재, unknown CLI, dataclass field 부재로 확인했고 GREEN은 CPU backend CTest 2/2와 Python 28 passed/23 skipped, CUDA backend CTest 1/1과 Python 50 passed/1 skipped다.
- Milestone 3 Task 2는 `DenseMlpView`, `Mxfp4MlpView`와 두 block API를 정의하고 CPU backend에서 기존 scalar matvec과 portable `situ_glu()`를 결정론적으로 조합한다. 모든 expert triplet을 먼저 검증하므로 뒤쪽 expert가 잘못돼도 부분 profiler event가 남지 않는다. RED는 새 타입과 method 부재 컴파일 오류로 확인했고 GREEN은 CPU CTest 5/5와 pytest 68 passed/23 skipped다.
- Milestone 3 Task 3은 strict 식 순서를 유지하는 `launch_situ_glu()`와 FP32/BF16 출력 경로를 추가했다. 첫 CUDA build는 Task 2 순수 가상 API의 CUDA override 부재를 드러내 최소 실패 stub으로 계약을 복구했고, 두 번째 RED는 `situ.cuh` 부재에서 확인했다. literal/guard test는 통과했고 Compute Sanitizer는 0 errors, 실제 NVCC flags에는 `--use_fast_math`가 없다.
- Milestone 3 Task 4는 dense/shared FFN triplet을 한 stream에서 input upload, gate/up cuBLASLt, strict SiTU, down cuBLASLt, final output download 순으로 실행한다. warm resident test에서 3 hits, 추가 weight H2D 0, activation H2D 12 bytes, D2H 8 bytes, sync 1회, block call 1회를 확인했다. BF16은 input/weight와 activation을 RNE로 저장하며 독립 rounded oracle 허용오차를 통과했다. `cuda_dense|cuda_ffn` CTest 2/2와 FFN memcheck 0 errors다.
- Milestone 3 Task 5는 선택된 exact native MXFP4 expert triplet 전체를 preflight한 뒤 shared latent를 한 번만 upload하고 ordered expert output을 한 번의 최종 sync로 반환한다. warm group은 six resident hits, 추가 weight H2D 0, activation H2D 128 bytes, D2H 8 bytes, block 1회와 expert 2개를 기록했다. 두 번째 expert reserved scale은 copy, cache counter, profiler event 전에 거부되며 2209-byte capacity에서는 적어도 한 weight를 exact bypass해 CPU oracle과 같은 출력을 냈다. `cuda_mxfp4|cuda_ffn` CTest 2/2와 FFN memcheck 0 errors다.
- Milestone 3 Task 6은 dense, shared, routed expert FFN만 `cuda_boundary=ffn-block`으로 분기하고 router, stable Top-K, routing score, expert order, CPU mixing, residual, KDA/MLA state는 유지한다. diagnostics에 prefill routed expert trace를 추가해 operation과 block의 선택이 같음을 직접 검증했다. FP32/BF16과 scalar/grouped 4조합이 exact token과 routing trace를 보존했다. CPU parity 22 passed/22 skipped, CUDA CTest 11/11, CUDA graph parity 43 passed/1 skipped다.
- Milestone 3 Task 7은 `situ_glu` profiler event, benchmark token IDs, exact 4-case FFN boundary runner를 추가했다. runner는 provenance와 token parity를 검증하고 operation counter 0, block counter 양수, matching case 대비 D2H/sync 감소를 요구한다. 실제 1-sample FP32 dry run은 네 case 모두 exact token이었고 scalar D2H 111600→83952 bytes, sync 630→423, grouped D2H 111600→83952, sync 486→369를 측정했다. 이 결과는 dry run이며 B-0004가 아니다. CUDA benchmark schema는 12/12 통과했다.
- Milestone 3 최종 검증은 CPU CTest 5/5와 pytest 70 passed/26 skipped, CUDA CTest 11/11과 pytest 95 passed/1 skipped다. `test_cuda_dense`, `test_cuda_mxfp4`, `test_cuda_memory`, `test_cuda_residency`, `test_cuda_situ`, `test_cuda_ffn`의 Compute Sanitizer는 각각 0 errors였고 actual NVCC flags에는 fast math가 없다.
- B-0004는 코드 commit `0f6bbdd`, WSL2 Ubuntu 24.04.4, RTX 5080, 3 warmup과 20 sample로 FP32/BF16 각각 operation/ffn-block × scalar/grouped 네 경로를 측정했다. 모든 경로가 exact token `[43, 32, 28, 49, 9, 28]`을 보존했다. FP32 scalar block은 operation 대비 decode 16.3576→17.0713 tok/s, activation H2D 126144→92736 bytes, D2H 111600→83952 bytes, sync 630→423을 기록했다. BF16 scalar block은 16.3874→16.9847 tok/s였지만 최대 절대오차 0.00402409이고 FP32 block보다 느려 opt-in을 유지한다.
- B-0004 compact manifest 8개 레코드는 원본 JSON의 decode, prefill, TTFT, H2D, D2H, synchronization, peak VRAM, kernel time, 최대 절대오차와 token IDs를 자동 대조해 exact match를 확인했다. 합성 artifact SHA-256은 `59c1f83f571fb59dcdad27ef80da8d42b03176dfb5fa63ae5195717c141775ed`다.
- FP32 FFN block scalar는 합성 실험 추천 경로로만 채택하고 reference default는 `operation`으로 유지한다. block 경계가 activation traffic과 synchronization을 줄였지만 kernel time은 증가했고 decode 향상은 4.36%에 그쳐 다음 병목은 CPU 중심 KDA/MLA, routing, residual 및 비-FFN 경계와 launch orchestration이다.
- 최종 Terra high 읽기 전용 리뷰는 FFN-block preflight가 kernel의 고정 E8M0/32 계약과 달리 임의 group size metadata를 허용하는 Important 문제를 발견했다. 64-column fixture의 group size 16/64가 payload 길이 검증을 통과하도록 만든 RED 테스트가 기존 코드에서 실패했고, gate/up/down 모두 `group_size == 32`를 side effect 전에 요구하는 최소 수정으로 GREEN이 됐다. 수정 commit은 `3df8d3f`다.
- 수정 후 CPU CTest 5/5와 pytest 70 passed/26 skipped, CUDA CTest 11/11과 pytest 95 passed/1 skipped, `test_cuda_ffn` Compute Sanitizer 0 errors를 확인했다. FP32/BF16 각 1-sample four-case smoke도 exact token과 traffic/counter 계약을 통과했다. valid group-32 실행부는 바뀌지 않아 B-0004 측정 commit과 수치는 보존하며 smoke 수치를 새 성능 측정으로 사용하지 않는다.

## 2026-08-09 Milestone 4 설계

- 공개 `main`의 Milestone 3 완료 상태에서 `codex/milestone-four-async-l0-l1` 격리 브랜치를 만들었다. baseline은 CPU CTest 5/5와 pytest 70 passed/26 skipped, CUDA CTest 11/11과 pytest 95 passed/1 skipped다.
- 현재 `Reader`는 선택 expert extent를 매번 pageable vector로 동기 읽고, resident admission과 transient H2D는 compute stream에 enqueue된다. 따라서 기존 `cudaMemcpyAsync` 표기만으로 L1→L0 copy/compute overlap을 증명할 수 없다.
- CUDA 13.3 공식 문서에 따라 bounded reusable page-locked slab, 별도 non-default transfer stream, readiness event와 compute-stream wait를 필수 계약으로 선택했다. 무제한 pinning, per-request `cudaHostRegister`, default-stream copy는 배제한다.
- 세 대안 중 router 이후 exact payload를 prepare하고 routed-down projection과 H2D를 겹친 뒤 single-use token으로 consume하는 two-phase 경계를 선택했다. Expert 내부 pipeline은 첫 transfer를 숨기지 못하고, 범용 worker scheduler는 L2/cache/predictor를 성급히 결합하므로 연기했다.
- 첫 async 조합은 `cuda-custom + ffn-block + reused + transient`로 제한하고 synchronous를 기본값으로 보존한다. Static residency와 async admission 결합, L1 persistent cache, L2 NVMe async read, eviction, predictor는 다음 독립 milestone이다.
- 상세 TDD 계획은 공개 계약, 고정 pinned slab, single-flight transfer pipeline, prepared FFN, graph overlap, 계측, B-0005와 원장 갱신의 여덟 의미 단위로 나눴다. 자체 검토에서 copyable opaque token과 backend-enforced single-use를 선택하고, 모든 async counter를 첫 계약 task로 이동했으며, CUDA 임시 override와 synchronous-plus-capacity 거부 검증을 명시했다.
- Milestone 4 Task 1은 synchronous/prefetch 옵션, pinned capacity, opaque token, `INVALID_STATE`, 두 단계 backend API와 12개 zero-default 통계를 TDD로 고정했다. RED는 새 C++ 계약 부재 컴파일 오류와 accepted capability 조합을 제거한 mutation에서 확인했고, GREEN은 CPU backend CTest 2/2, CUDA backend CTest 1/1과 transfer CLI targeted pytest를 통과했다. CUDA prepared API는 후속 pipeline 전까지 명시적으로 `BACKEND_UNAVAILABLE`을 반환한다.
- Milestone 4 Task 2는 backend 밖의 단일 할당 `PinnedBuffer`를 추가했다. 0 byte와 재할당을 거부하고 `cudaHostAlloc` 성공 뒤에만 current/peak pinned byte를 반영하며, 소유한 `cudaFreeHost` 성공 시 current를 0으로 되돌린다. 타입 부재 RED 후 real RTX 5080 CTest가 통과했고 Compute Sanitizer memcheck는 0 errors였다.
- Milestone 4 Task 3은 고정 pinned/device slab, 별도 nonblocking transfer stream, transfer/readiness/wait event와 한 pending request를 소유하는 `AsyncMxfp4Pipeline`을 구현했다. 전체 expert triplet을 native group-32/E8M0 규칙으로 선검증하고 router 순서의 여섯 extent를 padding 없이 한 slab으로 복사한다. 두 pipeline의 첫 token 충돌 RED를 추가로 확인해 process-global monotonic opaque ID로 foreign token도 거부하며, duplicate/stale/layer/phase/capacity/scale/extent 실패가 valid pending request와 성공 counter를 보존한다. 실제 device byte order CTest와 Compute Sanitizer memcheck는 통과했고 0 errors였다.
- Milestone 4 Task 4는 valid prefetch 옵션에서만 pipeline을 생성하고 CUDA 13.3의 실제 `asyncEngineCount`와 `cudaDevAttrGpuOverlap`을 기록한다. Prepared FFN은 activation upload 뒤 readiness wait를 enqueue하고 staged device view로 gate/up, strict SiTU, down을 실행해 expert output만 반환한다. Prepare 뒤 모든 원본 payload를 변조한 RED가 temporary `BACKEND_UNAVAILABLE`에서 실패했고, GREEN은 CPU pre-mutation oracle과 일치했다. 한 final sync, expert 2개, exact 2210-byte weight H2D, cache 무사용, foreign/repeated token과 invalid beta/input의 pending 보존을 확인했으며 FFN/pipeline CTest와 prepared FFN memcheck 0 errors를 통과했다.
- Milestone 4 Task 5는 synchronous MoE 순서를 보존하고 prefetch 모드에서만 routing trace 뒤 exact payload load/prepare, routed-down projection, prepared consume 순으로 재배치했다. Graph acceptance에서 async 실행 여부를 직접 판별해야 하므로 Task 6의 runner 직렬화 중 pinned current, prefetch calls/bytes, wait count 네 필드만 앞당겨 노출했고 benchmark dataclass/CSV와 나머지 계측은 그대로 Task 6에 남겼다. FP32/BF16 및 scalar/grouped에서 operation reference, synchronous transient block, prefetch transient block 모두 token `[43, 32, 28, 49, 9, 28]`과 routing trace를 보존했다. Targeted pytest 6개와 CUDA CTest 14/14가 통과했으며 prefetch는 host synchronization count를 늘리지 않았다.
- Milestone 4 Task 6은 transfer mode/capacity, current/peak pinned bytes, prefetch calls/bytes, ready/late, wait count, staging/transfer/stall time와 device overlap capability를 runtime JSON, `BenchmarkRecord`, JSON/CSV에 연결했다. Ready/late는 sample마다 합이 call 수인지 검증하고 중앙 ready count와 그 보수로 보고해 timing 변동에도 count invariant를 유지한다. CPU schema 8 passed/5 skipped, CUDA schema 13/13이 통과했으며 1-sample real prefetch 측정은 양수인 transfer/pinned counter와 exact token quality를 확인하는 schema 검증일 뿐 성능 결론으로 사용하지 않는다.
- Milestone 4 Task 7은 synchronous/prefetch × scalar/grouped 네 경로만 고정하는 B-0005 runner를 추가했다. 각 raw JSON/CSV 존재, 옵션과 provenance, token/routing parity, H2D 합계, synchronous zero counters, prefetch positive/invariant counters, host synchronization 비증가를 실행 중 강제한다. 단위 테스트 13개와 CPU/CUDA benchmark schema 8 passed/5 skipped 및 13/13이 통과했다. 실제 RTX 5080 FP32 1-sample smoke도 네 경로에서 exact token과 routing을 보존하고 prefetch call/wait 27회, ready-before-use 27회, pinned 1 MiB를 기록했지만 성능 결론으로 사용하지 않는다.
- Milestone 4 전체 검증은 CPU CTest 5/5와 pytest 94 passed/27 skipped, CUDA CTest 14/14와 pytest 120 passed/1 skipped다. CUDA test binary 10개 모두 Compute Sanitizer memcheck `ERROR SUMMARY: 0 errors`를 기록했다. 새 seeded source shard는 Milestone 3 source와 byte-identical이고 converter maximum source read는 257 bytes다. K3X superblock의 crash-resume UUID는 매 변환마다 새로 생성되므로 이번 artifact SHA-256 `e245c52759dffcfaccfe182bbba56fa069288d99f0d70a1cd779169bb51e6993`을 측정 identity로 고정했다.
- B-0005는 commit `99cf1e4`, WSL2 Ubuntu 24.04.4, RTX 5080, 3 warmup/20 samples로 FP32/BF16 synchronous/prefetch × scalar/grouped를 측정했다. 모든 row가 token `[43, 32, 28, 49, 9, 28]`과 24-entry routing trace를 보존했다. Matched decode delta는 FP32 scalar -1.03%, grouped +0.51%, BF16 scalar -0.38%, grouped +0.90%다. Prefetch는 H2D와 host sync를 바꾸지 않고 27/27 request를 use 전에 ready로 만들었지만 1 MiB pinned/device slab과 0.198--0.312 ms exposed stall이 있어 opt-in을 유지한다.
- B-0005 compact manifest의 8개 record는 raw JSON의 throughput, TTFT, RSS, traffic, VRAM, sync, kernel, error, pinned/prefetch/event timing fields와 token/routing/H2D invariants를 PowerShell로 자동 대조해 일치했다. 다음 병목은 synchronous file-to-host materialization과 persistent L1 expert cache 부재이며, L2 NVMe async I/O와 deadline scheduling은 아직 구현되지 않았다.
- Milestone 4 final Terra high 읽기 전용 리뷰는 stored-only use sequence, prepared failure side effect 우려, matched-pair H2D/sync 검증 공백을 Important로 보고했다. 실제 backend는 token/layer/phase/input/parameter를 이미 scratch와 activation H2D 전에 검사하므로 wait를 activation upload 앞으로 옮기지 않았다. 대신 token에 use sequence를 포함해 backend/pipeline 양쪽에서 검증하고, foreign/sequence 실패가 allocation, device memory, activation H2D, wait, profiler H2D를 바꾸지 않으며 valid pending을 보존하는 회귀를 추가했다. Ablation은 matched total/weight/activation H2D와 sync의 exact equality를 강제한다.
- Review fix commit `190459b` 뒤 CPU CTest 5/5와 pytest 98 passed/27 skipped, CUDA CTest 14/14와 pytest 124 passed/1 skipped가 통과했다. `test_cuda_async_pipeline`과 `test_cuda_async_ffn` Compute Sanitizer는 각각 0 errors였고 FP32/BF16 각 1-sample four-case smoke가 exact token/routing 및 강화된 traffic/sync 계약을 통과했다. Valid B-0005 실행 순서는 바뀌지 않아 3×20 측정을 보존한다.
- PR #4의 첫 push/PR correctness run `31298666191`/`31298678056`은 CPU-only CI의 build directory가 `build`인데 테스트가 이름 `build-cpu`만 CPU build로 인식해 1건씩 실패했다. Runtime은 올바르게 `BACKEND_UNAVAILABLE` code 4를 반환했다. Commit `c961026`은 directory 이름 추론을 제거하고 CPU 전용 `test_backend_unavailable` binary 존재로 capability를 판별한다. 로컬 CPU targeted 2/2와 전체 98 passed/27 skipped, CUDA targeted 1 passed/1 skipped를 확인했다.
- 수정 push/PR correctness run `31298872275`/`31298874190`은 모두 성공했다. PR #4는 ancestry 확인 뒤 merge commit 없이 `c961026`으로 public `main`에 fast-forward되어 GitHub에서 MERGED가 됐고, main correctness run `31298966035`도 성공했다. Worktree와 feature branch는 다음 milestone을 위해 보존한다.

## 2026-08-09 Milestone 5 설계

- Public main closure `04e9cbc`와 correctness run `31299092196` 성공 상태에서 `codex/milestone-five-l1-cache` 브랜치를 만들었다. Milestone 4의 prefetch는 `Model::load_expert`가 매 선택마다 수행하는 six synchronous extent reads와 temporary pageable vectors 뒤에서 시작하므로 다음 경계는 persistent L1 source-byte residency다.
- Reader-level tensor cache는 expert-atomic admission과 통계를 잃고 trunk/expert를 섞으며, CUDA backend-owned host cache는 storage를 compute backend에 결합한다. Model과 Reader 사이의 immutable whole-expert store를 선택해 CPU, synchronous CUDA, prepared CUDA가 같은 exact native MXFP4 payload handle을 소비하게 한다.
- 첫 L1 모드는 `disabled|static`만 지원한다. Static은 complete gate/up/down triplet이 hard capacity에 맞을 때만 no-eviction admission하고, 공간이 없으면 exact transient bypass한다. First-observation admission은 storage primitive 검증용이며 LRU/LFU/Least-Stale, task/session prior, transition prediction, eviction, L2 async I/O는 후속 milestone이다.
- B-0006은 fixed `cuda-custom + ffn-block + reused + transient + scalar`에서 L1 disabled/static과 L1-to-L0 synchronous/prefetch를 교차한다. Static은 exact token/routing과 matched GPU traffic을 보존하면서 positive hit/miss, bounded residency, zero selected-capacity bypass, reduced Reader calls/bytes를 보여야 한다. Logical Reader bytes는 physical NVMe traffic으로 표기하지 않는다.
- Milestone 5 runtime은 `disabled|static` L1 옵션, immutable complete-expert shared handle, overflow-guarded hard capacity, exact transient bypass를 구현했다. CPU operation, CUDA synchronous FFN-block, prepared prefetch가 같은 handle 표현을 소비하며 1-byte capacity는 모든 expert를 정확히 bypass한다.
- Full verification은 CPU CTest 6/6와 pytest 106 passed/34 skipped, CUDA CTest 15/15와 pytest 139 passed/1 skipped다. CUDA 바이너리 10개 모두 Compute Sanitizer memcheck 0 errors였다.
- B-0006은 measurement commit `616c857`, seeded chunk-257 artifact SHA-256 `077e10a3ba478e83ac8dfd2509ea51a6ea2bfdfe670b60fcadc7f74b97ff810c`, FP32/BF16 각 3 warmup/20 samples로 측정했다. Static은 18 misses 뒤 36 hits, 29,376 resident bytes, zero bypass를 기록하고 logical Reader calls/bytes를 428/665,616에서 212/606,864로 줄였다.
- Matched disabled/static decode는 FP32 synchronous 16.6714/50.5246, prefetch 16.9078/49.4904, BF16 synchronous 16.5688/47.2476, prefetch 16.7753/50.9757 tok/s다. 모든 행이 exact token `[43,32,28,49,9,28]`, routing, H2D/D2H, FFN count, synchronization을 보존했다. 이 큰 차이는 tiny repeated-route WSL2 graph의 synchronous pageable read 제거 효과이므로 full-model TPS로 해석하지 않고 static을 opt-in으로 유지한다.
- Final Terra high review는 캐시가 generation 호출마다 재생성되어 session warm-cache 계약을 충족하지 못하는 점과 native expert admission 검증이 불충분한 점을 Important로 보고했다. Commit `2a0cb27`은 `RuntimeSession`에 store 수명을 두고 one-shot 호환 API를 유지하며, group-32 packed/scales 크기, reserved E8M0, triplet shape를 admission 전에 검증한다. 연속 generation 회귀에서 두 번째 호출은 misses가 늘지 않고 hits가 54 증가하며 Reader delta가 감소했고, malformed payload는 통계와 residency를 바꾸지 않았다.
- 검토 수정 뒤 B-0006을 같은 artifact와 3 warmup/20 samples 조건으로 다시 측정했다. 위 최초 수치는 이 재측정으로 대체된다. 새 disabled/static decode는 FP32 synchronous 16.5587/47.6845, prefetch 16.7636/50.6235, BF16 synchronous 16.4052/47.7956, prefetch 16.5073/47.6198 tok/s다. 모든 cache, logical Reader, token, routing, GPU traffic 불변식은 동일하게 통과했다.
- Public PR #5의 push/PR correctness run `31301870446`과 `31301880423`이 모두 성공했다. `origin/main`이 `dac4ed0`의 조상임을 확인한 뒤 fast-forward했고, post-merge main correctness run `31301960103`도 성공했다.

## 2026-08-09 Milestone 6 설계

- Public main `a7e8acf`에서 `codex/milestone-six-l2-reader`를 시작했다. 현재 hot path는 extent마다 새 `std::ifstream`을 열고 seek/read하며, L1 miss의 native expert 하나가 여섯 번 이 경로를 호출한다.
- Engine과 page-cache policy를 한 enum에 합치지 않고 `pread|io_uring`과 `buffered|direct` 두 축으로 분리한다. Single-read wrapper와 ordered batch를 함께 두고 expert loader가 gate/up/down packed/scale 여섯 extent를 한 batch로 요청한다.
- `io_uring`은 optional Linux capability로 두며 first comparison에서 registered buffers, SQPOLL, IOPOLL을 제외한다. `O_DIRECT`는 `statx(STATX_DIOALIGN)`으로 확인된 alignment만 사용하고 silent buffered fallback을 금지한다.
- 현재 WSL2 source path는 9p/DrvFS이고 liburing development package가 설치되지 않았다. WSL2는 correctness/smoke에만 쓰며 P44 Pro native-Linux default 결정 근거로 사용하지 않는다. Logical/syscall/process I/O counters도 physical NVMe GB/token으로 표기하지 않는다.
- Milestone 6 Task 1 첫 RED는 `ReaderOptions`, L2 enums, `ExtentRequest`, expanded counters, ordered batch API 부재로 compile 실패했고 CLI는 `--l2-io`를 unknown argument로 거부했다. 최소 계약을 추가한 뒤 exact ordered read, empty batch no-op, invalid range prevalidation, default CLI가 통과했다. CPU 전체는 CTest 7/7과 pytest 122 passed/34 skipped다. 실제 hot data plane은 아직 `std::ifstream` 기준선이므로 persistent `pread` 구현을 주장하지 않는다.
- Task 2 RED는 Reader open 뒤 artifact 경로를 rename하면 첫 tensor read가 실패해 extent마다 path를 다시 여는 사실을 재현했다. Reader-owned data plane은 Linux에서 `O_RDONLY|O_CLOEXEC` descriptor 하나를 유지하고 `pread`의 EINTR, partial progress, EOF를 처리한다. Windows fallback은 기존 portable exact read를 유지한다. CUDA build와 CTest 16/16, Reader/L1 targeted pytest 5개가 통과했다.
- Task 3 RED는 first-generation Reader calls와 batch submissions가 동일해 native expert의 six extents가 개별 batch였음을 재현했다. Loader는 세 projection record를 모두 확인한 뒤 packed/scale 여섯 요청을 한 ordered batch로 제출하고 payload를 원래 gate/up/down 순서로 재구성한다. 18 cache misses에서 logical calls는 유지하면서 batch submissions가 정확히 90 감소했고, CPU 전체 CTest 7/7과 pytest 123 passed/34 skipped, CUDA session 및 synchronous/prefetch L1 targeted 7개가 통과했다.
- Task 4는 WSL Ubuntu에 `liburing-dev` 2.5를 설치하고 `K3X_ENABLE_IO_URING` optional Linux build를 추가했다. Default build는 liburing을 링크하지 않으며 requested unbuilt mode는 `STORAGE_UNAVAILABLE`로 fail closed한다. Bounded ring은 explicit offsets, stable operation index, negative CQE, short read, out-of-order completion, 0x7ffff000-byte operation chunking을 처리하고 `IORING_OP_READ` support를 probe한다. Deterministic completion seam과 real-kernel tensor/session tests가 통과했으며 optional build 전체는 CTest 8/8과 pytest 125 passed/34 skipped다.
- Task 5 RED는 direct와 io-uring-direct 요청이 `STORAGE_UNAVAILABLE`인 상태를 확인했다. Linux direct path는 `STATX_DIOALIGN`의 실제 memory/offset alignment가 모두 있을 때만 `O_DIRECT`를 열고, aligned interval과 move-only bounce buffer에서 정확한 logical slice를 복원한다. Small scale extent, final EOF extent, aligned-byte amplification, full expert/session parity를 두 engine에서 검증했다. WSL root ext4에서는 pread-direct와 io-uring-direct가 통과했고 `/mnt/c` 9p/DrvFS는 alignment unavailable로 명시적으로 거부됐다. Direct-enabled default는 CTest 8/8과 pytest 127 passed/38 skipped, liburing build는 CTest 8/8과 pytest 130 passed/35 skipped다.
- Task 6는 runtime JSON과 benchmark schema에 두 L2 축, queue depth, direct alignment, batch/completion/error, logical/aligned storage byte, Reader storage elapsed time, Linux `/proc/self/io`의 `rchar/read_bytes` delta를 추가했다. B-0007 runner는 네 조합을 독립 교차하고 `STORAGE_UNAVAILABLE`만 capability skip으로 기록하며 다른 오류는 숨기지 않는다. Supported case는 token, 실제 24-entry routing trace, logical calls/bytes parity와 성공 accounting을 강제한다. io_uring 부분 제출은 실제 제출 바이트만 기록하고 제출된 작업을 모두 completion 처리한 뒤 오류를 반환하도록 보강했다. WSL2 ext4 1-sample smoke에서 네 조합 모두 exact였고 logical 665,616 bytes, 428 calls, 158 batches를 보존했으며 direct는 756,736 aligned bytes였다. 이는 capability/correctness smoke이며 성능 기본값 근거가 아니다. 최종 targeted tests는 19 passed/5 skipped, 전체 Python은 default CPU 135 passed/40 skipped, liburing/direct build 136 passed/39 skipped다.
- Task 7 최신 검증은 CPU CTest 8/8와 pytest 135 passed/40 skipped, liburing/direct CTest 8/8와 pytest 136 passed/39 skipped, CUDA CTest 17/17와 pytest 168 passed/7 skipped다. CUDA 바이너리 10개 모두 Compute Sanitizer memcheck 0 errors다. Commit `5049f26`, WSL2 ext4, queue depth 8, 3 warmup/20 samples B-0007은 네 모드에서 exact token/routing과 428 logical calls, 158 batches, 665,616 logical bytes를 보존했다. Buffered는 665,616 bytes, direct는 756,736 bytes를 제출했다. Decode는 pread/buffered 5,870.8082, io_uring/buffered 5,616.1034, pread/direct 163.3491, io_uring/direct 428.8471 tok/s였으나 tiny CPU graph와 WSL2 ext4 capability 측정이므로 native P44 Pro default나 full-model TPS 근거로 쓰지 않는다.
- Final Terra high read-only review는 io_uring submit/wait 오류가 outstanding 요청 또는 stale SQE를 batch buffer 수명 밖에 남길 수 있는 Important 결함과, 아직 없는 multi-megabyte fixture의 현재형 문서 주장 한 건을 찾았다. `IoRingBatchGuard`는 모든 early return에서 buffer보다 먼저 ring을 닫고 Reader를 fail-closed시키며 `EINTR` wait를 재시도한다. 실제 ring lifetime regression, liburing ASan/UBSan CTest 9/9, post-fix 네 조합 exact smoke를 통과했고 설계 문서는 fixture를 proposed로 정정했다. Post-review 전체는 CPU CTest 8/8와 pytest 136/40, liburing CTest 9/9와 pytest 137/39, CUDA CTest 17/17와 pytest 169/7이다. Successful path는 바뀌지 않아 B-0007 3×20 raw 측정은 유지한다.
- Public PR #6의 push/PR correctness run `31305244311`과 `31305256170`이 모두 성공했다. `origin/main`이 `1f2853e`의 조상임을 확인한 뒤 merge commit 없이 public `main`에 fast-forward했고 GitHub에서 PR #6이 MERGED로 확인됐다. Post-merge main correctness run `31305351800`도 성공했다. Worktree와 feature branch는 다음 milestone을 위해 보존한다.
- Milestone 6 publication 기록 commit `11e1d9a`는 branch/main correctness run `31305484133`/`31305487562`에서 모두 성공했다. 보존된 격리 worktree를 public main에서 `codex/milestone-seven-bounded-slice`로 전환했다. Baseline CPU CTest는 8/8이다. 첫 pytest 실행은 `K3X_BUILD_DIR` 미지정으로 존재하지 않는 `build/k3x_run`을 찾아 51건 실패했고, 실제 원인을 확인한 뒤 `K3X_BUILD_DIR=build-cpu`로 재실행하여 136 passed/40 skipped를 확인했다.
- Milestone 7은 실제 released expert 한 개의 17,547,264-byte MXFP4 payload만 물리적으로 생성하는 non-executable storage fixture를 선택했다. Sparse holes는 I/O 증거를 왜곡하고 full-dimension executable graph는 bounded scope를 벗어나므로 거부했다. Optional feature bit로 fixture를 명시하고 전용 expert-load benchmark만 허용한다. 사용자의 Cloud Run 전 단순 승인 사전 허가에 따라 별도 승인 질문 없이 명세와 계획을 진행한다.
- Milestone 7 구현 계획은 streaming source fixture, converter optional identity/resume, C++ execution guard, exact six-extent benchmark, B-0008 ablation, 측정/ledger/publication의 여섯 독립 검증 단위로 고정했다. 각 production behavior는 실제 artifact를 이용한 RED-GREEN test를 먼저 거치며, token benchmark schema를 storage benchmark에 재사용하지 않는다.
- Milestone 7 Task 1 RED는 `k3x_ref.storage_fixture` 부재로 collection 실패했다. GREEN은 safetensors shard를 `.partial`에서 bounded chunks로 물리 생성하고 fsync/atomic replace한 뒤 deterministic manifest를 기록한다. Targeted pytest 2 passed이고 1 MiB CLI 생성은 payload 17,547,264 bytes, 최대 chunk 1,048,576 bytes, shard SHA-256 `1e310ebdcdd7a8a7ec124fac1e59ca44bbdf5da1ef279502c81a8b70f06379f5`를 보고했다. 첫 독립 검사 명령은 PowerShell/WSL quoting 오류로 시작되지 않았고, 분리한 실제 CLI와 pytest inspect 경계로 재검증했다.
- Milestone 7 Task 2 RED는 storage optional constant 부재로 collection 실패했다. GREEN은 `k3-storage-slice-v1`의 kind/config/ID/세 matrix shape와 17,547,264-byte payload를 fail-closed 검증하고 optional bit 0을 K3X에 기록한다. 실제 slice conversion, malformed source, two-extent resume, changed-source rejection을 포함한 targeted 21건과 전체 CPU pytest 146 passed/40 skipped가 통과했다. 자체 검토에서 synthetic resume fingerprint 호환성을 보존하도록 optional feature가 0이 아닐 때만 feature identity를 fingerprint에 추가했고 resume/format 18건을 재검증했다.
- Milestone 7 Task 3 RED는 C++ Reader가 storage optional bit를 노출하지 않아 test mode가 10을 반환했고 runtime이 `INVALID_EXTENT: missing FP32 tensor: model.embeddings`까지 진입했다. GREEN은 optional bit 0을 C++ superblock에 보존하고 모든 generation overload가 공유하는 경계에서 `NON_EXECUTABLE_ARTIFACT`를 반환한다. Targeted 2 passed, CPU CTest 8/8, 전체 pytest 148 passed/40 skipped다.
- Milestone 7 Task 4 첫 RED는 `k3x_storage_bench` binary 부재로 2건이 명시적으로 실패했다. 구현은 metadata-only Reader로 gate/up/down data+scale 여섯 extent를 한 batch에 읽고 ordered SHA-256, exact bytes, latency percentiles, Reader/storage/process-I/O counters만 JSON으로 출력하며 token field를 만들지 않는다. Targeted 2 passed, CPU CTest 8/8, liburing build targeted 2 passed다. 자체 검토에서 storage fixture가 alphabetic `down/gate/up` 물리 순서로 pack되는 결함과 2-sample p95가 median보다 작아지는 percentile 결함을 각각 재현했고, execution order `gate/up/down`과 nearest-rank percentile로 수정한 뒤 targeted 3 passed와 CPU CTest 8/8을 재확인했다.
- Milestone 7 Task 5 RED는 B-0008 module 부재로 collection 실패했다. GREEN runner는 네 Reader 조합을 실제 executable로 호출하고 identity/digest/logical bytes/counters/direct alignment/percentile/process-I/O를 검증하며 `STORAGE_UNAVAILABLE`만 skip한다. WSL2 ext4 1-sample smoke는 네 모드 모두 digest `e5fb7939474a57ab9263a791999d76ba078bd767cc3f155f3522b1bec576c7e4`, 17,547,264 bytes, six completions, zero failures를 보존했고 actual dimensions가 512-byte 정렬에 맞아 direct byte amplification은 0이었다. 이 수치는 smoke이며 ledger benchmark가 아니다. 전체는 CPU CTest 8/8와 pytest 152/40, liburing CTest 9/9와 pytest 157/35, CUDA CTest 17/17와 pytest 185/7, ASan/UBSan liburing CTest 9/9와 storage targeted pytest 4 passed다.
- B-0008은 code commit `9198ed2`, WSL2 ext4 artifact, queue depth 8, 3 warmup/20 samples로 측정했다. 네 row 모두 ordered digest `e5fb7939474a57ab9263a791999d76ba078bd767cc3f155f3522b1bec576c7e4`, 120 completions, 350,945,280 logical/submitted bytes, zero failure/short read를 보존했다. Median wall/Reader ms는 pread-buffered 50.685/4.661, io_uring-buffered 51.592/4.579, pread-direct 60.402/14.832, io_uring-direct 56.426/11.633이다. Direct amplification은 0%다. Artifact SHA-256은 `b14610fd2b405dd97c09004fb29157f5b318522591546337bce89e7e8a6a2b65`이며 raw/summary 교차 확인은 exact다. 이는 allocation+hash를 포함한 WSL2 expert-load 측정이며 tok/s, cold-cache, P44 Pro, full-model 증거가 아니다.
- 첫 Compute Sanitizer loop 두 번은 외부 PowerShell이 Bash `$target`을 제거해 target-invalid 메시지를 냈지만 도구가 0을 반환했으므로 성공으로 세지 않았다. 각 CUDA binary를 이름으로 명시해 WSL ext4 `/tmp/k3x-csan-m7`에 복사하고 개별 실행한 최종 검증에서 10개 모두 `ERROR SUMMARY: 0 errors`를 보고했다.
- Milestone 7 Terra high final review는 Critical 0건, Important 2건으로 병합 불가 판정했다. 원인은 storage manifest의 shard/tensor SHA-256이 생성만 되고 변환 경계에서 검증되지 않는 점과, resume ledger가 완료 extent의 canonical prefix, 길이, offset, 현재 source CRC를 검증하지 않는 점이다. 두 문제를 별도 실패 재현 테스트로 확인한 뒤 최소 수정한다.
- 리뷰 수정의 RED 단계에서 source 변조·digest 불일치·manifest 게시 실패 3건과 resume ledger 손상 5건이 모두 재현됐다. Content-addressed shard와 manifest-last 게시, bounded shard/tensor SHA-256 검증, manifest가 참조하는 shard만의 source fingerprint, canonical prefix 및 source/partial 이중 CRC 검증으로 닫았다. 대상 스위트 32/32, CPU CTest 8/8 및 pytest 161/40, liburing CTest 9/9 및 pytest 162/39, CUDA CTest 17/17 및 pytest 194/7이 통과했다. B-0008 payload/Reader/benchmark code는 바뀌지 않아 측정 수치를 재작성하지 않았다.
- Public PR #7은 branch/push correctness run `31308034386`과 PR run `31308046877` 성공 후 ready로 전환했고, `e9a5d4d`를 공개 `main`에 fast-forward하여 GitHub가 merged로 기록했다. Post-merge main correctness run `31308147456`도 성공했다. Merge commit은 별도로 만들지 않았다.
- Milestone 8은 Reader native async API나 미래-layer predictor를 선행하지 않는다. Blocking Reader를 worker 내부에서 재사용하는 bounded deadline queue를 만들고, 현재 layer의 정확한 natural Top-K가 결정된 뒤 routed/shared 독립 계산과 expert load를 겹친다. N+1/N+2 lookahead는 scheduler API의 후속 소비자이며 이번 구현 상태로 주장하지 않는다.
- Task 1 scheduler는 single bounded worker와 absolute latest-start priority를 사용한다. Controlled blocker 뒤에 요청을 쌓는 deterministic test가 smaller slack 우선, stable sequence tie, queue-full fail-closed, loader failure, ticket single-consumption, resident inline completion, destructor drain을 검증했고 5회 반복 통과했다.
- Task 2 RED에서 동일 L1 key의 동시 miss는 loader를 8회 실행했고, shared Reader 병렬 pread는 payload는 성공했지만 `calls` counter가 손실됐다. `HostExpertStore::get_or_load`와 `Reader::read_extents`의 data-plane/counter 구간만 각각 mutex로 직렬화해 pread/io_uring 동시성 테스트를 통과했다. TSan 빌드는 성공했지만 WSL2에서 `unexpected memory mapping`으로 실행 불가했고, ASan/UBSan liburing에서 scheduler, L1, pread/io_uring 병렬 경로가 통과했다.
- Task 3 deadline mode는 router가 확정한 current-layer natural Top-K만 제출하고, resident hit를 inline 처리한 뒤 non-resident load를 worker에 넣는다. Main thread는 routed-down 및 shared FFN을 계산하고 expert use 직전에 ticket을 기다린다. Blocking mode의 순서와 기본값은 그대로다. Static L1 session parity에서 tokens, routing, hit/miss, Reader calls/requested/completed bytes가 exact였고 scheduler submissions/completions가 일치했다. Pending callable은 Engine이 아니라 Reader와 session-owned store만 캡처해 failure unwind 수명을 보존한다.
- Task 4는 `--l2-schedule blocking|deadline`을 추가하고 blocking을 기본으로 유지한다. JSON은 submissions, resident inline, completions, ready/late, estimated misses, requested bytes, queue high-water, worker time, exposed wait를 기록한다. Blocking/deadline CLI parity와 invalid `fifo` rejection, benchmark schema tests 9/5가 통과했다. N+1/N+2 lookahead는 여전히 명시적으로 미구현이다.
- Task 5 B-0009 runner는 L1 static 65,536 bytes에서 `blocking|deadline × pread|io_uring × buffered|direct` 8개 조합을 측정한다. `STORAGE_UNAVAILABLE`만 pair 단위 capability skip으로 허용하고, 모든 supported row에서 exact tokens/routing/L1 counters/logical Reader calls와 bytes를 강제한다. Blocking scheduler counters는 전부 0이어야 하고 deadline은 submissions/completions/readiness, inline-resident hit, requested bytes, queue activity, worker time 불변식을 만족해야 한다. RED는 module 부재로 collection 실패했고 GREEN targeted tests는 10/10, 실제 build 경로를 지정한 benchmark/parity 묶음은 68 passed/36 skipped, CPU CTest는 9/9다.
- B-0009은 review-fix measurement code commit `68b3e54`, artifact SHA-256 `392b9237274e5580b665cf95afbda9a09e8d01ba7484bed00cf83a4ae99eb4fa`, WSL2 ext4 warm 상태, 3 warmup/20 samples로 교체 측정했다. 8개 row 모두 exact tokens/routing, 606,864 logical bytes, 212 completions, zero failure/short read를 보존했다. Deadline row는 54 submissions/completions와 36 inline hits를 기록했다. Decode tok/s는 blocking/deadline 순서로 pread-buffered 6,508.251/5,112.555, io_uring-buffered 6,234.853/4,971.308, pread-direct 808.171/768.502, io_uring-direct 1,966.491/1,766.937이었다. 이 tiny warm WSL2 결과에서는 deadline이 모두 느려 기본 활성화 근거가 없으며 native P44 Pro, cold-cache, full-model 성능 증거가 아니다. Raw/summary 교차검증 test는 11/11이다.
- Milestone 8 Terra high final review는 Reader counters와 L1 stats의 잠금 없는 참조, 예외 반환 뒤 raw Reader/store capture가 남을 수 있는 수명 문제를 Important 3건으로 판정했다. Snapshot API를 잠금 아래 값 반환으로 바꾸고, per-layer fetch estimate를 첫 submit 전에 한 번 캡처했으며, scheduler idle barrier를 generation 성공·예외 반환 양쪽에 연결했다. API 반환형과 동시 snapshot, idle barrier 회귀를 추가했고 targeted pytest 56/35 및 CPU CTest 9/9가 통과했다. Estimate capture 시점이 바뀌어 B-0009를 `68b3e54`에서 교체 측정했고 raw/summary 교차검증 11/11이 통과했다.
- 동일 Terra high 재검토는 이전 Important 3건이 모두 종결됐고 새 Critical/Important가 없다고 판정했다. Post-review 전체 검증은 CPU CTest 9/9와 pytest 175/41, liburing/direct CTest 10/10과 pytest 177/39, CUDA CTest 18/18과 pytest 208/8, ASan/UBSan liburing CTest 10/10과 targeted pytest 69/33이다. CUDA test binary 10개는 모두 Compute Sanitizer 0 errors다. TSan은 build 성공 후 WSL2 runtime의 `unexpected memory mapping`으로 실행 불가하여 결과를 주장하지 않는다.
- Public PR #8은 branch push correctness run `31310497619`와 PR run `31310504588` 성공 후 ready로 전환했고, `a289172`를 공개 `main`에 fast-forward하여 GitHub가 MERGED로 기록했다. Post-merge main correctness run `31310608231`도 성공했다. Merge commit은 별도로 만들지 않았다.
- Milestone 8 publication record `07dd36b`는 branch/main correctness run `31310722378`/`31310723374`에서 모두 성공했다. 보존 worktree를 public main에서 `codex/milestone-nine-expert-cache-policies`로 전환했다.
- SpecMD arXiv 2602.03921의 Least-Stale는 이전 forward cycle의 stale queue를 current queue보다 먼저 퇴거하고 각 큐에서 layer-position FIFO를 사용하는 spatial-temporal 정책이다. 논문은 공개 구현을 아직 `open-sourcing underway`로 표시하고 author-linked repository가 확인되지 않아 K3X는 code port가 아닌 paper reproduction으로 기록한다. Milestone 9는 disabled/static reference를 보존하며 exact LRU/LFU/Least-Stale, selected-set protection, collision miss accounting, B-0010만 범위에 둔다.
- Milestone 9 Task 1은 동일 크기 expert trace로 LRU의 최근 사용, LFU의 사용 빈도, Least-Stale의 stale-cycle 후 layer-position 순서를 결정적으로 검증한다. 현재 natural Top-K selected set은 퇴거 후보에서 제외하고, 같은 forward cycle에서 퇴거된 expert를 다시 요청할 때 collision miss를 기록한다. 새 정책 단위 테스트와 CPU CTest 9/9가 통과했다.
- Task 2는 `lru`, `lfu`, `least-stale` CLI identity를 추가하고 disabled 기본값을 보존했다. Forward cycle은 재사용되는 `RuntimeSession`이 원자적으로 발급하여 generation 간 cycle ID 재사용을 막고, Engine은 각 MoE layer의 natural Top-K 전체를 admission 전에 표시한다. 1-expert 용량 테스트가 selected-set exact bypass를 검증했고, blocking/deadline 경로의 새 정책들은 token, routing, logits, Reader byte 계약을 유지했다. CPU CTest 9/9와 `test_cpp_parity.py` 56 passed/31 skipped가 통과했다.
- Task 3 telemetry는 eviction과 same-forward collision miss를 C++ JSON, deterministic benchmark sample, `BenchmarkRecord`, JSON/CSV에 연결했다. SpecMD 원문의 `left` 정의와 upcoming-layer 보호를 재대조해 Least-Stale가 stale current/future를 절대 layer 오름차순으로 퇴거하던 초기 comparator를 폐기했다. 수정 comparator는 이미 처리한 left layer를 먼저, 불가피한 future fallback에서는 가장 먼 layer를 먼저 퇴거하며 deterministic trace에서 LRU collision 1, Least-Stale 0을 검증했다. B-0010 runner는 disabled 기준선과 4개 정책을 2·8·16-expert 용량에서 교차하며 exact token/routing·numerical error과 cache/Reader accounting을 강제한다. 관련 pytest는 76 passed/36 skipped, CPU CTest는 9/9이다.
- Milestone 9 final review의 Important 1건은 동일 `RuntimeSession`의 동시 generation이 store의 단일 active cycle/layer/selected set을 덮어쓰는 경계였다. 전체 generation을 session mutex로 직렬화하고 별도 thread의 generation이 guard 해제 전에 완료되지 않는 회귀 테스트를 추가했다. 리뷰의 B-0010 collision 9 모순 지적은 기각했다. 이전 token에서 잔류한 future-layer expert가 현재 token의 앞 layer에서 퇴거되고 뒤 layer에서 재요청되는 것이 same-forward collision이며, 기존 결정적 trace가 이 경로를 직접 재현한다.
- Review fix 후 B-0010을 measurement code `fd05d95`에서 3 warmup/20 samples로 교체 측정했고 result commit은 `ff65b5b`, summary SHA-256은 `f2ff111f08fd6e9bc2cee2df426aeef337dd08150663d4e6a71922ebf60c5b8b`이다. 13개 row 모두 exact token/routing/numerical-error identity를 유지했다. 8-expert 용량에서 Least-Stale/LRU/LFU는 각각 hits 23/20/19, misses 31/34/35, collisions 0/1/7, logical Reader bytes 628,080/632,976/634,608이었다. 16-expert에서는 LFU가 36 hits·18 misses·606,864 bytes로 가장 적어 일반 기본 정책은 채택하지 않았다.
- 재검토는 generation guard로 Important 1이 종결되었고 future-layer same-forward collision 해석이 타당하다고 확인한 뒤 Critical/Important 없음으로 끝났다. 수정 후 최종 검증은 CPU CTest 9/9·pytest 194/41, liburing/direct 10/10·200/35, CUDA 18/18·227/8, ASan/UBSan 10/10·targeted pytest 80/33이다. CUDA Compute Sanitizer 10개 0 errors 후 host-only guard만 변경되었고, TSan은 다시 build 성공 후 WSL2 `unexpected memory mapping`으로 실행 불가했다.
- 공개 통합 전 원장 자체 검토에서 `git diff --check`가 통과했고, 전용 B-0010 회귀 테스트는 `PYTHONPATH=. pytest -q tests/python/test_expert_cache_policy_ablation.py`로 12개가 통과했다. `summary.json` SHA-256도 원장에 기록한 `f2ff111f08fd6e9bc2cee2df426aeef337dd08150663d4e6a71922ebf60c5b8b`와 다시 일치했다.
- Public PR #9은 branch push correctness run `31312742423`과 PR run `31312750299` 성공 후 ready로 전환했고, `185ae5d`를 공개 `main`에 merge commit 없이 fast-forward하여 GitHub가 MERGED로 기록했다. Post-merge main correctness run `31312852935`도 성공했다.
- Milestone 9 publication record `05af321`는 branch/main correctness run `31312983502`/`31312984375`에서 모두 성공했다. 보존 worktree를 public main에서 `codex/milestone-ten-task-session-profiles`로 전환했다.
- Milestone 10은 runtime-only metadata와 prior/live evidence를 분리한 persistent routing profile만 구현한다. Profiled cache는 opt-in이며 preloading과 LFU counter seeding은 각각 불필요한 선행 I/O와 prior/live 불투명성 때문에 채택하지 않았다. Transition과 hot-bank는 저장하지만 transition predictor 및 prefix/KDA payload cache는 이번 범위가 아니다.
- Milestone 10 Task 1은 metadata key/value bounds와 UTF-8/control 검증, cycle/layer-aware frequency·transition 관측, deterministic hot bank, prior/live usefulness, canonical v1 text format, CRC32C 검증, sibling temporary publish를 구현했다. Missing-header RED를 확인한 뒤 dedicated profile CTest와 전체 CPU CTest 10/10이 통과했다.
- Milestone 10 Task 2는 `profiled`를 기존 exact L1 정책과 분리된 opt-in mode로 추가했다. Helpful prior trace에서는 prior-hot expert를 보존하고, prior와 반대인 live route를 4회 관측한 trace에서는 session-hot expert를 보존해 crossover를 확인했다. Current selected set 보호와 exact load/bypass 계약은 기존 store 경로를 그대로 사용한다.
- Milestone 10 Task 3은 `RuntimeSession`이 profile을 소유하고 모든 natural Top-K access set을 관측하도록 연결했다. CLI는 `--runtime-metadata`, `--runtime-profile-in|out`, `--profile-prior-strength`, `--l1-expert-cache profiled`를 지원하며 load/save bytes·nanoseconds, metadata count, live observations, prior weight를 JSON에 기록한다. Baseline/profiled/resumed 실행은 token, routing, logits, recurrent state가 exact 일치했고, CPU CTest 10/10과 targeted pytest 17개가 통과했다. Flush+rename은 process interruption에 대한 atomic publication만 주장하며 fsync 없는 power-loss durability는 주장하지 않는다.
- 자체 검토에서 default session이 불필요하게 모든 route를 관측하던 비용, TTFT 반복이 외부 profile evidence를 덮던 문제, bounded frequency cap에서 저장되지 않은 관측 수가 증가해 자체 출력이 load 거부될 수 있던 문제를 발견했다. 각각 explicit observation gating, per-run temporary profile과 최종 full-generation materialization, persisted-record에만 observation count를 더하는 방식으로 수정했다.
- 최종 B-0011은 13,056-byte capacity에서 LFU, Least-Stale, profiled cold, matching prior, minimum-overlap alternate prior를 비교했다. 모든 row가 token `[43,32,28,49,9,28]`, 24-entry routing, logits, recurrent state를 exact 유지했다. Matching prior는 Least-Stale와 동일한 23 hits·31 misses·628,080 logical Reader bytes였지만 decode 5,006.701 tok/s로 Least-Stale 5,900.245보다 낮았고, alternate prior도 개선하지 못해 기본 정책은 바꾸지 않았다.
- 최종 summary SHA-256은 `029004e02a8484f281a332c09d49e7adc8eb1ed343ec692b030760288adbd94f`, synthetic K3X artifact SHA-256은 `0dfe0fe7c64b364fc745fff5b6c9a1f06d1faf1dc140630a9240591540dd684d`이다. Materialized profile 1,439/1,439/1,645 bytes는 raw save telemetry와 일치한다. 최종 검증은 CPU 10/10·pytest 211/41, liburing 11/11·213/39, CUDA 19/19·244/8, ASan/UBSan 11/11·targeted pytest 82/33, CUDA Compute Sanitizer 10개 0 errors다.
- Public PR #10은 branch/PR correctness run `31315847372`와 `31315848859` 성공 후 ready로 전환했다. `3d8c042`를 공개 `main`에 merge commit 없이 fast-forward했고 GitHub가 PR을 MERGED로 기록했으며, post-merge main correctness run `31315950735`도 성공했다.
- Milestone 10 publication record `a82d733`는 branch/main correctness run `31316068815`/`31316069868`에서 모두 성공했다. 보존 worktree를 `codex/milestone-eleven-adaptive-topk-rescue`로 전환했다.
- Milestone 11 조사에서 현재 C++ router는 모든 expert sigmoid score와 correction-biased stable order를 이미 계산하지만 denominator, selected set, loader, CUDA group, CPU mixing이 모두 checkpoint `config_.top_k`에 직접 결합되어 있음을 확인했다. Checkpoint Top-K mutation은 reference identity를 잃고 residency-aware substitution은 exact rescue를 깨므로 배제했다.
- 채택 설계는 full score/order를 보존한 순수 routing policy가 natural order prefix를 선택한다. Natural은 기존 behavior exact, fixed/adaptive reduced K는 명시적 lossy mode다. Adaptive는 cumulative unbiased mass, entropy effective support, boundary confidence, external quality floor를 모두 사용한다. 선택 후 non-resident expert는 기존 exact MXFP4 경로로 반드시 load하며 lower-ranked resident expert로 대체하지 않는다.
- Milestone 11 Task 1은 missing `routing_policy.hpp` compile failure로 RED를 확인한 뒤 standalone policy를 구현했다. Natural은 checkpoint K, fixed는 K4/6/8/12/16, adaptive는 mass target·entropy effective support·boundary gap·quality floor를 적용한다. Stable full order와 unbiased normalized weights를 반환하며 NaN, out-of-range sigmoid score, zero mass, invalid K/threshold를 거부한다. 전용 policy test와 전체 CPU CTest 11/11이 통과했고 Engine/CLI 행동은 아직 바꾸지 않았다.
- Milestone 11 Task 2는 fixture builder에 optional explicit config를 추가했다. Explicit default와 implicit default는 manifest 및 두 source shard SHA-256가 동일하다. 16-of-24 fixture는 각 MoE layer 24 experts와 natural Top-16을 materialize한다. PyTorch oracle은 C++과 같은 mass·entropy·boundary·floor 규칙을 적용하고, reference model이 per-token reduced K를 실행한다. Fixed K16은 natural logits, layer outputs, state SHA-256가 exact 일치했고 전체 Python suite는 216 passed/41 skipped였다.
- Milestone 11 Task 3은 C++ Engine의 expert loop, async loader, CUDA group, CPU mixing을 단일 immutable routing decision에 연결했다. CLI는 natural/fixed/adaptive와 K4/6/8/12/16, mass·boundary, agent failure·critical quality floor를 검증한다. 선택된 non-resident expert는 기존 exact MXFP4 경로로 load되며 residency는 ID 선택에 관여하지 않는다. JSON/CSV에는 per-decision K, 평균 K·entropy·mass·boundary confidence, 실제 floor escalation, cold rescue를 기록한다. Natural과 fixed K16은 exact 일치하고 fixed K4는 Python oracle과 token exact, failure 2는 K12, critical은 K16으로 검증했다. CPU CTest 11/11과 전체 pytest 220 passed/41 skipped였다.
- B-0012 runner는 natural K16, fixed K4/8/12/16, adaptive threshold 세 가지, adaptive failure/critical, fixed-K escalation ladder, bounded LRU exact rescue를 교차한다. Smoke에서 reduced K의 첫 decision은 natural prefix지만 이후 hidden state divergence가 router 순서에도 전파됨을 확인했다. 따라서 첫-decision prefix를 invariant로 강제하고 전체 동일-position prefix rate를 품질 지표로 기록하며, residency 불변식은 fixed K4 cache-disabled와 rescue 실행의 token·전체 prefill route·quality error exact 비교로 검증한다. Top-16 synthetic smoke와 관련 pytest 17 passed/5 skipped가 통과했다.
- B-0012 정식 3-warmup/20-sample WSL2 측정에서 fixed K4/K8/K12는 natural K16 대비 logical Reader bytes를 40.8%/27.2%/13.6% 줄이고 tiny CPU decode를 3.24x/1.92x/1.34x로 높였지만 token, logits, recurrent state가 모두 달라졌다. Fixed K16과 critical K16은 exact다. Nearly-uniform synthetic router 때문에 모든 adaptive 행은 K16을 선택했다. 6,528-byte LRU rescue는 fixed K4와 exact 동일한 실행을 유지하며 108 cold loads, 0 hits, 동일 traffic을 기록했다. Natural default를 유지한다. Final self-review는 Top-2 natural reference가 K8/K16 external floor를 오류로 처리하던 경계를 재현해 natural이 floor를 무시하도록 수정했다. 검증은 CPU 11/11·227/41, liburing/direct 12/12·233/35, CUDA 20/20·260/8, ASan/UBSan 12/12·101/34, Compute Sanitizer 10개 0 errors다.
- Milestone 11 public integration head `edc6d60`은 branch/PR correctness run `31318880063`/`31318890885`를 통과했고 PR #11로 fast-forward merge되었다. Post-merge `main` run `31318993688`도 성공했다.

## 2026-08-09 Milestone 12 설계

- 공개 main과 동기화된 `dd80e53`에서 `codex/milestone-twelve-fused-cuda` 브랜치를 시작했다. 사용자의 Cloud Run 전 자율 진행 지시를 routine design gate의 승인으로 적용하되, 설계 문서와 self-review는 생략하지 않았다.
- 현재 exact native-MXFP4 `ffn-block`은 gate/up/SiTU/down을 GPU에서 계산하지만 선택 expert마다 latent vector를 D2H 복사한 뒤 CPU에서 router scale과 합산한다. Natural Top-16에서는 이 경계가 Top-K에 비례한다.
- 세 후보를 비교했다. 현재 row-parallel down kernel에 SiTU를 직접 넣으면 activation을 output row마다 재계산하므로 기각했다. Gate/up 단일 launch는 input upload가 이미 공유되어 우선효과가 작다. Down projection 결과에 router scale을 적용하고 stream order대로 한 device mixed vector에 누적하는 방식을 선택했다.
- 선택안은 extra kernel 없이 expert별 down kernel이 첫 expert를 store하고 이후 expert를 FMA accumulate하며, 최종 mixed latent만 한 번 D2H 한다. 기존 unfused block은 reference/default로 남고 synchronous와 prepared-prefetch 양쪽을 독립적으로 검증한다.
- FlashKDA 원 구현은 서로 다른 parallelism 축을 무리하게 단일 kernel로 합친 초기안보다 K1/K2 분리가 빠르다고 보고했다. 따라서 이번에는 KDA/MLA, CUDA Graph, persistent kernel을 함께 묶지 않고 별도 측정축으로 남겼다.
- Milestone 12 Task 1은 `launch_mxfp4_matvec_accumulate`가 없다는 NVCC compile error로 올바른 RED를 확인했다. 기존 row reduction을 그대로 두고 thread 0이 첫 expert에는 `contribution * sum`을 저장하고 이후 expert에는 `fmaf(contribution, sum, output[row])`를 수행하는 최소 launcher를 추가했다. Literal test는 high/low nibble, 서로 다른 E8M0 group, 320 columns, 음수·양수·0 contribution, ordered accumulation을 CPU oracle과 비교한다. Focused test가 통과했고 Compute Sanitizer는 0 errors를 보고했다.
- Task 2 synchronous RED는 `ComputeBackend::mxfp4_situ_moe` 부재로 compile 실패했다. CPU oracle과 CUDA backend에 동일한 narrow contract를 추가했고, CUDA는 기존 gate/up/SiTU 및 residency 경로를 재사용하면서 down projection만 ordered store/FMA로 실행한다. 2-expert scalar fixture에서 unfused D2H 8 bytes 대비 fused D2H 4 bytes, 동일 weight hits·H2D·sync·FFN counters를 검증했다. Contribution count mismatch와 NaN은 allocation, cache, profiler side effect 전에 거부된다. CPU backend, CUDA MXFP4/FFN focused CTest가 통과했고 `test_cuda_ffn` memcheck는 0 errors였다.
- Task 3 prepared RED는 `mxfp4_situ_moe_prepared` 부재로 compile 실패했다. Single-use prepared token의 identity와 contribution count/finite validation을 consume 및 allocation 전에 수행하고, synchronous와 동일한 ordered device accumulation 및 one-D2H contract를 구현했다. `none|routed-accumulate` CLI는 `cuda-custom + ffn-block`에서만 fused mode를 허용하며 기본값은 `none`이다. Runtime은 routing decision에서 확정된 normalized weight와 `routed_scale`을 그대로 backend에 전달하고 residency로 재정렬하지 않는다.
- Graph parity는 FP32/BF16 × scalar/grouped × synchronous/prefetch에서 operation, unfused block, fused block을 비교했다. 모든 행의 token `[43,32,28,49,9,28]`, routed trace, logits, layer outputs, recurrent state가 기존 tolerance 안에서 일치했고 fused D2H가 matching unfused보다 작았다. Fused call/expert telemetry와 invalid CLI 조합 11건이 통과했고 prepared memcheck는 0 errors였다.
- B-0013 synthetic runner는 synchronous/prefetch와 `none|routed-accumulate`를 교차하고 provenance, token, routed IDs, per-decision K, FP32 maximum error, fused counters, strict D2H reduction을 강제한다. Benchmark JSON/CSV schema에는 fusion identity와 fused call/expert counters를 추가했다. Natural Top-16 실제 CUDA smoke 4행이 모두 correctness gate를 통과했다.
- Released-dimension microbenchmark는 기존 17,547,264-byte storage fixture의 3,584×3,072 native MXFP4 expert를 한 번 load하고 같은 immutable view를 16 slots로 반복한다. 이는 routing 의미나 full-model TPS가 아닌 kernel/D2H fixture로 JSON에 명시한다. 각 mode는 untimed unfused GPU reference와 비교하며 timed delta만 기록한다. 실제 one-iteration test에서 unfused 229,376 bytes와 fused 14,336 bytes D2H 계약, fused 1 call/16 experts, numerical tolerance를 통과했다. 관련 schema/runner/실제 CUDA tests는 21 passed였다.
- B-0013은 3 warmup/20 samples로 synthetic natural Top-16 4행과 released-dimension 2행을 측정했다. Synthetic fused decode는 synchronous에서 13.6984→15.2499 tok/s, prefetch에서 14.7718→16.0877 tok/s였고 두 경우 모두 D2H를 51,840 bytes 줄였다. Token `[56,55,18,11,11,13]`, routing, K, numerical tolerance가 일치했다.
- Released 3,584×3,072 expert repeated-view에서는 fused가 D2H를 4,587,520→286,720 bytes로 93.75% 줄였지만 median latency가 7,867,604→8,497,998 ns로 8.01% 느려지고 aggregate kernel time도 5.88% 증가했다. 따라서 `routed-accumulate`는 실험적 opt-in이며 기본값은 `none`으로 유지한다.
- 전체 검증은 CPU CTest 11/11과 pytest 235/44, liburing/direct CTest 12/12와 pytest 237/42, CUDA CTest 20/20과 pytest 271/8, ASan/UBSan liburing CTest 12/12와 targeted pytest 49/5를 통과했다. 기존 10개 CUDA test와 새 released benchmark까지 11개 Compute Sanitizer 실행이 모두 0 errors였다. 최초 liburing pytest는 capability env 누락으로 expected-selection 1건이 실패했고 수정 실행은 통과했다. 최초 sanitizer loop는 PowerShell quoting 오류로 유효 target을 실행하지 못했으며 명시적 11회 실행으로 교정했다.
- Ledger commit 후 현재 HEAD 재검증에서 CPU CTest 11/11, 수정된 `PYTHONPATH=.` CPU pytest 235/44, CUDA CTest 20/20, CUDA pytest 271/8이 통과했다. 첫 CPU pytest 명령은 `PYTHONPATH=.` 누락으로 `tools` import collection error 11건을 냈고, 첫 CUDA 결합 명령은 124초 도구 timeout으로 결과를 반환하지 못했다. CTest와 pytest를 분리한 교정 실행이 각각 통과했다.
- Milestone 12 public integration head `9e59a9d`는 branch/PR correctness run `31322043556`/`31322049903`을 통과했고 PR #12로 fast-forward merge되었다. Post-merge `main` run `31322191670`도 성공했다.

## 2026-08-10 Milestone 13 설계

- 공개 main publication head `dc23020`에서 `codex/milestone-thirteen-speculative-verification` 브랜치를 시작했다. 기존 linked worktree를 확인해 중첩 worktree는 만들지 않았다.
- DSpark paper arXiv `2607.05147`과 DeepSpec commit `005e03b81cec38b7da6399833d609ee89a2587f2`의 `base_evaluator.py`, DSpark `evaluator.py`, `draft_ops.py`를 확인했다. 실제 구현은 accepted anchor를 proposal 첫 토큰으로 두고 accepted prefix와 target bonus를 commit한 뒤 target/draft cache를 crop/update한다.
- DeepSpec evaluator는 nonzero-temperature rejection sampling을 구현하지만 K3X 현재 runtime은 greedy다. M13 strict verifier는 candidate가 target argmax와 같은 동안만 longest prefix를 accept하고 target bonus token을 commit한다. 이는 K3X 계약이며 DeepSpec 동작을 greedy라고 오기하지 않는다.
- Expert-major 실행, DSpark learned drafter, confidence scheduler, AURORA, EcoSpec, MoE-Spec, AcceptMoE를 동시에 구현하는 안은 거부했다. 먼저 external draft lifecycle과 token-major exact target reference를 고정하고, rejected suffix를 target state에 적용하지 않는 state boundary를 검증한다.
- Milestone 13 Task 1 RED는 `k3x/speculative.hpp` 부재 compile failure로 확인했다. Anchor, bounded candidates, committed bonus, provider lifecycle type을 추가하고 perfect, first/middle mismatch, empty, oversize, invalid token, target failure를 검증했다. Empty callback RED는 `std::bad_function_call` abort를 재현했고 side-effect 전 `INVALID_STATE` 거부로 수정했다. Focused speculative test와 CPU CTest 12/12가 통과했다.
- Task 2 RED는 `generate_speculative`, final state/full routing, speculative telemetry 부재 compile failure로 확인했다. 별도 incremental entrypoint가 proposal의 anchor와 bound를 target forward 전에 검증하고, accepted history만 one-token Engine에 적용하며, accepted prefix와 target bonus를 provider에 통지한다. Perfect 2-block과 early/partial/empty 혼합 4-block 모두 greedy token, final KDA/MLA state, 전체 route/K, Reader calls/bytes, L1 hit/miss가 exact 일치했다. Count 1은 provider를 호출하지 않고, exhausted provider, zero block, non-incremental, wrong anchor, oversized proposal은 명시적으로 실패한다. CPU CTest 12/12와 targeted cross-language pytest가 통과했다.
- 사용자 요청에 따라 공개 `main`의 PR #11/#12 합병 및 README 전용 절을 재확인했다. Public docs head `7748ca3`의 correctness run `31322913019`가 성공했다. M13 구현 이후 README, ARCHITECTURE, DECISIONS, BENCHMARKS, PROJECT_STATE를 실제 구현/검증 상태로 동기화하되 B-0014 미측정과 미완성 CLI 경계를 유지했다.
- M13 Task 3은 `none|scripted-reference`, block size, anchor:candidate script를 CLI에 추가했다. Malformed, wrong-mode, zero-block, exhausted, unused record를 거부하고 final state, full route/K, target forward count, proposal/accept/commit telemetry를 JSON/CSV로 기록한다. Focused speculative tests 9개와 전체 CPU CTest 12/12, pytest 245 passed/44 skipped가 통과했다.
- B-0014는 commit `2cf50b4`에서 3 warmup/20 samples로 greedy, perfect block-2, mixed block-2를 측정했다. Decode는 171.4333/174.0861/173.2344 tok/s이고 acceptance는 n/a/1.0/0.25였다. 모든 row가 target decode forward 5회, Reader 665,616 bytes, token/state/full route/K/L1 parity를 보존하므로 +1.55%/+1.05%를 speculative speedup으로 채택하지 않았다. Summary JSON/CSV SHA-256은 `7cd834b1c65d507367320170cdf72ca76aace9f6a743da85a0a9f0cca4a21062`/`9c5fdba84c547f93e2a0a7d4c0b76412181ffb2c635ffd969537a154950ce75b`이다.
- M13 post-measurement verification은 CPU 12/12·245/44, liburing/direct 13/13·247/42, CUDA 21/21·281/8, ASan/UBSan 13/13·targeted 26/3(104 deselected)을 통과했다. 첫 liburing pytest는 `K3X_TEST_IO_URING=1` 누락으로 expected-unavailable test 1건이 실패했고 실제 오류를 확인한 뒤 capability env를 복구한 재실행이 통과했다. CUDA perfect/mixed speculative CLI Compute Sanitizer는 각각 0 errors였다.
- B-0014 artifact와 ledger는 `e2e37bf`에 기록했다. 최종 자체 리뷰에서 default greedy 불변성, proposal의 target side-effect 전 검증, exhausted/unused script 거부, raw-summary parity, measured/proposed 문서 경계를 재확인했고 Critical/Important issue를 발견하지 못했다.
- M13 public integration head `463e9ca`는 branch/PR correctness run `31324378917`/`31324381376`을 통과했고 PR #13으로 fast-forward merge되었다. Post-merge main correctness run `31324492327`도 성공했다.
- 사용자의 전체 문서 갱신 요청에 따라 PR #11/#12/#13의 `MERGED` 상태와 공개 `main`을 다시 대조했다. README의 Milestone 11·12·13 전용 절과 ARCHITECTURE/DECISIONS/BENCHMARKS 원장은 이미 일치했지만, README 상단 배지가 Milestone 10에 머물렀고 `PROJECT_STATE.md`에 token-major speculation을 미구현으로 표시한 모순 및 최신 publication head 누락이 있어 현재 증거에 맞게 수정했다.
- 문서 동기화 head `4984728`은 PR #14로 fast-forward merge되었다. Push/PR correctness run `31325118488`/`31325132631`과 post-merge main run `31325294591`이 모두 성공했으며, 로컬 CPU CTest 12/12와 `K3X_BUILD_DIR=build-cpu` Python 245 passed/44 skipped도 통과했다. 최초 Python 명령은 build 디렉터리 환경 변수를 빠뜨려 `build/k3x_run` FileNotFoundError 92건을 냈고, 실제 설정을 확인한 교정 실행으로 통과했다.

## 2026-08-10 Milestone 14 설계

- 공개 main `df263c2`에서 `codex/milestone-fourteen-expert-major-verification` 브랜치를 기존 linked worktree에 생성했다. 직전 기준 검증은 CPU CTest 12/12와 Python 245 passed/44 skipped다.
- 현재 `Engine::forward`는 한 토큰의 전체 레이어와 persistent state를 즉시 변경한다. 단순 route replay는 실제 hidden state, target logits, Reader traffic, state commit을 검증하지 못하고, 전체 CUDA block 구현은 KDA/MLA rollback과 expert batching을 한 번에 묶는다. 따라서 첫 경계는 CPU·blocking·L1 disabled·natural routing으로 제한한 실제 layer-major block 실행이다.
- vLLM Kimi K3 commit `44351f81`은 multi-token `KimiMoE.forward`와 speculative KDA metadata의 `num_accepted_tokens` 경계를 제공한다. MoE-Spec `2602.16052` Appendix C는 active expert별 token gather와 batched computation을, SpecMoE `2604.10152`는 duplicate migration coalescing을 기술한다. EcoSpec `2607.12696`과 AcceptMoE `2608.02989`는 token 수, expert union, transfer traffic을 분리해 측정해야 함을 뒷받침하지만 그 정책은 이후 lossy 실험으로 남긴다.
- Milestone 14 구현 계획은 pure stable expert grouping, pure vector target verification, exact CPU layer-major block/state commit, CLI/telemetry, B-0015, 전체 검증·공개 합병의 여섯 의미 단위로 고정했다. 각 단위는 missing-interface RED, focused GREEN, 전체 회귀, semantic commit을 요구하며 token-major 기본값과 unsupported-combination preflight를 명시적으로 보존한다.
- Milestone 14 Task 1은 missing `k3x/expert_major.hpp` compile failure로 RED를 확인했다. Stable first-use planner는 token→router-slot 순서로 group과 assignment를 만들고 empty/mismatched/duplicate/non-finite route를 거부한다. Task 2는 missing `verify_greedy_target_block` compile failure로 RED를 확인한 뒤 완전한 `C+1` target vector를 선검증하고 기존 strict greedy commit 결과를 계산하는 pure API를 추가했다. Focused tests와 전체 CPU CTest 13/13이 통과했다.
- 선택 설계는 proposal input `[anchor,candidates...]` 전체를 레이어별로 실행하고, 각 MoE 레이어에서 natural route union을 first-use order로 만든다. Payload는 unique expert당 한 번 load하고 각 token/router-slot 결과를 저장한 뒤 token별 원래 router 순서로 누적한다. KDA는 위치별 snapshot, MLA는 temporary append 후 crop으로 accepted prefix state만 commit한다.
- 사용자의 전체 문서 갱신 및 합병 지시에 따라 공개 PR #11·#12·#13·#14의 `MERGED` 상태와 `origin/main=df263c2`를 재확인했다. 공개 README에는 Milestone 11·12·13 전용 절이 이미 포함되어 있으며, 이번 동기화는 현재 M14 개발 상태를 완료로 과장하지 않고 README·ARCHITECTURE·DECISIONS·BENCHMARKS·PROJECT_STATE·checklist·context notes에 같은 경계로 기록하는 작업으로 고정한다.
- Milestone 14 Task 3은 제안 block 전체의 state를 복사해 레이어 우선으로 실행하고 위치별 KDA/MLA snapshot을 보존한다. 각 MoE 레이어에서 natural route union을 만들어 unique expert payload를 한 번만 load하고, 결과는 token/router-slot에 저장한 뒤 원래 slot 순서로 누적한다. Pure verifier가 확정한 committed position의 snapshot과 route만 session에 반영하며 거절 suffix route는 evaluated telemetry로 분리한다. CPU focused test와 전체 CTest 13/13을 통과했고 `862d401`에 커밋했다.
- Task 4는 `--speculative-verification token-major|expert-major`, block/position/union/assignment/payload 계수, evaluated route JSON/CSV를 연결했다. Expert-major는 CPU·disabled L1·blocking L2·natural routing·profile 미관찰 조합만 허용하고 지원하지 않는 조합을 Reader/output side effect 전에 거부한다. 새 테스트의 `perfect_script` 지역 변수 오류 1건을 실제 traceback으로 확인해 `script`로 교정했고, WSL2 CPU CTest 13/13과 Python 253 passed/44 skipped를 통과한 뒤 `bdf4a66`에 커밋했다. Windows의 새로 relink된 executable은 Application Control `4551`로 차단되어 정책을 우회하지 않고 primary Linux 경로를 사용했다.
- B-0015는 greedy, token/expert perfect block-2, token/expert mixed block-2 다섯 행을 3 warmup/20 samples로 측정했다. Perfect expert-major는 30 assignment 중 6개를 reuse해 Reader bytes/calls를 665,616/428에서 655,824/392로 줄이고 201.5550 tok/s를 기록했다. Mixed expert-major는 8 position 중 3개를 discard하며 680,304 bytes, 482 calls, 122.6010 tok/s로 token-major 163.0028 tok/s보다 느려졌다. 모든 행이 greedy token·최종 state·committed route를 exact 보존했고 favorable direction은 강제하지 않았다.
- B-0015 artifact SHA-256은 `29f3fd10c95dcde9f2b012e10e36962363b5cdd79dfeda5f5e3bbaca0cb89b75`, aggregate-record SHA-256은 `cb95eff274713a21b821695d75ff2655da735513c99215ec5ec14f5ed995b813`이다. Raw JSON/CSV SHA를 summary에 보존하고 focused ablation/schema 12 passed/5 skipped로 재계산·field parity를 확인한 뒤 `1e73121`에 커밋했다. README와 TITAN Ledger는 PR #11/#12 합병 상태, M13 공개 상태, M14 개발/측정 상태를 동일하게 표시하도록 갱신했으며 M14 공개 합병은 아직 완료로 표시하지 않았다.
- M14 pre-publication 검증은 CPU CTest 13/13·Python 253/44, liburing/direct CTest 14/14·257/42, CUDA CTest 22/22·291/8, ASan/UBSan liburing CTest 14/14·targeted 95/35를 통과했다. CUDA FFN Compute Sanitizer는 0 errors였다. CPU-only expert-major CLI를 Compute Sanitizer로 감싼 시도는 instrumented CUDA API call 전 종료로 tool exit 1을 반환했으며 이를 sanitizer pass로 과장하지 않고 ASan/UBSan 증거를 사용한다.
- 최종 자체 검토는 default token-major 스위치, unsupported-combination preflight, proposal 선검증, committed-only state/route, evaluated suffix 분리, unique-load/assignment 계수, benchmark caller/schema 전체, 디버그 잔여, `git diff --check`를 대조했다. Raw JSON/CSV SHA 10개와 aggregate SHA를 재계산하고 perfect/mixed 변화율을 독립 계산해 문서 소수점과 일치함을 확인했다. 공유 가능한 수준으로 판정하되 full-model·native Linux·physical NVMe/H2D·coding quality 미측정 caveat는 유지한다.
- M14 integration head `012e598`은 branch/PR correctness run `31328853375`/`31328869071`을 통과했고 PR #15로 fast-forward merge되었다. Post-merge `main` correctness run `31329045623`도 성공했다. GitHub은 PR #15를 `MERGED`로, `origin/main`을 `012e598`로 확정했다.

## 2026-08-10 Milestone 11–14 공개 문서 재동기화

- 사용자 지시에 따라 PR #11과 PR #12의 GitHub 상태를 다시 조회했다. PR #11은 `edc6d605`, PR #12는 `9e59a9db`에서 병합되었고 두 커밋 모두 공개 문서 기준점 `46105f8`의 조상임을 확인했다.
- README에는 동적 `main` correctness 배지, PR #11/#12 병합 커밋, 공개 문서 기준점 `46105f8`과 성공한 correctness run `31329200483`을 추가했다. `PROJECT_STATE.md`의 이전 M14 활성 브랜치 표기는 M15 설계 준비 상태와 문서 전용 브랜치 경계로 교정했다.
- ARCHITECTURE, DECISIONS, BENCHMARKS, PERFORMANCE_MODEL, PLAN, K3X_FORMAT, PROJECT_CHARTER, 참고 문서와 이전 설계/계획 문서를 점검했다. 이미 정확한 구현·결정·측정·헌법·역사 기록은 불필요하게 다시 쓰지 않았으며, M15 구현이나 성능은 존재하는 것으로 표시하지 않았다.

## 2026-08-10 Milestone 15 설계

- M14의 stable first-use union은 expert payload를 host에서 한 번 읽지만 CUDA scalar FFN을 assignment마다 호출하면 transient gate/up/down weight가 반복 H2D된다. 따라서 M15의 좁은 목표를 단일 expert와 여러 token latent를 받는 native MXFP4 batch primitive로 고정했다.
- vLLM `83ad767e`는 token assignment를 expert별 정렬하고 한 expert weight에 token tile을 묶어 L2 재사용을 유도한다. MoonshotAI Kimi K3 `3cb39dfd`의 native MXFP4, latent 3,584, expert hidden 3,072, natural Top-16 계약을 유지한다. NVIDIA CUDA 13.3은 grouped/batched API가 모든 크기에서 우월하다고 보장하지 않으므로 범용 grouped GEMM 대신 기존 E2M1/E8M0 산술을 확장한 `(row, token)` 2차원 kernel을 측정 대상으로 선택했다.
- 임시 VRAM residency로 scalar 호출을 감싸는 안은 cache 소유권과 telemetry가 섞여 H2D union 증거가 모호해 배제했다. 모든 expert/token을 한 persistent kernel로 묶는 안은 routing, state rollback, variable assignments, ordered mixing을 한 번에 결합하므로 이번 milestone 범위를 넘는다.
- 첫 CUDA 경계는 `cuda-custom + ffn-block + reused + transient + synchronous + fusion none`, disabled L1, blocking L2, natural routing, no profile, four-layer synthetic graph로 제한한다. Token-major와 CPU expert-major는 그대로 남고, learned draft·동적 block·EcoSpec/MoE-Spec/AcceptMoE는 다음 독립 단계다.
- M15 구현 계획은 portable flat batch 계약, native MXFP4 2D launcher, CUDA FFN/H2D accounting, runtime/CLI integration, B-0016 tooling, 전체 측정·검증·공개의 여섯 의미 단위로 고정했다. 각 production 단계는 missing API/launcher/module의 실제 RED를 먼저 확인하고, focused GREEN과 semantic commit을 요구한다.
- Portable batch API RED는 `ComputeBackend` method 부재로 확인했다. CPU oracle은 기존 exact scalar group path를 순서대로 재사용하고 zero/overflow/flat-shape/native-weight/SiTU 오류를 profiler side effect 전에 거부한다. Native CUDA launcher는 `blockIdx.y`를 token으로 확장해 batch-size-one scalar bit parity와 3-token CPU oracle을 통과했으며 Compute Sanitizer는 0 errors였다.
- CUDA FFN batch path는 input을 한 번, gate/up/down packed+scale payload를 각각 한 번 올리고 gate/up/down batch launcher와 flattened SiTU를 거쳐 flat D2H 한 번으로 반환한다. 2-token literal은 weight H2D 1,105 bytes, activation H2D 256 bytes, D2H 8 bytes, batch call/token 1/2를 확인했고 malformed input은 allocation/counter/profiler를 변경하지 않았다.
- Runtime은 CPU scalar 경계를 유지하고 CUDA에서 stable expert group별 latent를 gather해 batch call 한 번으로 실행한 뒤 원래 `(token, router slot)`에 scatter한다. CLI는 `cuda-custom + ffn-block + reused + transient + synchronous + fusion none`, disabled L1, blocking L2, natural routing, no profile만 허용한다. Perfect/mixed CUDA integration은 token, final state, committed route parity와 batch-call=payload-load, batch-token=assignment를 확인했다.
- B-0016 runner는 greedy, token/expert perfect-2, token/expert mixed-2의 CUDA graph 5행과 released single-expert scalar/batch 2·4-token 4행을 분리한다. 최초 직접 CLI smoke는 repository import root와 실제 `--l2-schedule` flag 불일치를 찾아 회귀 테스트와 함께 교정했다. 1-sample smoke 뒤 3 warmup/20 samples 본 측정이 226.4초에 완료됐다.
- 최종 리뷰는 CSV가 생성 시 CRLF bytes로 hash된 뒤 Git blob은 LF로 정규화되어 raw CSV 9개와 summary.csv digest가 저장소 증거와 불일치하는 Important 1건을 찾았다. LF `lineterminator` 회귀 테스트는 기존 writer에서 RED 2건을 재현했고 공통/raw/summary writer를 교정한 뒤 GREEN 2/2를 통과했다.
- 교정 writer로 B-0016 3×20 전체를 다시 측정했다. Graph perfect expert-major는 token-major 60.1815 대비 70.1067 tok/s, 4,981,824 대비 4,972,032 weight H2D를 기록했다. Mixed expert-major는 3 rejected position 때문에 40.7627 tok/s와 6,627,744 weight H2D로 회귀했다. 모든 행은 greedy token, final KDA/MLA state, committed route를 exact 보존했다.
- Released 17,547,264-byte expert에서 batch-2는 scalar 대비 weight H2D를 701,890,560→350,945,280 bytes, latency를 3,444,884→1,737,798 ns로 줄였다. Batch-4는 1,403,781,120→350,945,280 bytes, 6,705,324→2,631,900 ns였다. Activation H2D/D2H는 pair별 동일하고 maximum error는 0이다. 이는 routing semantics 없는 bounded kernel/traffic 증거다.
- 새 canonical aggregate는 `09a2537337df1fd2b8b39439f92ba7306cb09a6ed5e3f8bdc8db7d9d787029aa`다. 전체 검증은 CPU CTest 13/13·pytest 262/47, liburing/direct CTest 14/14·pytest 264/45, ASan/UBSan CTest 14/14, CUDA CTest 22/22·pytest 301/8이다. GPU utilization, memory bandwidth, physical NVMe와 full-model quality는 측정하지 않았다.
- Compute Sanitizer는 native MXFP4, CUDA FFN, released batch-2, perfect/mixed expert-major CLI에서 모두 0 errors였다. Perfect/mixed 첫 직접 호출은 PowerShell/WSL 이중 파싱이 semicolon script를 잘라 유효 실행이 아니었고, 인자 배열을 전달하는 임시 Python helper로 교정 실행한 뒤 helper를 제거했다.
- Milestone 15 branch/PR correctness run `31332732339`/`31332745907`이 통과했고 PR #17이 공개 integration head `c18df33`으로 rebase merge되었다. 병합 후 `main` correctness run `31332852551`도 통과해 B-0016 코드, 측정 증거, LF-stable digest 교정, 최종 리뷰가 공개 기준선에 포함되었다.
- 문서 정합화 PR #18의 push/PR correctness run `31333096506`/`31333098541`이 통과했고 공개 문서 head `4052221`로 rebase merge되었다. 병합 후 `main` correctness run `31333233834`도 통과했다.

## 2026-08-10 Milestone 16 준비

- 새 Windows worktree의 첫 전체 baseline은 B-0011 profile SHA-256 한 건만 실패했다. Git blob과 summary는 LF bytes의 `897e872d…`로 일치했지만 `core.autocrlf=true`가 canonical ASCII `.k3xp`를 CRLF `940f0353…`로 바꾼 것이 원인이었다. `*.k3xp -text`를 추가하고 다섯 profile을 blob에서 재물질화한 뒤 CTest 13/13과 Python 262/47이 통과했다.
- DSpark 원문과 DeepSpec `005e03b8` 구현은 confidence의 누적 prefix survival로 proposal을 자르고 verified target state만 draft context에 반영한다. EcoSpec은 target verifier를 바꾸지 않고 marginal expert activation cost를 draft 선택에 넣으며, MoE-Spec은 verifier expert budget으로 품질 경계를 바꾼다. M16은 DSpark checkpoint나 expert budget을 흉내 내지 않고 reduced-Top-K self draft의 실제 acceptance와 기존 exact target cost를 먼저 측정한다.
- 외부 trained drafter는 Kimi K3 checkpoint/ABI가 없고, scripted trace는 대표 acceptance가 아니며, persistent AURORA state는 KDA/MLA crop까지 한 번에 묶는다. 따라서 별도 CPU Reader/backend/session이 committed prefix를 replay하는 AURORA reference를 선택했다. target은 natural token/expert-major strict verifier 그대로이고 draft/target traffic은 분리한다.
- Adaptive block scheduler는 `{1,2,4}`를 한 rung씩 탐색하고 위치별 Laplace-smoothed prefix survival 0.5와 measured unique-load/assignment ratio 0.9를 gate로 사용한다. 거절은 즉시 이전 rung으로 backoff한다. 이 값들은 B-0017 synthetic 실험 identity이며 기본 runtime 정책이 아니다.
- M16 구현 계획은 target feedback/provider stats, pure scheduler, replay provider, runtime feedback, CLI/schema, B-0017·전체 검증·공개의 여섯 의미 단위다. Artifact가 필요한 provider/model-session executable은 CTest로 위장하지 않고 Python fixture가 Top-16 K3X를 생성해 직접 실행한다.
- M16 Task 1은 `DraftVerification`에 target evaluated/discarded position과 expert-major payload-load/assignment feedback을 추가하고, 기존 provider 호환성을 보존하는 default-zero `DraftProviderStats` 계약을 추가했다. Missing-field RED 뒤 focused speculative CTest가 통과했다.
- M16 Task 2는 missing-source RED 뒤 순수 `{1,2,4}` scheduler를 구현했다. 길이 2의 fully accepted 관측이라도 payload-load/assignment 비율이 0.9를 넘으면 qualified frontier를 올리지 않아 이전 rung으로 되돌아가는 회귀를 추가했고, focused scheduler/speculative 실행이 통과했다. 길이 1 rejection은 다음 선택을 0으로 만들며 all-zero verification은 no-op이다.
- M16 Task 3은 missing-header RED 뒤 별도 Reader, CPU backend, RuntimeSession으로 committed prefix 전체를 재실행하는 replay provider를 구현했다. `model.cpp`는 실행 파일별 중복 컴파일에서 `k3x_runtime` 단일 소유로 옮겼다. Provider는 outstanding proposal 하나, anchor/position/history 일치, accepted candidate prefix, target-corrected commit, latched lifecycle error를 검증하고 draft Reader/routing/time 통계를 target과 분리한다. 실제 Top-16 artifact에서 후보가 독립 fixed-K4 greedy oracle과 일치했으며 CPU CTest 14/14와 focused pytest 1/1이 통과했다.
- M16 Task 4는 missing-result-field RED 뒤 token-major의 실제 target forward delta와 expert-major block의 evaluated/discarded position, payload load, assignment를 `DraftVerification`에 `update` 전 기록한다. 성공 반환에서만 provider 통계 13개를 `GenerationResult`로 복사하고 ordinary greedy는 모두 0을 유지한다. Model-session/speculative/AURORA focused 빌드와 artifact pytest 2개가 통과했다.
- Task 5 자체 대조에서 길이 1 rejection의 zero cap이 all-zero no-op과 결합해 adaptive scheduling을 영구 0-token으로 고정하는 결함을 발견했다. Zero cap은 request-zero 호출이 아니라 실제 다음 양수 request에서 한 번 소비되며, ordinary target step의 no-op feedback 뒤 길이 1 탐색을 재개하도록 회귀를 추가했다.
- M16 Task 5는 `aurora-replay`, draft K4/6/8/12, fixed/adaptive block policy를 CLI와 benchmark schema에 연결했다. AURORA 옵션은 해당 mode에서만 허용하고 incremental natural target, 빈 script, block 1/2/4, checkpoint natural K 미만을 output 생성 전에 강제한다. 별도 CPU Reader/backend/profiler가 draft traffic을 target Reader와 섞지 않으며 JSON/CSV는 13개 draft counter와 AURORA identity를 명시적으로 기록한다. Fixed/adaptive Top-16 실행은 natural token, final KDA/MLA state, complete target routing이 exact 일치했고 CPU CTest 14/14 및 focused Python 99 passed/37 skipped가 통과했다.
- B-0017은 deterministic Top-16 fixture에서 natural, fixed block 1/2/4, adaptive token, fixed-2 expert-major, adaptive expert-major 7행을 3 warmups/20 samples로 측정했다. 모든 행은 token/final-state/committed-route exact parity를 유지했고 raw JSON/CSV 14개 digest, LF CSV, canonical aggregate를 독립 재계산했다. Replay 전 행은 natural보다 46.35%~62.52% 느렸으며, 가장 나은 fixed-2 expert-major도 target 1,102,416 bytes 외에 draft 1,454,112 bytes를 읽었다.
- M16 전체 검증은 CPU CTest 14/14와 pytest 268/47, liburing/direct CTest 15/15와 pytest 274/41, ASan/UBSan CTest 15/15, CUDA CTest 23/23과 pytest 307/8을 통과했다. 새 CUDA expert-major AURORA CLI row는 Compute Sanitizer `ERROR SUMMARY: 0 errors`를 보고했다.
- Milestone 16 branch/PR correctness run `31337234073`/`31337240722`가 통과했고 PR #20이 공개 integration head `df5c07d`로 rebase merge되었다. 병합 후 `main` correctness run `31337365175`도 통과해 AURORA replay 구현, B-0017 증거, README, TITAN Ledger가 공개 기준선에 포함되었다.
- Milestone 16 공개 문서 정합 PR #21의 push/PR correctness run `31337548635`/`31337554179`가 통과했고 public documentation head `0eb0966`으로 rebase merge되었다. 병합 후 `main` correctness run `31337694471`도 성공했다.

## 2026-08-10 Milestone 19 실행 계획

- 상세 TDD 계획은 provider 계약, CLI 소유권, 독립 telemetry, B-0020 측정, 전체 검증과 공개 병합의 다섯 작업으로 고정했습니다.
- 계획 자체 검토에서 자리표시자와 구현을 막는 모순은 발견되지 않았습니다. 기존 exact `ResidentWeightTable`만 재사용하며 새로운 eviction, kernel, precision 축은 추가하지 않습니다.
- 활성 정책상 하위 에이전트는 사용하지 않고 주 에이전트가 `executing-plans` 절차로 RED, GREEN, 측정, 문서 동기화를 연결해 수행합니다.
- Task 1 RED는 8 MiB resident provider 생성이 기존 transient-only gate에서 거부되는 `test_cuda_aurora_draft.cu:85` 실패로 확인했습니다. 최소 GREEN은 transient-zero와 resident-positive만 허용하며, full-fit hit/miss와 zero bypass, one-byte exact bypass, CPU/transient/resident proposal parity를 검증했습니다. 집중 pytest 1개와 CUDA CTest 23/23이 통과했습니다.
- Task 2 RED는 새 옵션의 다섯 경계가 모두 `unknown argument: --aurora-draft-resident-bytes`로 실패하고 기존 20개 preflight 사례는 통과하는 것으로 확인했습니다. GREEN은 unsigned 64-bit 파싱, mode/backend 소유권, CPU build 무폴백, zero-transient/positive-resident backend 구성을 추가했습니다. CPU 두 파일은 96 passed/34 skipped, CUDA 파일은 2 passed/1 skipped, CPU/CUDA CTest는 14/14와 23/23으로 통과했습니다.
- Task 3 RED는 ordinary schema와 CUDA resident 실행 모두 새 세 필드의 `KeyError`로 실패했습니다. GREEN은 `draft_cuda_resident_bytes`, `draft_resident_weight_bytes`, `draft_peak_resident_weight_bytes`를 C++ JSON, `BenchmarkRecord`, 모든 process call, deterministic/options 검증, JSON/CSV CLI에 연결했습니다. 8 MiB resident benchmark는 bounded 점유, cache hit/miss, zero bypass, target residency zero를 보였고 CPU 12 passed/7 skipped·CTest 14/14, CUDA 5 passed/1 skipped·CTest 23/23이 통과했습니다.
- Task 4 runner RED는 `ModuleNotFoundError: tools.ablate_cuda_aurora_residency`로 확인했습니다. GREEN은 natural 1행과 transient/resident 4쌍, exact target·proposal·acceptance parity, 8 MiB capacity, zero bypass, lower weight H2D, pair delta와 hit rate, raw/summary digest를 검증하며 live one-sample 2/2가 통과했습니다. 측정 코드는 canonical 결과 생성 전에 별도 커밋합니다.
- Canonical B-0020은 WSL2 RTX 5080에서 3 warmups/20 samples로 184.1초 동안 실행했습니다. Resident는 네 pair에서 draft weight H2D를 88.809% 또는 89.775% 줄였고 hit rate는 75.682% 또는 77.273%, 점유는 644,160 또는 647,424 bytes, bypass는 모두 0이었습니다. Paired decode 변화는 fixed-token +15.582%, adaptive-token -2.561%, fixed-expert +22.673%, adaptive-expert +5.569%로 혼합되어 speedup을 일반화하지 않습니다. Aggregate digest는 `4bb84fe49cbbc735bc9ef8668ab4d2944fef3d4e3a0f1048a7973410b211df87`, summary CSV digest는 `059ede44149da8490f8342061b4dc10e623abe08f420266f84d7ca73963e3a62`이며 committed-evidence 포함 테스트 3/3이 통과했습니다.
- Fresh 전체 검증은 CPU CTest 14/14·pytest 284 passed/53 skipped, liburing/direct CTest 15/15·pytest 290 passed/47 skipped, ASan/UBSan CTest 15/15, CUDA CTest 23/23·pytest 328 passed/9 skipped로 통과했습니다. Compute Sanitizer는 8 MiB full-fit와 1-byte exact-bypass expert-major 경로 모두 `ERROR SUMMARY: 0 errors`를 보고했습니다. Full-fit는 666 hit/214 miss/0 bypass/644,160 bytes, bypass 경로는 0 hit/880 miss/880 bypass/0 resident bytes이며 동일 token을 생성했습니다.

## 2026-08-10 Milestone 17 준비

- 공개 `main` head `44aa049`에서 `codex/milestone-seventeen-persistent-aurora` worktree를 분리했다. CPU 기준선은 CTest 14/14와 Python 268 passed/47 skipped다.
- DeepSpec `005e03b8`의 실제 `draft_ops.py`는 block forward 뒤 `DynamicCache.crop(start)`를 호출하고 evaluator는 accepted draft prefix와 target token에 대응하는 verified hidden prefix만 다음 context로 남긴다. K3X는 target tensor ABI를 복사하지 않고 동일한 verified-only 상태 원칙을 KDA snapshot과 MLA logical crop으로 재현한다.
- 전체 `ModelState` deep copy는 MLA host traffic이 context에 비례하므로 기각했다. Periodic checkpoint partial replay도 반복 weight 실행을 남기므로 기각했다. 단일 mutable cursor, bounded KDA snapshot, append-only MLA size mark, target bonus teacher forcing을 M17 경계로 선택했다.
- 사용자의 기존 지침은 Cloud Run 이전의 단순 승인 단계를 중단 없이 진행하도록 허용한다. 따라서 비용·외부 자원·공개 API 기본값을 바꾸지 않는 이 설계 검토는 자율 승인으로 처리하되, replay oracle과 non-default 경계는 유지한다.
- M17 구현 계획은 cursor 초기 prefill/proposal, transaction crop/commit, persistent provider, CLI/schema, B-0018, 전체 검증·원장·공개의 여섯 의미 단위로 고정했다. 각 production 단계는 artifact-backed RED를 먼저 목격하고 focused GREEN과 semantic commit을 남긴다.
- 구현 실행은 사용자의 자율 진행 지침과 subagent 비사용 제약에 따라 이 세션의 inline `executing-plans` 경로를 선택한다.
- Task 1 RED는 `k3x/incremental_cursor.hpp` 부재로 정확히 실패했다. 최소 GREEN은 context 5 token을 한 번 prefill하고 첫 candidate 한 번만 forward해 fixed-K4 oracle과 같은 2-token proposal을 만들었다. Proposal 전 MLA length 5, proposal 후 speculative working length 6, incremental forward 1을 직접 확인했고 focused pytest 1/1과 CPU CTest 14/14가 통과했다.
- Task 2 RED는 아직 없는 `commit` 성공 경계에서 실패했다. Full-accept block-2는 proposal 1 forward 뒤 마지막 candidate와 target bonus를 각각 한 번 처리해 총 3 incremental forwards가 되었고 fresh teacher-forced flattened state와 같았다. Block-4에서 1개 accept는 처리된 rejected suffix 2 MLA position을 crop하고 KDA checkpoint를 복원한 뒤 다음 proposal까지 oracle과 일치했다. Zero proposal, wrong prefix, excessive acceptance, missing/multiple bonus, outstanding proposal, double commit은 실제 artifact로 검증했고 malformed commit은 Reader counter를 바꾸지 않았다.
- Task 3 provider RED는 `AuroraPersistentDraftProvider` 미선언에서 확인했다. Lazy cursor, 공통 runtime validator, update 선검증, scheduler/history 후커밋, Reader/time/routing/cursor telemetry를 연결했다. Top-16 synthetic artifact에서 full accept, zero schedule, rejection rollback 후보가 replay와 같았고 token-major 및 CPU expert-major target의 token/final state/routes/acceptance/proposal count도 같았다. Persistent draft Reader bytes는 동일 실행의 replay보다 작았으며 일반 greedy와 replay의 cursor 계수는 모두 0이었다. 기본 synthetic artifact는 expert 수가 fixed K4보다 작아 end-to-end AURORA를 실행할 수 있으므로, 해당 검증은 의도대로 Top-16 artifact를 만드는 `test_aurora_runtime.py`에 유지했다.
- Task 4 RED는 CLI의 `unknown speculative mode: aurora-persistent`와 benchmark schema의 cursor key 부재에서 확인했다. 기존 AURORA preflight 순서를 공유하면서 provider 선택만 persistent로 분기했고 `draft_context_prefill_tokens`, `draft_incremental_forward_calls`, `draft_rollback_events`, `draft_mla_positions_cropped`, `draft_kda_checkpoint_bytes`를 C++ JSON과 Python JSON/CSV record에 끝까지 전달했다. 기존 exact error 문자열은 호환 계약으로 보존했다. Fixed/adaptive persistent CLI는 natural baseline과 token/final state/routes가 일치했고 Python 집중 매트릭스 101 passed/37 skipped 및 CTest 14/14를 기록했다.
- Task 5 RED는 `tools.ablate_persistent_aurora` 부재에서 확인했다. B-0018은 warmup 3/sample 20의 9행 WSL synthetic 측정으로, 네 replay/persistent 쌍 모두 proposal·acceptance·target token·final state·committed route가 일치했다. Persistent fixed token은 replay 대비 decode +14.967%, draft Reader bytes -45.960%, adaptive token은 +41.755%/-63.075%, fixed expert-major는 +14.547%/-45.960%, adaptive expert-major는 +27.085%/-63.075%였다. Persistent context replay는 모두 0이고 context prefill은 5 token이었다. 이는 Reader logical bytes이지 물리 NVMe traffic이 아니다. Aggregate digest는 `abcef1afca7d6208808941323565bce44f25ecc6e9e0d28292ad54bfc7760cd0`, summary CSV digest는 `c65d3bb9d8805f66249d0bb6ba380b8aa2508fd53a073c1bc3dece82e00fe472`이며 committed evidence test 3개가 raw/summary/aggregate와 headline percent를 독립 재계산했다.
- Task 6 pre-publication verification은 CPU CTest 14/14·Python 272/47, liburing/direct CTest 15/15·Python 278/41, CUDA CTest 23/23·Python 311/8을 통과했다. ASan/UBSan liburing CTest 15/15와 artifact-backed persistent cursor/provider/evidence 5개도 통과했다. CUDA 13.3 native `sm_120` build에서 `aurora-persistent + expert-major + cuda-custom` Compute Sanitizer는 `ERROR SUMMARY: 0 errors`였다. CPU cursor는 ASan/UBSan, CUDA target 경계는 Compute Sanitizer 증거로 구분한다.
- Milestone 17 최종 자체 검토는 `origin/main...670f0f3`의 default path, replay compatibility, target ownership, malformed update Reader 무접근, MLA deep-copy 부재, telemetry zero/parity, raw-summary digest를 대조했고 Critical/Important를 발견하지 못했다. 기본 speculation은 `none`, replay는 exact oracle, persistent는 opt-in으로 유지된다.
- Milestone 17 전체 9-commit branch를 공개 `codex/milestone-seventeen-persistent-aurora`에 push하고 ready PR #23을 열었다. README 공개 표는 PR 상태를 링크하며 merge/post-merge CI 완료 전에는 merged로 표시하지 않는다.
- Milestone 17 branch/PR correctness run `31340338639`/`31340340063`이 통과했고 PR #23을 공개 integration head `30bbf7a8`로 rebase merge했다. 병합 후 `main` correctness run `31340476396`도 통과했다. 최종 read-only review는 B-0016 LF-stable CSV digest를 현재 blob과 다시 대조해 9/9 일치 및 새 Critical/Important 없음으로 종료했다.
- GitHub Actions는 `actions/checkout@v4`와 `actions/setup-python@v5`의 Node.js 20 deprecation 경고를 냈지만 run은 성공했다. 이는 M17 correctness나 B-0018 수치를 바꾸지 않는 별도 CI 유지보수 항목이다.

## 2026-08-10 Milestone 18 준비

- 공개 `main` documentation head `808b64d`에서 `codex/milestone-eighteen-cuda-aurora-draft` 작업트리를 분리했다. 최초 Windows checkout은 전역 `core.autocrlf=true` 때문에 canonical LF인 `results/**/*.json|csv`를 CRLF로 materialize해 B-0018 raw digest 테스트 1개가 실패했다. Git blob과 기존 LF worktree의 SHA-256은 summary와 일치했고 줄바꿈 외 diff는 없었다.
- `.gitattributes`에 `results/**/*.json text eol=lf`와 `results/**/*.csv text eol=lf`를 추가한 commit `cf4c8bd`의 새 checkout에서 실패하던 digest 테스트 1/1과 전체 Python 272 passed/47 skipped가 통과했다. 테스트 정규화나 CRLF digest 채택은 byte-exact evidence를 약화시키므로 기각했다.
- B-0018의 다음 병목은 persistent CPU draft graph 자체다. 첫 GPU 실험은 target CPU·draft FP32 cuda-custom transient만 바꿔 placement 효과를 분리한다. CUDA residency를 동시에 켜면 placement와 cache 효과가 섞이고, BF16부터 시작하면 proposal/acceptance 품질 변화가 섞이므로 각각 후속 축으로 미뤘다.
- Milestone 18은 `--aurora-draft-backend cpu|cuda-custom`을 추가하되 기본 CPU, replay CPU-only, persistent CUDA opt-in을 유지한다. CUDA draft identity는 reused·transient·grouped·ffn-block·synchronous·fusion-none으로 고정하고 draft profiler/memory/runtime counters를 target과 별도 직렬화한다.
- M18 구현 계획은 provider의 canonical CUDA 계약, CLI 소유권·preflight, 분리 telemetry/schema, B-0019, 전체 검증·원장·공개의 다섯 review 단위로 고정했다. Provider 내부 계약을 먼저 완성해 중간 commit이 CUDA 선택을 CPU로 오표기하지 않도록 CLI 공개 단계와 분리했다.
- Task 1 RED는 CUDA build에 `test_cuda_aurora_draft` 실행 파일이 없는 상태로 정확히 실패했다. `aurora.cpp`의 공통 option gate와 backend gate를 분리해 replay는 CPU-only, persistent는 CPU 또는 FP32·reused·transient·grouped·ffn-block·synchronous·fusion-none·zero-capacity CUDA identity만 허용했다. Direct provider test는 initial/full-accept/partial-rollback proposal과 cursor 계수 parity를 확인했고 focused Python 3개와 CUDA CTest 23/23이 통과했다. 구현 commit은 `89ca6c6`이다.
- Task 2 RED는 세 새 CLI case가 `unknown argument: --aurora-draft-backend`로 실패하고 기존 17개 preflight case는 통과하는 것으로 확인했다. Parser와 mode gate, 별도 draft backend 생성·소유권, `none|cpu|cuda-custom` JSON identity를 추가했다. CPU target의 CPU/CUDA draft fixed·adaptive 행은 token/state/route/proposal·acceptance/cursor 계수가 일치했고 CPU build CUDA 요청은 typed unavailable로 종료해 fallback하지 않았다. 구현 commit은 `785f73e`이며 focused CPU 22/1, CUDA 3/1과 CPU/CUDA CTest 14/14·23/23이 통과했다.
- Task 3 RED는 ordinary benchmark record의 `aurora_draft_backend` 부재와 CUDA runtime JSON의 `draft_device` 부재로 확인했다. Draft 전용 profiler·memory·runtime snapshot과 고정 CUDA identity를 CLI JSON 및 `BenchmarkRecord` JSON/CSV에 분리해 전달했다. CPU target의 kernel/H2D/VRAM 계수는 0을 유지하면서 CUDA draft의 해당 계수만 양수였고, focused CPU 12 passed/6 skipped와 CUDA 1 passed/1 skipped 및 CPU/CUDA CTest 14/14·23/23이 통과했다.
- Task 4 measurement runner RED는 `tools.ablate_cuda_aurora_draft` import 부재로 확인했다. 최소 GREEN은 natural 1행과 persistent CPU/CUDA draft의 fixed/adaptive × token/expert 네 쌍을 고정하고, target token/state/committed route 및 proposal/acceptance 동등성을 summary 작성 전에 검증한다. One-sample live RTX 5080 test 2개가 통과했으며 CPU target counter는 0, CUDA draft kernel/H2D/peak VRAM만 양수임을 확인했다.
- B-0019는 WSL2 RTX 5080에서 3 warmup/20 samples로 9행을 측정했다. CUDA draft는 CPU draft 대비 fixed/adaptive token에서 -96.465%/-96.810%, fixed/adaptive expert에서 -97.000%/-96.219% decode regression을 기록했다. Exact target token/state/committed route와 proposed/accepted/committed counts는 모든 쌍에서 같았지만, transient synchronous draft는 실행당 5,843,840~6,428,224 H2D bytes와 37,471,088~54,549,680 ns kernel time을 추가했다. Aggregate digest는 `ce1a599eb04077f3b0c1b8350254b126a58f4dc311421bfc38fc8f7a78478c59`, summary CSV digest는 `1b6234889c8997486e5b268d277af3ca892b2b6c152489b4f85cea81717edd1f`이며 committed evidence test 3개가 통과했다.
- M18 전체 CPU 첫 실행은 CUDA live ablation이 CPU build에서도 실행되는 test capability gate 누락을 발견했다. Runtime은 의도대로 `BACKEND_UNAVAILABLE`을 반환했고, live test만 `K3X_TEST_CUDA=1`에서 실행하도록 수정하면서 matrix/evidence tests는 CPU에서도 유지했다. 최종 검증은 CPU CTest 14/14·pytest 278/50, liburing/direct CTest 15/15·pytest 284/44, ASan/UBSan CTest 15/15, CUDA CTest 23/23·pytest 319/9를 통과했다. Exact CUDA draft와 CPU expert-major target의 Compute Sanitizer는 `ERROR SUMMARY: 0 errors`였다.
- M18 pre-publication self-review는 `origin/main...HEAD`의 CPU default, replay CPU-only gate, canonical CUDA identity, backend failure propagation, target/draft counter ownership, proposal/acceptance parity, raw-summary digest, measured/proposed wording, full-checkpoint/cloud 금지를 대조했다. 새 Critical/Important 문제를 발견하지 못했고, transient CUDA path는 구현된 exact diagnostic이지만 기본값으로 거부된다는 경계를 모든 원장에 동일하게 유지했다.
- Milestone 18의 16-commit branch를 공개 `codex/milestone-eighteen-cuda-aurora-draft`에 push하고 ready PR #25를 열었다. README 공개 표는 PR 상태를 링크하며 merge/post-merge CI 완료 전에는 merged로 표시하지 않는다.
- Milestone 18 push/PR correctness run `31343260116`/`31343261633`이 통과했고 PR #25를 공개 integration head `7899a7ae`로 rebase merge했다. 병합 후 `main` correctness run `31343401178`도 통과했다. Transient CUDA draft는 exact experimental path로 공개됐지만 B-0019에 따라 기본값으로는 거부된 상태를 유지한다.
- Milestone 19는 B-0019의 5,843,840~6,428,224 draft H2D bytes와 410~451 sync를 분리하기 위해 FP32 exact static residency만 바꾼다. 기존 `ResidentWeightTable` 재사용을 선택하고, dynamic L0 eviction은 policy 축으로, persistent multi-token/expert kernel은 launch 축으로, reduced precision은 quality 축으로 각각 미뤘다. Canonical B-0020은 8 MiB full-fit capacity를 사용하되 zero bypass와 actual occupancy로 full-fit을 검증하며, 속도 개선 방향은 gate로 강제하지 않는다.
- Milestone 19 합병 직전 검증은 CPU CTest 14/14·pytest 284/53, liburing/direct CTest 15/15·pytest 290/47, ASan/UBSan CTest 15/15, CUDA CTest 23/23·pytest 328/9를 통과했다. Compute Sanitizer full-fit와 1-byte exact-bypass는 모두 `ERROR SUMMARY: 0 errors`였고 동일 토큰을 생성했다. 최종 읽기 전용 검토는 B-0016 Git blob의 raw JSON/CSV 9쌍과 summary CSV digest까지 재계산해 일치를 확인했으며 새 Critical/Important 문제를 발견하지 않았다.
- Milestone 19 push/PR correctness run `31346575341`/`31346587586`이 통과했고 PR #27을 공개 integration head `c88456c0`로 rebase merge했다. 병합 후 `main` correctness run `31346725071`도 통과했다. README와 TITAN Ledger는 실제 공개 상태를 별도 문서 정합성 변경으로 기록한다.

## 2026-08-10 Milestone 20 준비

- 공개 `main` head `01eac162`에서 `codex/milestone-twenty-resident-grid` 브랜치를 시작했다. 수정 전 baseline은 CPU CTest 14/14·pytest 284 passed/53 skipped와 CUDA CTest 23/23을 통과했다.
- B-0020 exact 8 MiB resident fixed-token 경로를 Nsight Systems 2026.1.3으로 한 번 진단했다. 5 context-prefill과 5 incremental draft forward에서 kernel launch 1,040회, async memcpy 1,346회, stream sync 410회가 발생했지만 GPU kernel 총시간은 약 1.13 ms였다. 계측된 host API 총시간은 launch 약 35.03 ms, async memcpy 약 71.55 ms, stream sync 약 0.94 ms였다. 이는 throughput 측정이 아니라 다음 경계 선택용 attribution이다.
- 자기회귀 draft token은 이전 draft logit의 argmax에 의존하므로 여러 후보 token을 동시에 실행한다는 해석을 기각했다. 대신 동일 K3 shape의 resident expert와 독립 token 입력을 `[expert, token, row]` grid로 처리하는 계약을 선택한다. AURORA runtime은 token count 1로 multi-expert 이득만 사용하고, direct backend test가 token count 2/4를 검증한다.
- CUDA Graph cache는 ordered routing-set 재사용률과 bounded cache policy가 미측정이라 보류했고, 전체 device-resident KDA/MLA/router/argmax graph는 correctness 변경 범위가 너무 넓어 후속 단계로 분리했다. Resident grid는 four-launch, separate output, stable CPU accumulation, whole-request serial fallback을 핵심 불변식으로 둔다.
- Milestone 20 상세 계획은 CPU rectangular oracle, native 3D grid primitive, resolved resident/fallback backend, AURORA ownership, 독립 telemetry, B-0021 tooling/evidence, 전체 검증·공개의 여덟 review 단위로 고정했다. 각 production 변경은 focused RED를 먼저 확인하고 의미 단위 commit으로 분리한다.
- Task 1 RED는 `ComputeBackend`에 `mxfp4_situ_mlp_grid`가 없다는 compile failure로 확인했다. GREEN은 expert-first/token-second CPU oracle, equal-shape·unique nonzero tensor ID·overflow·payload 선검증, `resident_grid` identity와 zero-default counters를 추가했다. Focused backend와 CPU CTest 14/14가 통과했고 CUDA backend는 다음 RED를 위해 기본 unavailable 상태다.
- Task 2 RED는 CUDA descriptor, layout enum, grid launcher가 없다는 NVCC compile failure로 확인했다. GREEN은 `(expert, token, row)` 3D native MXFP4 kernel과 shared/expert-token-major input layout을 추가했다. 1x1, 1x4, 2x2, 4x4 literal cases가 통과했고 Compute Sanitizer memcheck는 0 errors였다.
- Task 3 RED는 CUDA backend의 기본 unavailable 결과로 확인했다. GREEN은 모든 resident acquisition을 먼저 판정하고, 전부 상주할 때 descriptor 한 번 업로드와 gate/up/SiTU/down 네 번의 launch 및 한 번의 synchronization을 수행한다. bypass가 하나라도 있으면 성공 grid counter를 건드리지 않고 기존 정확한 serial FFN 경로를 token별로 재사용하며, 선행 admission bytes도 H2D 통계에 보존한다. `cuda_expert_grid`, `cuda_residency`, `cuda_ffn`과 Compute Sanitizer가 통과했다.
- Task 4는 `resident-grid`를 target과 AURORA persistent cuda-custom draft의 폐쇄형 identity로만 허용했다. Stable LatentMoE는 token_count 1 grid를 선택하되 기존 router-slot 누적 순서를 유지한다. CPU CLI 계약, 실제 CUDA provider의 full-fit·1-byte fallback, token-major·expert-major CLI parity가 통과했고 전체 CPU CTest 14/14와 CUDA CTest 24/24가 통과했다.
- Task 5는 일곱 resident-grid counter를 target·draft JSON과 `BenchmarkRecord` JSON/CSV에 그대로 전달한다. 반복 샘플에서는 median이나 sum 대신 첫 샘플과의 deterministic identity를 강제하고, requested option parity에 draft batching을 추가했다. CPU schema/parity 110 passed·38 skipped와 CUDA AURORA 2 passed·1 skipped가 통과했다.
- Task 6은 in-memory synthetic 1/2/4 expert-token grid direct benchmark와 B-0021 9-row grouped/grid AURORA runner를 추가했다. Pair gate는 token·acceptance·routing·Reader parity와 full-fit zero fallback, 네 launch/call, grouped 대비 실제 MoE launch 감소를 요구하되 TPS 방향은 강제하지 않는다. Direct 4x4 측정은 3 measured calls, 12 launches, max abs error 5.96e-08이었고 live one-sample ablation 2 tests와 schema 18 tests가 통과했다.
- Task 7 B-0021은 warmup 3·sample 20으로 RTX 5080/WSL2에서 완료됐다. 네 pair 모두 MoE launch가 75% 감소했고 grid decode는 grouped 대비 fixed-token +10.79%, adaptive-token +24.09%, fixed-expert +38.00%, adaptive-expert +21.86%였다. 모든 grid row는 fallback 0이며 token/state/route/acceptance/Reader parity gate를 통과했다. Aggregate SHA-256은 `a628064544cdae0d06af7177539bc253f264946840f59651508121146af2edda`다.
- M20 최종 검증은 CPU CTest 14/14·pytest 290/55, liburing/direct CTest 15/15·pytest 296/49, ASan/UBSan CTest 15/15, CUDA CTest 24/24·pytest 336/9를 통과했다. Direct grid와 4x4 benchmark의 Compute Sanitizer는 모두 오류 0건이다.
- 문서 동기화에서 M11·M12의 공개 PR #11/#12 병합 상태와 전용 README 설명을 유지하고, M20의 구현·B-0021·한계·비기본 상태를 README와 TITAN Ledger 전체에 반영했다. Grid는 launch를 줄였지만 exact weight H2D를 줄이지 않았고 total draft H2D는 소폭 증가했으므로 full-model 또는 coding-quality 성능으로 확대 해석하지 않는다.
- Milestone 20 push/PR correctness run `31351465644`/`31351486146`이 통과했고 PR #29를 공개 integration head `90b20c87`로 rebase merge했다. 병합 후 `main` correctness run `31351649761`도 통과했다. README와 원장은 M11·M12를 포함한 실제 공개 상태와 M20 PR 링크를 기준으로 다시 동기화한다.

## 2026-08-10 Milestone 21 준비

- 공개 문서 reconciliation head `7728acd0`에서 `codex/milestone-twenty-one-resident-moe-layer` 브랜치를 시작했다. 사용자의 Cloud Run 전 상시 승인에 따라 별도 승인 질문 없이 다음 측정 가능한 correctness boundary로 진행한다.
- B-0021 fixed grid row는 30 MoE grid call, 470 draft stream synchronization, 108,800 activation H2D bytes, 102,880 D2H bytes를 기록한다. Adaptive row는 33 call과 517 synchronization을 기록한다. Weight H2D는 이미 644,160~647,424 bytes로 bounded이므로 다음 병목은 split MoE subcall 사이의 host round trip이다.
- 선택안은 router를 CPU에 유지하면서 routed-down, resident expert grid, router-slot ordered sum, RMSNorm, routed-up, shared SiTU MLP, final add를 한 stream과 한 최종 synchronization으로 묶는 `moe-layer` boundary다. CUDA Graph cache는 ordered expert-set reuse와 bounded cache policy가 미측정이라 보류하고, 전체 token graph는 KDA/MLA/router/rollback을 동시에 바꾸므로 후속 단계로 남긴다.
- NVIDIA CUDA Programming Guide는 graph topology의 definition/instantiation/execution을 분리하고 parameter update와 topology-changing re-instantiation을 구분한다. 이 제약 때문에 이번 milestone에서 graph caching을 실행 경계와 함께 도입하지 않는다.
- M21은 exact FP32/native-MXFP4 resident-only opt-in으로 제한한다. Hard-cap bypass는 launch 전 `executed=false`로 반환해 M20 split CUDA path 전체를 재사용하고, CUDA 오류는 fallback하지 않는다. B-0022는 sync/H2D/D2H 감소를 gate로 삼되 TPS 방향은 강제하지 않는다.
- M21 상세 계획은 portable whole-layer oracle, ordered mix/strict RMSNorm/add CUDA primitives, resident backend/fallback, runtime·AURORA·CLI ownership, 독립 telemetry, B-0022 tooling/evidence, 전체 검증·공개의 여덟 review 단위로 고정했다. Public type과 counter 이름을 backend부터 JSON/CSV 및 evidence까지 동일하게 유지하며 각 production task는 focused RED를 먼저 확인한다.
- Task 1은 누락된 `ResidentMoeLayerView`, `resident_mxfp4_moe_layer`, telemetry 필드의 컴파일 실패를 RED로 확인한 뒤 GREEN으로 전환했다. CPU oracle은 전체 요청을 실행 전에 검증하고 routed-down, expert SiTU, router 순서 혼합, RMSNorm, routed-up, shared SiTU, 최종 합산을 exact reference 순서로 실행한다. 선택 CTest `backend|ops|profile` 5/5가 통과했으며 CUDA 전용 카운터는 CPU 성공·거부 모두 0이다.
- Task 2는 `moe_layer.cuh` 누락 컴파일 실패를 RED로 확인한 뒤 ordered expert mix, CPU 순서의 double 제곱 누산 RMSNorm, vector add launcher를 구현했다. `cuda_moe_layer_ops` CTest가 통과했고 CUDA 13.3 절대 경로로 실행한 Compute Sanitizer는 오류 0건을 보고했다. 새 profiler operation은 아직 backend event에 연결되지 않았으며 Task 3에서 13-operation timing에 사용한다.
- Task 3은 inherited `backend_unavailable` 종료를 RED로 확인한 뒤 six dense/vector 및 3-per-expert resident preflight, layer 전용 grow-only scratch, 26 reusable events, 13-operation stream, one-result D2H, one synchronization, launch-free hard-cap bypass를 구현했다. `cuda_moe_layer|cuda_expert_grid|cuda_ffn|cuda_residency` 5/5와 complete-layer Compute Sanitizer 오류 0건을 확인했다.
- 구현 자가 검토에서 split CPU RMSNorm에는 없던 routed norm L0 admission 때문에 B-0022의 기존 equal-weight-H2D gate가 물리적으로 불가능함을 확인했다. D-047은 이 bytes를 숨기거나 activation으로 재분류하지 않고 weight H2D로 유지하며, layer-minus-split weight delta와 resident-byte delta의 일치 및 total-H2D 감소를 gate로 바꾼다.
- Task 4는 `--cuda-boundary moe-layer`와 독립 `--aurora-draft-boundary ffn-block|moe-layer`를 fail-closed 조합으로 연결했다. Model dispatch는 routed-down/shared split 계산 전에 complete-layer backend를 한 번 호출하며, hard-cap `executed=false`에서는 같은 router decision, payload handles, contributions를 재사용해 M20 split path로 내려간다. 1-byte live case는 layer와 grid 모두 hard-cap bypass하여 `draft_resident_grid_calls=0`, `draft_resident_grid_fallbacks>0`가 되는 것이 의도된 계약이다. CPU focused matrix는 104 passed/35 skipped, CUDA matrix는 133 passed/6 skipped로 통과했다. 새 layer counter의 runtime JSON/benchmark schema 전달은 Task 5로 분리한다.
- Task 5 RED는 ordinary benchmark JSON의 `resident_moe_layer_calls` 부재로 확인했다. 다섯 layer counter를 target과 draft runtime JSON, `BenchmarkRecord`, JSON/CSV field order, 첫 샘플 parsing에 동일 이름으로 전달했다. CPU schema/ownership matrix는 13 passed/8 skipped, live CUDA matrix는 19 passed/2 skipped로 통과했으며 full-fit은 calls 양수·13 launches/call·fallback 0, 1-byte는 calls 0·fallback 양수를 증명한다.
- Task 6 RED는 `tools.ablate_cuda_aurora_moe_layer` import 부재로 확인했다. B-0022 runner는 natural 1행과 동일 조건의 split-grid/layer 4쌍을 고정하고, target/draft identity parity, 3 sync/call 감소, activation/total H2D와 D2H 감소, positive norm weight/resident delta 일치, 13 launches/call, zero fallback을 summary 기록 전에 강제한다. `benchmark_once`에는 독립 `aurora_draft_boundary` 전달을 추가했다. CPU matrix/schema는 12 passed/8 skipped, 실제 CUDA one-sample은 2 passed/1 skipped로 통과했다.
- Task 7 B-0022는 RTX 5080/WSL2에서 3 warmup·20 sample·9행으로 완료했다. 네 split/layer paired decode 변화는 +5.619%, -2.753%, -1.216%, +3.933%로 혼합됐다. Fixed/adaptive layer는 30/33 calls, 90/99 sync 감소, 14,496/15,984 total-H2D 감소, 26,880/29,568 D2H 감소를 기록했다. Norm admission은 모든 pair에서 weight H2D와 resident bytes가 동일하게 384 bytes 증가했고 fallback은 0이다. Aggregate digest는 `404064335dcf2adf6c580b7f99812627cb357693aa6d9bcd414f4d51b2b19a9b`, summary CSV digest는 `5852eeb4ee0c792524f31df92566bc8052212877a1be92f5367872a34b7e5e4d`다.
- Task 8 fresh verification은 CPU CTest 14/14·pytest 295/56, liburing/direct CTest 15/15·pytest 301/50, ASan/UBSan CTest 15/15, CUDA CTest 26/26·pytest 341/10으로 통과했다. Low-level operations와 complete layer Compute Sanitizer는 모두 오류 0건이다. `CudaBoundaryMode` 사용처와 다섯 target/draft counter의 backend→main JSON→BenchmarkRecord→CSV→B-0022 소비자를 검색했고 operation/CPU/natural/speculation-none 기본값이 유지됨을 확인했다. Focused evidence/CLI matrix는 103/33으로 통과했다.
- Milestone 21 branch/PR correctness run `31355460022`/`31355471896`과 CodeQL `31355471922`가 통과했고 PR #31을 public integration head `97eb3e4e`로 rebase merge했다. 병합 후 `main` correctness run `31355678835`도 통과했다. Public README와 TITAN Ledger는 이 실제 공개 상태를 기준으로 후속 문서 reconciliation에서 교정한다.

## 2026-08-10 Milestone 22 준비

- Public documentation head `feb17b5a`에서 `codex/milestone-twenty-two-released-moe-layer` 브랜치를 시작했다. B-0022의 traffic/sync 방향은 명확하지만 paired timing이 혼합됐으므로 CUDA Graph 선택 전에 released-dimension bounded evidence를 추가한다.
- 선택안은 hidden 7,168, routed latent 3,584, expert intermediate 3,072를 그대로 사용하고 기존 17,547,264-byte storage expert를 unique tensor ID의 1/4/16 slots로 반복 참조하는 direct CUDA benchmark다. Full Top-16 layer resident weight는 norm을 포함해 750,532,608 bytes이며 1 GiB hard capacity 안에 들어간다.
- Split CUDA 결과를 numerical oracle로 한 번 계산하고 별도 selected backend의 cold admission 뒤 warm 3/sample 20을 측정한다. B-0023은 boundary latency이며 token TPS가 아니다. Warm weight H2D 0, split/layer sync 4:1, activation/D2H 감소, norm cold/resident delta 14,336 bytes, zero fallback을 gate로 고정하고 latency 방향은 강제하지 않는다.

## 2026-08-10 Milestone 22 구현 계획

- 상세 TDD 계획에서 책임 경계를 고정했다. C++ 바이너리는 bounded released-shape 실행과 원시 telemetry를 소유하고, Python runner는 6행 pair 검증, summary, digest-backed evidence를 소유한다.
- 구현 순서는 CLI/fixture와 split 실행, cold/warm telemetry 및 numerical parity, B-0023 tooling, 정식 RTX 5080 evidence, 전체 검증과 TITAN Ledger 동기화다.
- 허용 오차 `1e-5` 이내의 numerical parity, bypass/fallback 0, 측정 구간 warm weight H2D 0, 반복당 split 4회 대 layer 1회의 synchronization, 더 낮은 activation H2D와 D2H, 정확히 14,336-byte인 routed-norm cold/resident delta를 gate로 유지한다. Latency 방향은 기록하되 강제하지 않는다.
- Task 1 RED는 `k3x_cuda_moe_layer_bench` 부재로 8개 case가 실패하는 것으로 확인했다. CUDA compiler 경로를 `/usr/local/cuda-13.3/bin/nvcc`로 명시해 `sm_120` build를 복원한 뒤, strict CLI, released-size deterministic FP32 fixture, unique logical expert views, exact split 및 complete-layer 실행을 구현했다. 두 live boundary와 6개 오류 계약은 8/8로 통과했다.
- Task 2 RED는 네 live case 모두 `hidden_width` key 부재로 실패했다. 별도 split CUDA oracle, selected backend cold admission, warmup, measured snapshot을 분리하고 전체 JSON contract를 구현했다. 1/16 experts의 split/layer와 6개 오류 계약은 10/10으로 통과했고, 1-expert layer Compute Sanitizer는 maximum error 0과 `ERROR SUMMARY: 0 errors`를 기록했다.
- Task 3 RED는 `tools.ablate_cuda_released_moe_layer` import 부재로 확인했다. Runner는 1/4/16 experts의 split/layer 6행을 고정하고, raw JSON digest와 LF-only summary CSV, aggregate/runner/artifact digest를 생성하기 전에 identity, numerical, zero-fallback, warm-residency, exact synchronization/launch, 14,336-byte norm delta, activation/D2H 감소를 강제한다. 단위 gate는 9 passed/1 skipped, RTX 5080 one-sample matrix는 10/10으로 통과했다.
- Task 4의 초기 B-0023 측정은 이후 최종 리뷰에서 oracle backend 수명 오염이 확인되어 폐기하고 재측정했다. 교정 결과와 유효 digest는 아래 최종 리뷰 수정 기록을 따른다.
- Task 5 full verification은 CPU CTest 14/14·pytest 305/67, liburing/direct CTest 15/15·pytest 307/65, ASan/UBSan CTest 15/15, CUDA CTest 26/26·pytest 362/10으로 통과했다. Released one-expert complete-layer Compute Sanitizer는 `ERROR SUMMARY: 0 errors`를 기록했다. 오래된 Windows worktree의 B-0011 `.k3xp` 두 파일은 Git blob과 summary가 정확했지만 CRLF로 materialize되어 digest test가 한 번 실패했고, WSL Git으로 canonical LF blob을 복원한 뒤 전체 matrix가 통과했다.
- B-0023의 wall regression은 aggregate kernel 증가보다 훨씬 크다. Source review에서 complete-layer preflight가 매 호출마다 469,776,384 bytes의 immutable dense/vector weights 전체에 finiteness scan을 수행하는 것을 확인했다. D-048은 이 원인을 별도 attribution하기 전 CUDA Graph/whole-token graph를 선택하지 않고, admission-time validation으로 correctness를 유지한 채 반복 scan을 제거한 후 같은 matrix를 재실행하도록 결정한다.

## 2026-08-10 Milestone 22 최종 리뷰 수정

- 최종 리뷰에서 split oracle backend가 selected backend 측정 중에도 살아 있어 두 resident table이 겹치고 process VRAM을 과소보고하는 문제를 확인했다. Oracle output과 oracle peak만 좁은 scope 밖으로 이동해 selected backend 생성 전에 oracle backend를 파괴하도록 수정했다.
- 회귀 테스트는 새 `oracle_peak_vram_bytes` 계약과 전체 peak의 oracle 포함을 먼저 요구해 RED를 확인했다. 수정 후 focused CUDA 22개 테스트가 통과했으며 B-0023 6행을 다시 측정했다.
- 교정 B-0023의 layer 대비 split median 변화는 1/4/16 experts에서 +1568.624%/+783.911%/+329.883%다. Aggregate digest는 `88c51b6a58340a4325b2b09faa0fb63d1bc5f4439542261383f6070dbe526ade`, summary CSV digest는 `4a95494381c87862aa6933811248f1fd2ff35a28d88e576917da57e50e87d621`다.
- 교정 후 최종 publication gate는 CUDA CTest 26/26, focused live/evidence pytest 22/22, released one-expert Compute Sanitizer 0 errors로 통과했다.

## 2026-08-10 Milestone 22 공개 통합

- 구현 PR #36의 branch/PR correctness `31358991710`/`31359003481`과 CodeQL `31359003436`이 통과했다. PR #36은 `e4820a18`로 rebase merge됐다.
- 공개 `main`의 post-merge correctness `31359158926`과 CodeQL `31359158878`도 통과했다. README와 TITAN Ledger를 PR #36 및 공개 head 기준으로 정합화한다.

## 2026-08-10 Milestone 23 준비

- 공개 head `e0ab5fa`에서 `codex/milestone-twenty-three-admission-validation` worktree를 만들었다. Baseline CUDA CTest 26/26과 released MoE-layer focused pytest 22/22가 통과했다.
- D-048 구현 대안 중 resident-table miss 검사는 부분 CUDA mutation 위험 때문에 거부했고, opaque prepared-layer token은 현재 범위에 비해 public API 변경이 커서 미뤘다. Backend-local registry는 complete host preflight, 기존 caller 유지, O(1) hot identity hit를 함께 만족해 채택했다.

## 2026-08-10 Milestone 23 구현 및 측정

- `admission`은 여섯 immutable view 전체의 identity 충돌과 finiteness를 먼저 확인하고 모두 성공한 뒤에만 registry를 commit한다. CUDA resident acquisition은 그 다음에 시작하므로 마지막 view 실패도 부분 upload/admission을 남기지 않는다.
- identity는 backend lifetime 내 tensor ID, host pointer, byte length, rows, cols로 고정했다. 같은 pointer의 in-place mutation은 탐지하지 못하므로 일반 기본값은 `per-call`로 유지하고 exact resident MoE-layer에서만 명시적으로 opt-in한다.
- profiler-off 행에서 D2H가 0으로 사라지는 초기 B-0024 실패를 실제 로그로 확인했다. D2H physical accounting을 backend runtime counter로 분리한 뒤 profiler on/off가 동일한 traffic을 보고하도록 수정했다.
- 최종 B-0024는 18행 모두 maximum error 0, warm weight H2D 0, bypass/fallback 0을 통과했다. Per-call은 20회에 9,395,527,680 bytes를 scan했고 admission은 cold 469,776,384 bytes 이후 warm scan 0, identity hit 120을 기록했다.
- profiler-off complete-layer median은 1/4/16 experts에서 19.570/20.729/24.519 ms에서 1.247/1.940/5.221 ms로 감소했다. 이는 layer microbenchmark attribution이며 token TPS나 full-model 결과가 아니다.

## 2026-08-10 Milestone 23 최종 리뷰 수정

- Critical/Important-only read-only review는 B-0024 원시 자료, 요약, CSV digest와 산식을 모두 재계산해 일치함을 확인했고 Critical은 찾지 않았다. Important 1건은 target의 `admission` 선택이 CUDA AURORA draft MoE-layer backend로 전달되지 않아 draft가 묵시적으로 `per-call`을 유지하는 CLI ownership 누락이었다.
- 회귀 테스트로 target은 `admission`이지만 draft가 `per-call`인 RED를 먼저 확인했다. CUDA AURORA draft boundary가 `moe-layer`일 때만 target validation mode를 전달하도록 최소 수정했고, 직접 CUDA 실행에서 target/draft 모두 `admission`, target/draft 최초 scan 18/18, identity hit 144/162를 확인했다. 전체 CUDA CTest는 26/26 통과했다.
- 동일 리뷰어의 focused re-review는 이전 Important가 해결됐고 새 Critical/Important가 없다고 판정했다. 코드의 MoE-layer 한정 전달, 회귀의 target/draft mode 및 실제 scan/hit 검증, 원장 설명이 서로 일치함을 확인했다.

## 2026-08-10 Milestone 23 공개 통합

- 최종 head `07cd743`의 push/PR correctness `31363433423`/`31363437230`과 PR CodeQL `31363437226`이 통과했다. PR #38을 public integration head `e24cac2`로 rebase merge했고, post-merge `main` correctness `31363673811`과 CodeQL `31363673857`도 통과했다.
- 공개 결과는 admission validation을 opt-in으로 유지한다. B-0024의 layer attribution을 token TPS나 full-model 품질로 확대 해석하지 않으며, 다음 선택 gate는 ordered routed-set reuse와 bounded CUDA Graph cache의 실제 비용 측정이다.

## 2026-08-10 Milestone 24 준비

- Public `main` `08b769c`에서 `codex/milestone-twenty-four-cuda-graph-cache` worktree를 만들었다. CUDA 13.3.73 native `sm_120` baseline은 CTest 26/26을 통과했다.
- NVIDIA CUDA 13.3 원문은 whole-graph update가 동일 topology와 dependency/sink ordering을 요구하고, individual update는 소수의 known node 변경에 유리하며, graph object가 thread-safe하지 않다고 명시한다. CUDA Samples `b7c5481`의 explicit/capture 구현과 현재 resident MoE-layer의 13 logical kernel, stable resident pointers, grow-only scratch를 대조했다.
- Whole-token graph는 정확성 범위를 과도하게 넓히고, superset node enable은 현재 variable-expert grid와 중복된다. M24는 `disabled|update|cache`를 분리해 direct reference, whole-update 비용, ordered-set bounded-cache reuse를 독립 측정한다. 기본값은 `disabled`이며 graph failure는 direct fallback으로 숨기지 않는다.
- M24 구현 계획은 portable ordered-key/index, strict option/schema, warm graph hit, eviction/update/invalidation, target/draft ownership, B-0025 tooling, formal measurement/ledger의 일곱 review 단위로 고정했다. 각 production 변경은 focused RED를 먼저 확인하고 의미 단위 commit으로 분리한다.
- Task 1 RED는 `cuda_graph_cache.hpp` 부재로 정확히 실패했다. Bounded index는 A/B/A/C에서 B를 퇴거하고 동일 key hit, zero-capacity rejection, erase/clear를 보존한다. 새 test와 기존 backend/ops 선택 게이트가 통과했다.
- Task 2는 `disabled|update|cache`와 entry-capacity 불변식을 CLI 및 CUDA factory 양쪽에 추가했다. non-disabled mode는 FP32, cuda-custom, reused allocation, resident weights, resident-grid, moe-layer, synchronous transfer, fusion none, admission validation, positive resident capacity에서만 허용한다. 실제 graph executor가 생기기 전에는 factory가 fail-closed 한다.
- Target와 draft JSON/CSV에 graph identity와 12개 counter를 독립적으로 추가했다. Released MoE-layer benchmark도 `--graph`, `--graph-entries`와 같은 telemetry를 소유한다. CPU default schema, option rejection, native CUDA compile을 통과했다.
- Task 3 RED는 valid cache mode factory가 아직 `BACKEND_UNAVAILABLE`을 반환하는 exit 40으로 재현했다. 첫 구현은 capture 내부의 같은-stream `cudaEventRecord`가 17개 실행 node 중 event node를 하나도 만들지 않고 event handle도 invalid하게 만든다는 실기 증거를 얻었다.
- 캡처 중 inline event를 생략하고, capture 후 3개 H2D + 13개 연산 + 1개 D2H 선형 graph를 검증한 뒤 각 연산 전후에 명시적 event-record node 26개를 삽입했다. 첫 miss/capture/instantiate와 둘째 hit가 서로 다른 input/contribution에서도 CPU oracle과 일치했고, CUDA CTest 27/27 및 Compute Sanitizer 0 errors를 통과했다.
- Task 4 RED는 update factory 부재로 exit 50, scratch growth invalidation 부재로 exit 63을 각각 확인했다. Cache capacity 1의 A/B/A/B는 4 miss·3 eviction, capacity 2는 2 miss·2 hit이며, 동일 두 expert의 순서 반전은 별도 ordered identity 두 개가 된다.
- Update mode는 매 호출 fresh graph를 capture하고 두 번째부터 `cudaGraphExecUpdate`를 시도한다. 성공 시 기존 executable ownership을 새 graph/staging으로 이동하며, 실패 시 old executable을 버리고 새 graph를 instantiate한다. 모든 layer scratch pointer/capacity identity를 추적해 변화 시 cache/update entry를 폐기한다. CUDA CTest 27/27과 Compute Sanitizer 0 errors를 통과했다.
- Task 5는 target과 persistent CUDA AURORA draft에 서로 독립적인 `disabled|update|cache` 설정과 entry capacity를 전달한다. Draft graph는 resident-grid MoE-layer와 admission validation을 요구하며 target 설정을 암묵적으로 상속하지 않는다. 양방향 실제 CUDA 소유권 테스트 4 passed/2 skipped, CPU 거부 계약 2 passed, benchmark schema 11 passed/6 skipped, CUDA CTest 27/27을 통과했다.
- Task 6 RED는 새 ablation 모듈 import 실패와 released benchmark의 `--trace` 거부로 각각 확인했다. 첫 live smoke는 새 tensor ID가 resident miss를 유발해 warm weight H2D 불변식을 깨는 것을 잡았고, 동일한 네 resident expert의 다섯 ordered permutation으로 graph key만 바꾸도록 수정했다. 15행 warmup 0/iteration 1 smoke와 canonical verifier 12/12가 통과했으며 raw JSON/CSV와 summary CSV는 LF-only digest를 기록한다.
- 최종 B-0025는 3 warmup·20 measured calls의 15행을 기록했다. Stable/alternating delta는 -4.407%에서 +4.466%로 혼합됐고 rotating cache는 6.091%–11.574% 느렸다. 모든 행은 maximum error 0, warm weight H2D 0, fallback/bypass 0, sync 20, logical layer kernel 260을 보존했다.
- Artifact/runner/aggregate/summary CSV digest는 `e087ff78284e99760a7d113cf744562878537a6379e7a63be95585eec8b9f1be`, `60ee26df29b05a9f6638323477abd7c277f22802fe67866bdb4b647ec7f85c21`, `f65ab85eb8b750a69330f10146f50f1644b5205e60c6752abcaf0d1deffa3bd8`, `a53482f9386eaeb292c263f921c165fd8fb931aa156f4ebd2ecee2f74672d2ad`다. Worktree verifier와 staged Git blob 재계산이 raw JSON/CSV 30개, aggregate, LF summary CSV를 모두 확인한다.
- Fresh 검증은 CPU CTest 15/15·pytest 332/70, liburing/direct CTest 16/16·pytest 334/68, ASan/UBSan CTest 16/16, CUDA CTest 27/27·pytest 386/16을 통과했다. Stable cache hit와 alternating capacity-one eviction Compute Sanitizer는 모두 `ERROR SUMMARY: 0 errors`였다.
- 최종 자체 리뷰에서 `cuda_graph_host_nanoseconds`가 설계와 달리 stream wait를 포함한 Important를 발견했다. Wait 전후 host 구간만 합산하도록 교정했고 stable 20-call smoke에서 32.1 ms 수준이 7.7 ms로 분리됐다. 모델 출력과 wall latency 정의는 바뀌지 않았지만 runner digest와 raw evidence는 폐기 후 전체 재측정했다.
- Host-time 교정 후 CUDA CTest 27/27과 pytest 386 passed/16 skipped를 다시 통과했고 stable hit 및 capacity-one eviction Compute Sanitizer가 모두 0 errors를 보고했다. Focused final review는 `3292524..27feced`에서 Critical/Important를 발견하지 않았고 raw JSON/CSV 30개, aggregate, summary와 문서 digest parity를 확인했다.
- Review head `27feced`를 공개 branch에 push하고 ready PR #40을 열었다. Merge 및 pull-request/post-merge correctness/CodeQL은 아직 완료로 기록하지 않는다.
- 문서 동기화 head의 branch/pull-request correctness `31371133295`/`31371136825`와 pull-request CodeQL `31371136804`가 통과했다. PR #40은 public integration head `13a403f`로 rebase merge됐고 post-merge `main` correctness `31371387067`과 CodeQL `31371387081`도 통과했다.

## 2026-08-10 Milestone 25 준비

- Public documentation head `aa0f935`에서 `codex/milestone-twenty-five-converter-integrity` 브랜치를 시작했다. 사용자의 Cloud Run 전 자율 진행 승인을 설계 gate에 적용하되 비용·공개 format 변경·실제 weight 다운로드는 범위 밖으로 유지한다.
- `PROJECT_STATE.md`의 converter hash/ledger gap은 현재 코드와 D-028 증거에 비해 stale했다. Current writer는 bounded fixture의 declared shard/tensor SHA-256, canonical extent prefix, source CRC, partial CRC를 이미 검증한다. M25는 이를 중복 구현하지 않고 generic external manifest containment, exact tensor-to-shard ownership, strict safetensors/ledger schema, uncommitted partial suffix recovery를 다룬다.
- 세 접근을 비교했다. D-028만 반복하는 no-op은 실제 진전이 없어 기각했고, signed manifest v2는 M26 real checkpoint discovery보다 앞서 형식을 고정하므로 보류했다. 기존 K3X v1과 source format을 유지하는 narrow trust-boundary hardening을 선택했다.
- M25 구현 계획은 source containment/ownership, safetensors metadata, resume schema, committed-prefix recovery, audit/ledger/publication의 다섯 독립 TDD 단위로 고정했다. 사용자의 자율 진행 지침에 따라 inline executing-plans 경로를 사용하며 각 production 변경 전에 실제 RED를 목격한다.
- 공식 safetensors 형식과 구현을 다시 대조해 header DoS 경계를 정정했다. M25 reader는 payload나 declared header를 할당하기 전에 upstream 기본값인 100 MB 초과를 거부하며, 이는 K3 shard 측정에 기반한 임의 제한이 아니라 외부 입력 안전 경계다.
- Resume crash 경계를 실제 synthetic stop 1~8로 측정했다. stop 2의 정상 partial은 20,736 bytes이고 다음 aligned boundary는 24,576 bytes여서, 정렬 끝을 committed로 간주하면 정상 중단을 손상으로 오판한다. Ledger가 CRC로 보증하는 마지막 extent의 정확한 `offset + length`만 committed boundary로 채택하고 다음 padding은 재개 시 다시 생성한다.

## 2026-08-10 Milestone 25 구현과 검증

- Source containment/ownership은 public commits `3947b87`와 non-standard constant 보정 `6f852bc`, safetensors header/metadata는 `9aa6882`와 `36ceded`, resume schema는 `d91d8b7`, exact orphan-suffix recovery는 `e4c5fd9`로 각각 RED/GREEN 구현했다. D-028의 기존 bounded-fixture hash 검증은 중복하지 않았다.
- B-0026 runner와 verifier는 `98146bf`에 추가했고, summary JSON도 LF-only final-newline으로 검증하도록 `35e4419`에서 보정했다. Canonical evidence는 `cce5223`에 기록했다.
- Canonical B-0026은 fresh/resume-clean/resume-orphan 세 시나리오에서 maximum source read 257 bytes와 final output 1,421,568 bytes를 기록했다. Resume 두 행은 20,736-byte committed prefix의 extent 2개를 재사용했고 orphan 행은 8,192-byte suffix를 검증 뒤 제거했다. RSS는 측정하지 않았고 token/GPU/NVMe/quality 필드는 내보내지 않았다.
- 첫 canonical direct-module 실행은 shared venv의 editable `k3x_converter`가 오래된 milestone-one worktree를 가리켜 final output size가 달랐다. 결과 파일은 남지 않았다. Repository `pyproject.toml`의 pytest path와 일치하도록 direct tool invocation에 `PYTHONPATH=converter:reference`를 명시해 현재 worktree 코드를 사용했고, shared venv 자체는 변경하지 않았다.
- Fresh full gates는 CPU CTest 15/15와 pytest 405 passed/70 skipped, liburing/direct CTest 16/16와 capability-aware `K3X_TEST_IO_URING=1 K3X_BUILD_DIR=build-liburing` pytest 407 passed/68 skipped, ASan/UBSan CTest 16/16, CUDA CTest 27/27와 pytest 459 passed/16 skipped를 통과했다. Released MoE-layer Compute Sanitizer regression은 `ERROR SUMMARY: 0 errors`였다.
- B-0026은 converter correctness/recovery audit이다. 공식 weight, publisher authenticity, peak RSS, token throughput, GPU 성능, physical NVMe, cloud 실행은 아직 증명하지 않는다.

## 2026-08-10 Milestone 25 공개 통합

- Final Critical/Important review에서 설계 문서 한 줄의 stale `align_up` 표현을 확인했다. 구현과 계획이 사용하는 exact `last.offset + last.length`로 `ca8c544e`에서 정정했고 focused resume/audit 회귀 40개와 scoped re-review를 통과했다.
- Public ready PR #42의 push/pull-request correctness `31379029215`/`31379074639`와 CodeQL `31379074656`이 통과했다. PR은 public integration head `ca8c544e`로 rebase merge됐다.
- Post-merge `main` correctness `31379311743`과 CodeQL `31379311695`도 통과했다. GitHub Actions의 Node.js 20 및 CodeQL v3 deprecation annotation은 성공 결론을 바꾸지 않으며 별도 maintenance 대상이다.

## 2026-08-10 Milestone 26 공식 source 조사

- Official Hugging Face `moonshotai/Kimi-K3` main은 commit `9f62e4e9fffbd0a83ddd60e1c209d828994b3569`로 resolve됐다. Public/ungated API는 118 files와 1,560,998,984,390 repository bytes를 보고했다.
- `model.safetensors.index.json`은 59,764,096 bytes, LFS SHA-256 `a1c5210650ce71d2d3ae9ec5a101ac4afd3cf4b10091be589853437eb967febd`다. Index는 497,220 tensors, 96 shards, total tensor bytes 1,560,860,324,864를 보고했다.
- Layer 1 expert 0의 w1/w2/w3 packed/scale 여섯 tensor는 shard 2에 함께 있다. Shard 2는 16,990,911,504 bytes, LFS SHA-256 `26a3284e1d2cb567934ebef002e6a1813551d646739e8bcb1e9e3fe7f878e0f5`다.
- 실제 8-byte HTTP Range는 206과 exact total을 반환했고 header length는 818,696 bytes였다. Header-only range에서 여섯 tensor가 하나의 17,547,264-byte contiguous payload union을 이룸을 확인했다. Tensor payload는 아직 다운로드하지 않았다.
- Official `modeling_kimi_linear.py`는 w1=gate, w2=down, w3=up을 명시한다. 기존 K3X released storage fixture의 gate/down/up 크기와 exact payload bytes가 일치한다.
- API-only는 tensor placement가 없어 기각했고 full 16.99 GB shard-first는 첫 real evidence가 느려 보류했다. Pinned index + header + exact expert range를 채택하되 provenance를 `transport-pinned-range`로 제한하고 full-shard-verified라고 주장하지 않는다.
- M26 live output은 non-executable storage fixture이며 real bytes와 K3X artifact를 Git에 commit하지 않는다. No-payload dry-run이 기본이고 live payload는 explicit capped mode에서만 허용한다.
## 2026-08-10 — Milestone 26 implementation boundary

- The accepted implementation plan is `docs/superpowers/plans/2026-08-10-k3x-official-range-discovery.md`.
- `config.json` is bound to its API-declared Git blob ID by recomputing the canonical Git blob SHA-1 before released-dimension validation.
- Implementation order is bounded transport and snapshot identity, strict index/config/header planning, atomic one-expert materialization, then the sole opt-in live B-0027 payload request.
- The live artifact remains untracked and non-executable; M27 owns the first real-weight CUDA layer execution.

## 2026-08-10 — Milestone 26 local RED/GREEN implementation

- Bounded HTTPS and snapshot discovery passed 15 focused tests; source/safetensors regressions passed 43 tests.
- Shared safetensors metadata parsing plus strict index/config/header/expert planning passed 109 focused and regression tests.
- Atomic released-size materialization, local content hashing, source-manifest provenance, K3X conversion, cleanup, and stale-artifact binding passed 50 Python storage/K3X tests.
- The attempted C++ reader regression identified only a missing `build/test_reader` binary in this fresh worktree; that binary must be built before the final cross-language gate.

## 2026-08-10 — B-0027 live official expert evidence

- Live dry-run resolved 118 files and 1,560,998,984,390 repository bytes, verified the 59,764,096-byte index and 818,704-byte header, and downloaded zero tensor payload bytes.
- The sole live materialization downloaded exact range `[1,268,562,960, 1,286,110,224)` and produced payload/microshard/K3X-root SHA-256 `1d925f…c36c`, `ed3f07…4a34`, and `d585d2…58be`.
- Strict B-0027 verification passed. Real bytes remain ignored below `artifacts/`; only summary JSON/CSV are tracked.
- Fresh gates pass CPU 15/15 + 451/70, liburing 16/16 + 453/68, ASan/UBSan 16/16, CUDA 27/27 + 511/10, and Compute Sanitizer zero errors. The real artifact directly returns exit 4 and `NON_EXECUTABLE_ARTIFACT`.

## 2026-08-10 — Milestone 26 final trust-boundary review

- Review found that `PurePosixPath` normalized repeated and dot path segments before validation. Witnessed RED cases reached shard-set comparison instead of failing as invalid index input; `ef37b96` validates original slash-separated segments first and the affected parser suite passes 71 tests.
- Review also showed that mutually consistent JSON/CSV rehashing could alter deterministic repository, snapshot, config, index, expert-layout, or artifact identities because the strict verifier checked only a subset. Nine witnessed RED mutations now fail against independent B-0027 constants in `fb7fb49`; the committed evidence still verifies.
- Fresh final-review gates pass CPU CTest 15/15 with pytest 462 passed/70 skipped, liburing/direct CTest 16/16 with pytest 464 passed/68 skipped, ASan/UBSan CTest 16/16, and CUDA CTest 27/27 with pytest 516 passed/16 skipped. No C++ or CUDA production path changed after the existing zero-error Compute Sanitizer run.

## 2026-08-10 — Milestone 26 public integration

- Public PR #44 passed both branch and pull-request correctness plus all CodeQL checks and was rebase-merged at implementation head `5b6345db`.
- Post-merge `main` correctness `31386873905` and CodeQL `31386873928` succeeded. Node.js 20 and CodeQL Action v3 deprecation annotations remain workflow maintenance rather than correctness failures.

## 2026-08-10 — Milestone 27 official expert CUDA boundary

- The existing released single-expert executable was run once against the ignored B-0027 artifact as read-only exploration. It completed on RTX 5080 with 7,212,040 ns wall, 1,962,624 ns kernel, 17,547,264 weight-H2D bytes, and `3.0267983675e-9` maximum CPU-oracle error. This is feasibility evidence only, not B-0028.
- Reusing the released schema cannot prove official identity, while adding caller-selected official labels would mix evidence contracts. M27 therefore uses a dedicated pinned official-expert harness and leaves `k3x_run` unchanged.
- M27 proves only the exact layer-1 expert-0 FFN. Real routed/shared/trunk dependency closure remains M28, preventing a synthetic-dense plus real-expert mixture from being described as a real full layer.
- The implementation plan fixes four reviewable units: pure pinned identity, dedicated CUDA harness, strict two-row B-0028 evidence, and measured/public integration. It binds the ignored K3X file SHA-256 `e08293cd854ed11913bd8f1bc3a51d1eb577202fd5fd9b5b7e3c96ef1bccecc7` in addition to the root and ordered payload digests.
- Task 1 witnessed RED at CMake generation because the declared `official_expert.cpp` did not exist. The minimal pure identity implementation then passed the new field-mutation contract and the complete CPU CTest suite 16/16 without adding I/O, allocation, environment, or CUDA behavior.

## 2026-08-11 — Milestone 27 dedicated official expert CUDA harness

- Task 2 witnessed RED at the missing dedicated source, then added a benchmark-only executable that verifies the pinned K3X root, ordered expert digest, optional features, layer/expert IDs, payload bytes, and shapes before constructing any backend.
- The first real-artifact GREEN attempt passed identity, CUDA execution, CPU-oracle parity, and weight-H2D invariants but reported zero runtime D2H bytes. Root-cause tracing showed that `mxfp4_situ_mlp_group_impl` recorded D2H in the profiler while omitting the corresponding `BackendRuntimeStats` increment used by the new canonical schema.
- A CUDA FFN runtime-counter regression witnessed exit 58 before the one-line backend correction. After the correction, the focused CUDA test and all six official-harness Python cases passed. Direct transient/resident calls both reported `3.0267983675e-9` maximum absolute error; transient measured 17,547,264 weight-H2D bytes and resident measured zero after cold admission.
- The complete CUDA CTest suite passes 28/28. The actual resident official-expert invocation under CUDA 13.3 Compute Sanitizer reports `ERROR SUMMARY: 0 errors`. These direct timings remain Task 2 validation, not formal B-0028 evidence.

## 2026-08-11 — Milestone 27 B-0028 evidence tooling

- Task 3 witnessed the planned import RED before `tools.ablate_official_expert_cuda` existed. The implementation runs exactly transient then resident, forwards only model/mode/warmup/iterations, and emits compact sorted LF raw JSON plus one LF-only summary CSV.
- The verifier independently enforces the complete harness schema, fixed official identities, mode-specific weight-H2D and residency formulas, activation/D2H and synchronization formulas, finite `1e-6` parity, raw digests, canonical aggregate, CSV digest/parity, and forbidden metric absence.
- `strict_official=False` skips only the fixed artifact SHA and 3/20 measurement gate. Strict mode additionally requires actual artifact and runner paths so their bytes are rehashed rather than trusting summary strings.
- Ten controlled runner and mutation tests passed before formal measurement. Final review added an eleventh committed-evidence regression that fixes the official artifact, runner, raw, aggregate, and CSV digests in CI. The Task 2 actual-artifact suite remains 6/6. The tool reads and hashes the artifact but never copies it or emits token, quality, or physical-NVMe measurements.

## 2026-08-11 — B-0028 formal measurement and M28 boundary

- The exact artifact SHA-256 `e08293…cc7` and native `sm_120` runner SHA-256 `48f0f2…1fa` were verified before the first and only formal 3-warmup/20-iteration run. No preferred-timing rerun occurred.
- Transient/resident medians are 2,508,377/331,868 ns. Both modes preserve `3.0267983675e-9` maximum CPU-oracle error. Transient transfers exactly 350,945,280 measured weight bytes; resident transfers zero after identical 17,547,264-byte cold admission and records 60 measured hits.
- Strict verification and a separate read-only cross-check validate all raw/summary/CSV payloads, formulas, aggregate, LF bytes, staged Git blob SHA-256 values, and forbidden-field absence. The initial attempted WSL Git blob pipeline was discarded because the Windows worktree pointer was invalid under WSL and produced empty-input hashes; Windows Git subprocess bytes were then hashed directly and matched all four files.
- Fresh gates after the committed-evidence regression pass CPU CTest 16/16 plus pytest 473/76, liburing/direct CTest 17/17 plus pytest 475/74, ASan/UBSan CTest 17/17, CUDA CTest 28/28 plus pytest 531/18, actual-artifact focused pytest 6/6, committed B-0028 pytest 11/11, and resident Compute Sanitizer with zero errors.
- D-054 selects a dependency-closed real MoE FFN sublayer for M28: real router and all 896 scores, natural Top-16, exact selected routed experts, real shared expert, mixing, residual behavior, and independent parity. No complete shard, full checkpoint, paid cloud, token, or full-layer claim is authorized by B-0028.
- Final Critical/Important review found one evidence-regression gap: temporary-output tests did not read the committed B-0028 files. The fixed-digest committed-evidence test was added, all three Python matrices were rerun, and the final scoped review found no remaining Critical or Important issue. Production defaults, `k3x_run`, model graph, converter, K3X format, reference graph, and tracked real artifacts remain unchanged.

## 2026-08-11 — Milestone 27 public integration

- Branch correctness `31455570571`, pull-request correctness `31455597581`, and pull-request CodeQL `31455597565` passed. PR #46 was rebase-merged at public integration head `ec08b827`.
- Post-merge `main` correctness `31455776634` and CodeQL `31455776673` both passed. M27 is publicly integrated; M28 now owns the dependency-closed real router-selected Top-16 plus shared-expert FFN boundary.

## 2026-08-11 — Milestone 28 official MoE FFN design and plan

- Public documentation PR #47 was rebase-merged at `01428f55`. Its push correctness `31457148284`, pull-request correctness `31457166189`, pull-request CodeQL `31457166199`, post-merge correctness `31457415104`, and post-merge CodeQL `31457415100` all passed.
- The official implementation, config, model index, and shard-2 header were inspected at pinned revision `9f62e4e9fffbd0a83ddd60e1c209d828994b3569` without downloading tensor payloads. Layer 1 requires eleven always-active BF16 tensors totaling 379,900,416 bytes plus 17,547,264 bytes per selected native-MXFP4 expert.
- M28 accepts one dependency-closed non-executable K3X fixture. Splitting trunk and expert artifacts would expand multi-reader identity and transaction scope, while authentic layer-1 activations require earlier KDA/attention dependencies and are deferred.
- Two deterministic FP32 prefix/block-residual inputs derive natural independent Top-16 routes. M28 performs no preferred-expert search, substitution, pruning, proxy, or adaptive Top-K behavior.
- K3X v1 retains version and record sizes but gains native BF16 dtype 3 plus required feature bit 0. The official fixture also sets the existing storage-fixture optional bit, so production `k3x_run` remains fail-closed.
- The detailed plan is `docs/superpowers/plans/2026-08-11-k3x-official-moe-ffn.md`. It separates BF16 format, bounded materialization, portable oracle, CUDA boundary, pinned harness, evidence tooling, and the sole formal B-0029 run into seven semantic TDD units.
- User authorization permits autonomous non-billable work through the pre-Cloud-Run milestones. Full checkpoint/shard download and paid Cloud Run provisioning remain outside this milestone.

## 2026-08-11 — Milestone 28 Task 1 native BF16 K3X contract

- Python RED failed at import because BF16 feature constants did not exist. The first C++ RED failed at compilation because the matching required/optional feature constants were absent. Separate negative RED tests then proved that malformed BF16 byte lengths and BF16 payloads disguised as MXFP4 were accepted before the focused fixes.
- K3X v1 now accepts dtype 3 only as exact two-byte BF16 with quantization NONE, no auxiliary extent, and required feature bit 0. Python and C++ Readers require the feature bit and observed BF16 tensor set to agree and continue to reject all unknown required bits at the superblock.
- The official source format sets both `OPTIONAL_STORAGE_FIXTURE` and `OPTIONAL_OFFICIAL_MOE_FIXTURE`, preserving the existing production non-executable guard. Existing source formats, record sizes, and K3X version remain unchanged.
- Focused Python/reader coverage passed 42 tests with 4 environment skips, CPU CTest passed 16/16, and converter/resume/safetensors/storage regressions passed 82 tests. `git diff --check` passed.

## 2026-08-11 — Milestone 28 Task 2 bounded planner foundation

- Official config binding now includes shared-expert count, latent normalization, RMS epsilon, sigmoid routing, renormalization, single expert group/top-k group, and routed scaling. The bool field required a dedicated comparison branch because Python bools are integer subclasses but the numeric validator intentionally excludes them.
- The pure planner validates the exact eleven layer-1 always-active tensors and 379,900,416-byte total. Deterministic cases A/B are serialized as little-endian FP32 digests, while Attention Residual, postnorm, sigmoid routing, correction-only selection, canonical ordering, and contribution renormalization have an independent literal tiny oracle.
- The bounded object store splits every request at no more than 8 MiB, persists a prefix digest after each fsynced chunk, rejects a damaged partial, resumes a valid partial, and reuses a content-addressed completed blob without another request.
- The source assembler writes a hashed safetensors-compatible shard and official fixture manifest. A RED/GREEN test caught an unreachable physical-order branch; the final K3X extents now follow declared first-use order rather than sorted tensor names.
- The official-source focused suite currently passes 53 tests. Full expert-union orchestration, CLI integration, and the actual ignored fixture remain in progress and are not claimed implemented.

## 2026-08-11 — Milestone 28 natural route derivation

- `--scope moe-ffn` now exposes a zero-payload dry-run record with the exact eleven always-active tensors, 379,900,416 always-active bytes, and 941,412,864-byte two-route upper bound while preserving the existing expert CLI as the default.
- Exact BF16 object files for Attention Residual norms/projection, postnorm, and the 896×7,168 router plus the FP32 correction bias are rehashed before mapping. The derived A/B natural Top-16 sets, canonical contribution ordering, and first-use union match an independent PyTorch implementation of the pinned official formulas.
- The route derivation fixture is about 13 MB and does not download official payloads. Full always-active plus selected-expert materialization remains the next Task 2 unit.

## 2026-08-11 — Milestone 28 two-phase materializer completion

- The source-order RED failed at import because `build_official_moe_source_tensors` did not exist. GREEN fixes first-use union ordering to case A then unseen case B experts, and packs every selected expert as gate, up, down before the remaining routed/shared BF16 tensors.
- The orchestration RED proved that no full materializer existed. Commit `0b0c944` now materializes all always-active objects, publishes the deterministic route manifest before expert planning, fetches only the exact selected union, assembles the official source manifest, converts one non-executable K3X artifact, and reopens it to verify both optional fixture bits.
- The CLI abbreviation RED exposed that argparse treated `--materialize` as the legacy `--materialize-expert`. `allow_abbrev=False` now keeps both external contracts distinct. `--scope moe-ffn` remains dry-run unless the exact `--materialize` flag and output directory are supplied.
- A separate telemetry RED added actual response-byte accounting to content objects. Fresh and damaged-partial restart report downloaded bytes, while completed-object reuse and a complete verified partial report zero. CLI traffic now separates actual `tensor_payload_bytes` from logical `source_object_bytes`.
- Focused materializer/CLI coverage passes 27 tests. The combined official-source, transport, safetensors, converter-resume, and source-manifest regression matrix passes 149 tests in 13.24 seconds. Compile validation and `git diff --check` pass. No official M28 payload, B-0029, CPU/CUDA result, token metric, or quality metric exists yet.

## 2026-08-11 — Milestone 28 portable official MoE oracle

- CMake RED failed because the declared `runtime/src/official_moe.cpp` did not exist. The minimal implementation introduces raw BF16 word views and explicit round-to-nearest-even boundaries without changing `ModelSession` or backend dispatch.
- The tiny graph fixes literal Attention Residual, router, routed projections, two native-MXFP4 experts, shared BF16 expert, and final residual values. Natural route derivation is separate from execution so later CUDA consumes the same validated IDs and contributions.
- Python parity RED failed because the C++ test emitted no boundary record. The `--dump` test mode now emits only the already-validated tiny boundaries; PyTorch independently recomputes all values and matches at `1e-6`.
- Malformed route count, duplicate IDs, non-finite or non-unit contribution mass, duplicate/missing expert payloads, malformed router dimensions, and invalid MXFP4 views fail before `OfficialMoeResult` publication.
- Commit `8a13cf5` passes CPU CTest 17/17 and full `test_cpp_parity.py` 113 passed/32 skipped. No network, filesystem, CUDA, global state, official payload, B-0029, token, or quality measurement is involved.

## 2026-08-11 — Milestone 28 native BF16 CUDA boundary

- Initial CUDA RED failed because `OfficialMoeFfnView` and `official_mxfp4_moe_ffn` did not exist. The minimal boundary keeps Task 3 Attention Residual/postnorm/router on CPU and consumes only the prepared hidden vector, prefix residual, canonical route, raw BF16 tensors, and native MXFP4 experts.
- Both transient and resident paths use byte-native BF16/MXFP4 storage. The resident second call adds zero weight H2D and increases exact tensor hits; one final output vector is the only D2H result.
- Self-review found that the CPU oracle rounds the prefix residual before final addition. The fixture now uses non-BF16-exact prefix values and CUDA explicitly rounds the device prefix before addition.
- Commit `bb634e1` passes CPU CTest 17/17, CUDA CTest 30/30, focused transient/resident parity, and Compute Sanitizer with zero errors. This is a tiny correctness/traffic gate only; official payload, B-0029, token rate, quality, and physical traffic remain unmeasured.

## 2026-08-11 — Milestone 28 pinned official MoE harness

- The pre-fetch route manifest could not bind a root that does not exist yet. Commit `a109409` keeps early publication intact and atomically adds the final K3X root/source/tensor digests only after conversion and Reader verification.
- Harness RED was the absent CMake target. Commit `bdab0da` adds strict duplicate-key/non-finite JSON rejection, pinned source/input identity, exact Reader root/tensor order/shape/dtype checks, both route recomputations, portable CPU oracles, and A/B/alternating CUDA execution with internal traffic formulas.
- The official CUDA boundary now records kernel-only CUDA-event time; the focused test requires nonzero profiler device time. CUDA CTest remains 30/30 and Compute Sanitizer reports zero errors.
- Harness pytest passes 18 with 3 skips. Those skips are the actual A transient, B transient, and alternating resident smoke cases; they remain pending because no M28 payload has been materialized.

## 2026-08-11 — Milestone 28 B-0029 evidence tooling

- Runner RED was the missing `tools.ablate_official_moe` module. The fixed matrix is A transient, A resident, and alternating resident; it does not rerun or rank timing samples.
- Strict verification fixes 3/20, rehashes artifact/manifest/runner/raw/aggregate/CSV, and rejects TPS, quality, physical-NVMe, identity, route, contribution, parity, traffic, finite, digest, order, and CSV mutations.
- Controlled runner plus harness coverage passes 28 with the same 3 real-fixture skips. Commit `ba3a0d2` contains tooling only; no B-0029 measurement exists yet.

## 2026-08-11 — Milestone 28 bounded official materialization and B-0029

- The default dry-run returned zero tensor payload and fixed the plan at eleven always-active tensors, 379,900,416 bytes, and a 941,412,864-byte two-case upper bound. Explicit authorized materialization selected two disjoint natural Top-16 routes, therefore a 32-expert union, and transferred exactly 941,412,864 tensor-payload bytes without downloading a complete shard or checkpoint.
- The first materialization used 150 bounded materialization requests and took 364.7146 seconds. A later verified reuse run reused 43 objects, made zero materialization requests, transferred zero tensor-payload bytes, and took 79.1528 seconds. Complete conversion intentionally chooses a new random file UUID, so the K3X root changed while source digest `d9e4425a11ca71b53abce52b8f120bd257740fc93cbe63df4c1fc3b7465cee35` remained stable. The current ignored artifact root is `1287d84bbfa02e849ab786808107fbfbfe14459477bf79e3048b2ebb6bdff288`.
- `k3x_run` rejects the bounded fixture with exact `NON_EXECUTABLE_ARTIFACT`. All three actual A/B/alternating smoke cases pass. The actual alternating resident Compute Sanitizer reports zero errors, zero warm weight H2D, 928,521,216 resident bytes, and `0.00048828125` maximum absolute error.
- Two formal attempts failed closed before publishing any files. The first exposed exact contribution equality where the accepted contract was `1e-6`; `7bfd152` corrected it. The second exposed a transient allocation formula that omitted 64 per-call temporaries; `1afda75` corrected it to 102 allocations per call. The subsequent strict 3-warmup/20-iteration run is the sole published B-0029 matrix at `bf147fa`; it was not rerun to select timing.
- B-0029 measures A transient/resident medians of 97,095,781/10,153,939 ns and an alternating resident A+B sequence median of 20,201,466 ns. Both resident rows have zero measured warm weight H2D. The resident A working set is 647,764,992 bytes, the two-route union is 928,521,216 bytes, and alternating parity error is `0.00048828125`. No token, quality, physical PCIe/NVMe, utilization, bandwidth, or complete-layer result is claimed.
- A first full CPU matrix exposed that the BF16 Python fixture helper omitted source/tensor integrity fields. Commit `bdfc0b6` added `source_sha256`, `tensor_sha256`, and canonical `tensor_order`. Fresh final verification passes CPU CTest 17/17 plus Python 507/97, liburing CTest 18/18 plus Python 509/95, ASan/UBSan CTest 18/18, CUDA CTest 30/30 plus actual-artifact Python 592/12, and actual alternating resident Compute Sanitizer with zero errors.
- D-060 selects one complete official transformer layer as M29's next boundary. KDA/MLA/Attention Residual must surround the validated FFN before kernel tuning or end-to-end token claims.

## 2026-08-11 — Milestone 28 public integration

- Public PR #48 carried 47 files and the complete native-BF16, bounded manufacturing, portable/CUDA MoE FFN, harness, B-0029 evidence, and synchronized ledger lineage. The ignored 941 MB fixture was not tracked.
- Branch correctness `31465297042`, pull-request correctness `31465320780`, and pull-request CodeQL `31465320778` passed. PR #48 was marked ready only after those checks and rebase-merged at `eb2c20860ee9c7c612b9b74984170bd8b4443ba1`.
- Post-merge `main` correctness `31465590414` and CodeQL `31465590416` passed. M28 is publicly complete; the next session starts from M29's bounded complete-layer design boundary rather than rediscovering M28.

## 2026-08-11 — Milestone 29 official KDA layer design

- The pinned 51,506-byte `modeling_kimi_linear.py` recomputes to Git blob `b8c41e8bfce768d74d8da3a37e693f5ee43876a0`, matching repository metadata. Header-only inspection of `model-00002-of-000096.safetensors` identifies exactly 17 new layer-1 KDA/self-Attention-Residual tensors and 887,843,840 payload bytes; no tensor payload or complete shard was downloaded.
- The checkpoint stores F32 `A_log[128]`, while the pinned Python constructor initializes `[num_heads]=[96]`. The KDA paper defines channel-wise decay in key dimension 128. M29 therefore treats the checkpoint header plus paper as authoritative and adds a fail-closed `[128]` contract instead of silently copying the constructor mismatch.
- The accepted boundary supplies deterministic layer-1 hidden/source-bank vectors and zero initial KDA state, then executes tokens A and B both together and incrementally. Including embeddings/layer 0 is rejected for this milestone; KDA-only execution is an implementation gate but not completion.
- The final natural expert union is derived after KDA and is not required to match M28. Existing content-addressed M28 objects may be reused only after rehashing, and only missing selected experts are fetched.
- The complete-layer unaligned payload is `1,267,744,256 + 17,547,264 * U` bytes for natural union size `U`, bounded by 1,829,256,704 bytes at `U=32`. B-0030 remains an isolated layer benchmark and must not emit or imply token throughput.
- Design commit `5f04768` synchronizes the accepted specification, README, architecture, performance model, decisions, benchmark boundary, checklist, notes, and project state. The detailed eight-task TDD plan is `docs/superpowers/plans/2026-08-11-k3x-official-kda-layer.md`.

## 2026-08-11 — Milestone 29 Task 1 metadata planner

- RED failed at import because `official_layer.py` did not exist. GREEN adds strict KDA fields to the pinned config record and validates the exact 17-tensor execution-order plan plus the existing M28 MoE plan before payload access.
- The first GREEN exposed the official `full_attn_layers` terminal `93` and existing test fixtures that did not mirror the full current config. The real pinned config and complete test fixtures now agree. A separate malformed-list RED proved an integer schema leaked `TypeError`; the parser now returns stable `OFFICIAL_CONFIG_MISMATCH`.
- Live metadata-only planning returns 17 tensors, 887,843,840 KDA bytes, 1,267,744,256 base bytes, and 1,829,256,704 maximum bytes with the pinned source blob. No tensor payload, complete shard, checkpoint, or paid resource was used.
- Official source/MoE/layer/CLI pytest passes 64 tests, compile validation passes, and `git diff --check` passes. No KDA recurrence, state parity, route, CUDA, B-0030, token, quality, or performance result exists yet.

## 2026-08-11 — Milestone 29 Task 2 scalar KDA oracle

- RED failed at the planned missing `k3x_ref.official_kda` module. The first GREEN run then exposed a test-fixture-only cardinality error: `[1,2,2,2]` needs eight values, not sixteen. Correcting that literal made the intended recurrence test runnable without changing production semantics.
- The independent oracle fixes BF16 projection/convolution boundaries, F32 channel-wise `A_log[head_dim]` decay, scalar-per-head beta, FP32 key-by-value recurrence, and explicit V-first state publication. It does not call converter or runtime dispatch.
- Full two-token execution matches incremental A-then-B exactly for BF16 output and convolution histories and within `1e-6` for FP32 recurrent state. The nonzero-state test independently reconstructs decay, delta, update, and output and confirms the input state is not mutated.
- Focused official/synthetic KDA and model coverage passes 17 tests; the combined Task 1/2 regression passes 81 tests. Dtype, shape, non-finite, convolution-history width, empty sequence, and state-layout drift fail closed. No official payload, CUDA execution, B-0030, token, quality, or performance result was produced.

## 2026-08-11 — Milestone 29 Task 3 bounded manufacturing

- RED failed on the missing layer route/materializer interfaces. The implementation reuses M28 range objects, resume/restart, source assembly, expert planning, conversion, and Reader verification rather than introducing a second transport ledger.
- Manufacturing fixes 17 KDA objects followed by 11 MoE trunk objects, derives fixed A/B full/incremental state-linked natural routes, atomically publishes `route-state-manifest.json`, then requests only the selected first-use expert union. Final packing preserves the 17-item KDA execution order and existing M28 MoE order.
- Self-review expanded parity from final output/state to every KDA boundary and stopped mmap creation for unused route-time MoE execution tensors. `official_layer` source metadata distinguishes the complete-layer fixture while existing optional bits preserve `NON_EXECUTABLE_ARTIFACT` behavior.
- A final trust-boundary review added complete-plan and pinned snapshot/source revalidation inside the materializer itself, so a direct caller cannot bypass the CLI by mutating a planned byte field. Layer/MoE/CLI/source/resume/source-integrity coverage passes 123 tests. The live pinned dry-run returned the exact accepted byte bounds and zero tensor-payload bytes in 12.867 seconds. No M29 payload, route union, artifact root, CUDA, B-0030, token, quality, or paid resource exists yet.

## 2026-08-11 — Milestone 29 Task 4 portable C++ KDA

- CMake RED failed at the deliberately missing `runtime/src/official_kda.cpp`. GREEN adds a standalone native-BF16/F32 C++ boundary with no Reader, filesystem, CUDA, dispatch, or global-state dependency.
- The C++ test fixes the same literal tensors as the PyTorch oracle, requires full/incremental equality for every boundary and final state, confirms input-state immutability, and emits a deterministic JSON dump. Python independently reconstructs all values and matches within `1e-6` with exact BF16 output/state words.
- A first combined verification command used a `|` regex that PowerShell/WSL interpreted as a shell pipe, so that CTest observation was discarded. Re-running with the plain `official_` regex passes 3/3. The complete CPU CTest suite passes 18/18, Python C++ parity passes 114/32, and strict warning compilation passes.
- Self-review added checked output products and rejects native or derived non-finite values before publishing a result. No official payload, complete layer, CUDA, B-0030, token, quality, or performance result exists yet.

## 2026-08-11 — Milestone 29 Task 5A portable complete-layer composition

- The RED build failed on the deliberately absent `runtime/src/official_layer.cpp`. GREEN composes the existing portable KDA and M28 MoE authorities without adding a Reader, filesystem dependency, CUDA dispatch, or global state.
- The boundary keeps both Attention Residual operations explicit, independently reconstructs the MLP residual and normalization, and requires exact equality with `prepare_official_moe_input` before natural routing and expert execution. Its owned result exposes every upper graph boundary, complete MoE result, and final V-first KDA state.
- The two-token tiny fixture changes routes from `[0,1]` to `[1,0]`. Full and incremental execution agree at every step and state, and an independent PyTorch graph reconstructs Attention Residual, KDA, native MXFP4 experts, routing, MoE accumulation, and final outputs within `1e-6`.
- Strict warning compilation initially found one unused test-only identity fixture. Removing that fixture was the only warning fix. Full CPU CTest passes 19/19, focused official CTest passes 4/4, and Python C++ parity passes 115/32. Pinned artifact/manifest preflight remains the unfinished Task 5 substage; no official payload, CUDA, B-0030, token, quality, or performance result exists yet.

## 2026-08-11 — Milestone 29 Task 5B pinned preflight

- RED failed because `k3x_cuda_official_layer_bench` did not exist. GREEN adds a strict duplicate-key/non-finite-safe JSON boundary and validates fixed repository/source/header identities, V-first state linkage, input hashes, natural route union, object identities, artifact root, converter source fingerprint, exact tensor execution order/dtype/shape, and every data/auxiliary SHA-256 before any backend construction.
- Self-review removed an invalid source-offset monotonicity assumption: manifest order follows execution, not shard layout. A live metadata/header-only re-plan then fixed the exact 17 KDA and 11 MoE trunk ranges in the harness without downloading tensor payloads.
- A second self-review found that the manifest microshard SHA-256 and K3X superblock source fingerprint have different definitions. D-063 preserves both under separate names, exposes the converter fingerprint in the C++ Reader, compares like-for-like identities, and reconstructs the deterministic safetensors header plus ordered payload stream to verify the microshard digest independently.
- Final schema review corrected q/k/v short-convolution metadata from a computation-flattened rank-2 view back to the checkpoint/K3X rank-3 shape `[12288,1,4]`. The portable KDA may reshape after loading, but artifact preflight must preserve the physical directory contract.
- Focused preflight/Reader/converter coverage passes 36 tests with 4 skips; the wider converter/source-integrity regression passes 123 tests; full CPU and CUDA CTest pass 19/19 and 32/32; strict warning compilation passes. The harness deliberately returns `BACKEND_UNAVAILABLE` after a successful preflight because official weight loading, CPU oracle execution, CUDA execution, and B-0030 remain unimplemented.

## 2026-08-11 — Milestone 29 Task 5C official-weight portable execution

- The first bounded materialization exposed sequence-length-dependent PyTorch CPU BF16 GEMM output. A 2,048-by-1,024 RED reproduces it; tokenwise reference projection makes full and incremental calls share the same sequence-one boundary without changing official weights or runtime semantics.
- Actual preflight then found two independent contract defects: MXFP4 microshard reconstruction had flattened the physical packed/scale shapes, and the state digest used projection-major convolution dimensions while the reference stores `[batch, history, projection]`. Both were corrected at the identity boundary before numerical comparison.
- PyTorch oneDNN BF16, explicit FP32 reduction, and explicit FP64 reduction produce different rounded Q/V projection words. The portable C++ hash matches FP64 reduction exactly, so byte equality with the framework backend would be a false portability requirement.
- D-064 adds a 6,541,344-byte atomic source-byte oracle sidecar bound by SHA-256. The bounded fixture has two disjoint Top-16 routes, 32 experts, 1,829,256,704 source-object bytes, and a 1,829,310,720-byte K3X artifact. The verified regeneration reused 60 objects and downloaded zero tensor payload.
- Measured maximum absolute source-byte versus portable differences are `1.52588e-05` output, `0.0078125` Q/K convolution state, `0.00390625` V convolution state, `4.39133e-05` recurrent state, and `1.06171e-06` contribution. Route IDs and portable full/incremental execution remain exact. The official harness now reaches the intended `BACKEND_UNAVAILABLE` only after all portable gates pass.
- CPU CTest passes 19/19, CUDA CTest passes 32/32, and focused Python passes 67 with 4 capability skips. No native official-layer CUDA execution, B-0030, token rate, quality, or physical traffic measurement exists yet.

## 2026-08-11 — Milestone 29 Task 6 native CUDA complete-layer boundary

- RED first failed on the absent complete-layer CUDA API. A second negative RED exposed that BF16 convolution state finiteness was not checked even though recurrent FP32 state and all weights were validated. The corrected backend rejects all three malformed BF16 state extents before allocation or launch.
- The minimum boundary keeps Attention Residual, RMSNorm, and exact natural all-896 routing on the host. All large BF16/F32 KDA projections, short convolution, channel decay, V-first recurrence, output projection, routed/shared BF16 work, and exact native-MXFP4 expert work execute on one CUDA backend with transient or exact-resident weights.
- Tiny transient/resident and full/incremental parity pass. CPU CTest is 19/19, CUDA CTest is 34/34, focused harness/Reader/C++ parity Python is 175 passed with 8 capability skips, and both new Compute Sanitizer runs report zero errors.
- The actual resident A-to-B cold smoke admitted 1,816,322,048 weight bytes, peaked at 1,824,612,416 tracked device bytes, moved 13,025,280 KDA-state bytes each direction, and matched the complete-layer oracle within `0.00048828125`. Its 381,907,507 ns wall and 32,897,536 ns profiled device time use zero warmups and one iteration, so they are explicitly capability observations rather than B-0030, token throughput, quality, physical traffic, or a runtime default.

## 2026-08-11 — Milestone 29 Task 7 strict B-0030 tooling

- RED failed on the missing `tools.ablate_official_layer` module. GREEN fixes three rows and closes schema, route/contribution identity, KDA call/launch/state traffic, resident warm-zero weight H2D, numerical error, full/incremental output/state digests, LF CSV, raw/aggregate hashes, and artifact/manifest/runner identities.
- The first validator GREEN exposed a mistaken resident-byte derivation that included host-only router/residual tensors. The actual backend identity is 1,816,322,048 resident bytes, not source payload minus only the three self-side vectors.
- Actual one-sample probes confirmed A transient, A-to-B incremental resident, and A+B full resident formulas. Full KDA launches 24 kernels because tokenwise projections add eight launches over the 16-launch one-token boundary; two incremental calls launch 32. Full and incremental BF16 output/state SHA-256 values are exact matches.
- Runner tests pass 8/8, compile validation passes, and focused CUDA CTest passes 3/3. Before commit, the schema was completed with BF16/F32/MXFP4 copy categories, process peak RSS, Reader logical/storage counters, per-file/directory fsync, atomic rename, and parent-directory fsync. No formal B-0030 result exists yet; the next action is the sole fixed 3-warmup/20-sample run from the committed Task 7 implementation.

## 2026-08-11 — Milestone 29 formal B-0030 and final local verification

- The sole fixed 3-warmup/20-sample B-0030 transaction completed from committed runner `8ace63d` and is preserved at evidence commit `bbdccb9`; no timing row was rerun or selected.
- Medians are 262,801,334 ns for A transient, 168,577,563 ns for resident A-to-B incremental, and 114,804,882 ns for resident A+B full. Both resident rows have zero warm weight H2D and exact full/incremental output plus final-state digest parity.
- Full resident wall time is 31.897887% lower than incremental, but per-sequence aggregate kernel time differs by only 0.416216%. D-067 therefore selects a separate immutable KDA validation/orchestration attribution experiment rather than inferring causality or tuning kernels first.
- Fresh final gates pass CPU CTest 19/19 plus Python 541/119, liburing/direct CTest 20/20 plus Python 543/117, ASan/UBSan CTest 20/20, and CUDA CTest 34/34 plus Python 639/21 against the actual bounded artifact. The committed evidence verifier passes 9/9.
- The first actual-artifact Compute Sanitizer attempts exceeded the external command limit because the tool's default 10-second attach timeout expired during checksum/preflight work and then waited for target completion. The retained log reported `No attachable process found`. Re-running with `--launch-timeout 0` completed in 80.6 seconds with `ERROR SUMMARY: 0 errors`, zero warm weight H2D, and maximum error `0.00048828125`.
- Production `k3x_run` still exits 4 with `NON_EXECUTABLE_ARTIFACT`. No complete shard/checkpoint, paid resource, token throughput, quality, or physical traffic result was created.

## 2026-08-11 — Milestone 29 public integration

- Public PR #50 carried the complete official KDA layer implementation, bounded B-0030 evidence, English README, and synchronized TITAN Ledger. The ignored 1.829 GB artifact and 6.5 MB oracle sidecar were not tracked.
- Branch correctness `31487723904`, pull-request correctness `31487748354`, and pull-request CodeQL `31487748339` passed. PR #50 was ready, clean, and rebase-merged at public head `2a4bfaf40284204ab314938f8112b280915f77df`.
- Post-merge `main` correctness `31488078940` and CodeQL `31488078974` passed. Milestone 29 is publicly complete; D-067 is the next bounded non-cloud experiment.

## 2026-08-11 — Milestone 30 official KDA admission-validation design

- B-0030 showed a 53.772681 ms resident full-versus-incremental wall gap while per-sequence device time differed by only 0.416216%. This selects attribution, not a causal conclusion.
- The existing official KDA boundary structurally validates and finiteness-scans eight BF16 plus six F32 immutable views on every call. Their runtime payload totals 887,800,832 bytes; dynamic hidden and KDA state are a separate per-call safety boundary.
- M30 reuses the M23 `CudaWeightValidationMode`, immutable identity registry, and counters. A KDA-specific cache and a harness-only bypass were rejected because both would create a second trust model.
- Admission is resident-only, exact-identity, and atomic across all fourteen views. A failed first scan must leave no partial identities, resident uploads, scratch work, or launches. The default remains `per-call`.
- B-0031 is fixed to resident incremental/full crossed with per-call/admission. It reuses the ignored M29 artifact, changes no tensor bytes or graph semantics, and makes no token, physical-traffic, utilization, bandwidth, or quality claim.
- The implementation plan has four connected tasks: backend admission semantics, harness CLI/telemetry, strict B-0031 tooling, and formal evidence plus publication. It deliberately leaves B-0030 files unchanged and requires one semantic commit per task.
- For twenty measured sequences, the closed validation formulas are 560 scans or hits for incremental execution and 280 for full execution. Per-call scanned bytes are 35,512,033,280 and 17,756,016,640 respectively; admission measured scan bytes are zero after the cold call and warmups.

## 2026-08-11 — Milestone 30 Task 1 official KDA admission semantics

- RED built successfully and exited 32 because official KDA recorded no immutable admission scan counters. A separate resident-only RED exited 29 because admission plus transient execution was still accepted.
- GREEN separates dynamic validation from fourteen immutable views, classifies exact pointer/byte/shape identities through the existing registry, scans all new views, and inserts only after every BF16/F32 payload is finite.
- Tiny coverage proves 14 scans and 384 bytes on first admission, 14 hits on reuse, 42 scans across three per-call executions, identity-conflict rejection, BF16/F32 non-finite rejection, atomic recovery after a failed first scan, and resident-only admission.
- Focused CUDA build passes `cuda_moe_layer_ops`, `cuda_moe_layer`, `cuda_official_kda`, and `cuda_official_layer` 4/4. No artifact, benchmark, routing, default, or production capability changed.

## 2026-08-11 — Milestone 30 Task 2 official-layer validation telemetry

- CLI/schema RED produced three expected failures because `--validation` was unknown. GREEN adds explicit `per-call|admission`, keeps the implicit default at per-call, and rejects admission plus transient weights before artifact access.
- New validation fields are emitted only when `--validation` is explicitly supplied. The implicit default output remains byte-schema compatible with B-0030 rather than retroactively changing its closed raw records.
- The actual bounded artifact passes the complete harness suite 25/25. Incremental admission cold execution records 14 scans, 14 hits, and 887,800,832 scanned bytes; its measured execution records 28 hits, zero scans/bytes/time, zero warm weight H2D, and unchanged numerical tolerance.
- B-0030 controlled evidence tests pass 9/9. Its strict verifier correctly rejects the newly rebuilt M30 executable as `runner_sha256 diverged`; final historical verification must use the preserved M29 runner identity rather than weakening or rewriting B-0030 evidence.

## 2026-08-11 — Milestone 30 Task 3 strict B-0031 tooling

- The B-0031 tool imports the established B-0030 canonical JSON, manifest identity, complete-layer traffic, hashing, fsync, and scalar serialization authorities. It adds only the four-row validation matrix, nine validation fields, their closed formulas, and cross-row exactness gates.
- The matrix is incremental per-call/admission followed by full per-call/admission. Every subprocess is exact resident and receives an explicit validation option, so the legacy implicit B-0030 schema remains separate.
- Controlled evidence tests mutate every validation counter/time category, validation identity, forbidden schema, cross-row output identity, raw JSON, CSV, and strict iteration identity. Failed transactions remove the partial directory and publish nothing.
- B-0031 plus B-0030 evidence-tool regressions pass 24/24, Python compile validation passes, and `git diff --check` passes. No formal B-0031 directory or timing result exists at this implementation checkpoint.
- The first formal command reached no benchmark subprocess and created no partial output because direct script execution could not resolve the package-qualified `tools` import. The runner now selects package import when imported and sibling import when executed directly; a subprocess `--help` regression fixes this exact boundary before the matrix is restarted.

## 2026-08-11 — Milestone 30 formal B-0031 and final local verification

- The restarted fixed 3-warmup/20-sequence four-row transaction completed in 360.1 seconds and atomically published B-0031. No row was rerun or selected for favorable timing.
- Incremental per-call/admission medians are 175.667985/70.584413 ms, a 59.819421% reduction. Full per-call/admission medians are 121.067320/67.236923 ms, a 44.463194% reduction. Validation consumes 103.874127/55.731721 ms per sequence on the per-call rows, while paired kernel totals change by less than 0.4%.
- All four rows preserve exact routes, contributions, output and final-state digests, 1,816,322,048 resident bytes, zero warm weight H2D, and `0.00048828125` maximum error. Admission leaves a 3.347490 ms incremental/full median gap.
- Strict verification, independent raw/CSV/summary SHA-256 cross-checks, and committed evidence tests pass. Aggregate SHA-256 is `5d6ba38a0d959902c5ab8e7f7bce4f13254f018644430a18490864c173b30a1f`; summary CSV is `ea4bcd6b6f613d4a07bdb4e8aa14cb989f2ef11a3fcdfd473112a846e0883501`.
- Actual admission incremental Compute Sanitizer reports `ERROR SUMMARY: 0 errors`. Production `k3x_run` remains fail-closed with exit 4 and `NON_EXECUTABLE_ARTIFACT`.
- Fresh final verification passes CPU CTest 19/19 plus Python 558/122, liburing/direct CTest 20/20 plus Python 560/120, ASan/UBSan CTest 20/20, and CUDA CTest 34/34 plus actual-artifact Python 659/21.
- D-068 accepts admission as an exact opt-in path but retains `per-call` as the default. This one-layer WSL2 result does not establish token throughput, quality, physical traffic, native-Linux behavior, or full-model lifetime safety.

## 2026-08-11 — Milestone 30 public integration

- PR #52 branch correctness `31493248372`, pull-request correctness `31493267425`, and pull-request CodeQL `31493267404` passed before merge.
- PR #52 rebase-merged at public head `51182575b32b49afa4b1fb2586f31df058a74155`.
- Post-merge `main` correctness `31493550970` and CodeQL `31493549669` passed. GitHub emitted action-version deprecation warnings only; no M30 correctness or analysis failure occurred.

## 2026-08-11 — Milestone 31 device-state design

- M30 publication PR #53 rebase-merged at `cd1cbd0d10d0231b4392d1aaec21dedd016ea3b1`; final documentation-only `main` correctness `31494813241` and CodeQL `31494813220` passed.
- D-069 is now scoped to one exact backend-owned KDA state slot. An opaque owner/generation token expresses seed, continuation, and final publication; host round trip remains the default.
- Implicit scratch reuse was rejected because sequence-sized offsets and allocation growth can move state, and stale callers cannot be detected. A general multi-session registry was deferred until the bounded transfer hypothesis is measured.
- B-0032 will compare host incremental, device-handoff incremental, and full host execution in one fixed resident-admission transaction. It will not claim token throughput, quality, physical traffic, or a default change.

## 2026-08-11 — Milestone 31 Task 1 RED

- The tiny official KDA CUDA test now requires device seed followed by final publication, exact CPU-oracle output/state parity, empty unpublished host-state vectors, one state transfer in each direction, successful seed/continuation/publication counters, and stale-token rejection before upload or launch.
- The focused CUDA build fails with seventeen expected compile errors because `OfficialKdaStateMode`, the opaque token/control surface, result publication fields, telemetry counters, and the controlled `official_kda` overload do not exist. No implementation code was present during RED.

## 2026-08-11 — Milestone 31 Task 1 GREEN

- `OfficialKdaStateMode` now keeps host round trip as the default and adds explicit device seed, continuation, and final publication. Results expose only an opaque backend-owner/generation token; no CUDA pointer crosses the interface.
- A dedicated grow-only state allocation stores convolution histories and recurrent state independently of sequence-sized operation scratch. Continuation binds exact backend, generation, layer, and KDA config and consumes tokens before mutation.
- Tiny tests prove CPU-oracle output/final-state parity, empty unpublished host vectors, one state H2D and D2H for two-call handoff, generation advancement, stale/cross-backend/wrong-layer/wrong-config rejection, continuation host-state rejection, and host-call invalidation.
- Focused CUDA KDA, official-layer, and MoE-layer tests pass 3/3. The non-CUDA build completes all targets and the backend test passes 1/1.

## 2026-08-11 — Milestone 31 Task 2 RED

- The official-layer CUDA test now requires exact A-to-B device seed/final-publication parity, empty intermediate host state, opaque token propagation, and one official 6,512,640-byte state transfer in each direction.
- The wrapper build fails with nine expected errors because it has no state-control argument or publication/token result fields. Focused Python CLI coverage has two expected failures because `--state-transfer` is not parsed.
- Review found that a post-KDA routing or MoE failure could otherwise strand an active token inside the backend. Task 2 therefore includes an explicit opaque-token discard operation and wrapper cleanup on downstream failure.

## 2026-08-11 — Milestone 31 Task 2 GREEN

- The official-layer wrapper propagates host/seed/continue/publish state control and returns publication state plus opaque token identity. Any downstream residual, routing, expert lookup, or MoE failure discards an active token before returning failure.
- The harness accepts explicit `--state-transfer host|device`, restricts device execution to `ab-incremental + resident + admission`, and emits cold/measured state-operation counters only when the option is explicit. Implicit B-0030/B-0031 schemas remain unchanged.
- Tiny failure-cleanup and exact device handoff tests pass. The actual bounded device smoke passes with measured 6,512,640-byte state H2D and D2H, one seed/continuation/publication, zero invalidations, zero warm weight H2D, and the existing maximum-error gate.
- Focused official-layer Python passes 28/28 in 218.63 seconds. CUDA KDA/layer/MoE tests pass 3/3; the non-CUDA build and backend test pass.

## 2026-08-11 — Milestone 31 Task 3 strict evidence tool

- B-0032 fixes three resident-admission rows in order: incremental host state, incremental device handoff, and full host state. The device row must reduce activation H2D, total D2H, state H2D, and state D2H by exactly 6,512,640 bytes per measured sequence.
- The schema extends B-0031 only with explicit state-transfer identity plus four cold and four measured state-operation counters. It reuses the B-0030/B-0031 manifest, validation, canonical JSON, LF CSV, checksum, fsync, and atomic-directory authorities without modifying historical evidence.
- RED failed at import because the tool did not exist. GREEN passes B-0032/B-0031/B-0030 evidence-tool regressions 46/46 plus Python compile validation. No formal B-0032 process has run and no timing result exists.

## 2026-08-11 — Milestone 31 formal B-0032 and local verification

- Final pre-measurement review found that an undefined `OfficialKdaStateMode` value could enter the device path. A dedicated RED exited 70; commit `9999918` adds exact four-value validation before transfer or launch and restores CUDA KDA/layer/MoE 3/3.
- Production `k3x_run` rejects the unchanged bounded artifact with exit 4 and `NON_EXECUTABLE_ARTIFACT` without creating output. Device-state actual-artifact Compute Sanitizer completes in 75.1 seconds with `ERROR SUMMARY: 0 errors`, one seed/continuation/publication, zero invalidations, one 6,512,640-byte state transfer in each direction, zero warm weight H2D, and maximum error `0.00048828125`.
- The sole fixed 3-warmup/20-sequence B-0032 process completed in 237.2 seconds and was sealed at `992e0de`. Host incremental, device incremental, and full host medians are 73.192169, 69.835612, and 68.224527 ms. Device handoff removes exactly 130,252,800 measured state bytes in each direction and lowers the incremental median 4.585951%.
- All rows preserve exact routes, contributions, output/final-state digests, 1,816,322,048 resident bytes, zero warm weight H2D, and `0.00048828125` maximum error. Device aggregate kernel time changes +0.339801%; orchestration falls 3.207668 ms per sequence; the device/full median gap is 1.611085 ms.
- Strict verification and independent SHA-256/formula/LF checks pass. Aggregate, summary JSON, and summary CSV SHA-256 are `88db7c3e8210035204a3e6679c482782f8223de1b49edd39f5d407ad3edab339`, `42cb8809b6e7b0b0a23f152f8377cdaffa1d5a6d0efd31d7182322d146963d5f`, and `9abfb06e0bc936211e258b39f2aa2cc0bf88e1c3ec553dd68e9427291fc79c11`.
- Fresh final gates pass CPU CTest 19/19 plus Python 578/125, capability-aware liburing/direct CTest 20/20 plus Python 584/119, ASan/UBSan CTest 20/20, CUDA CTest 34/34 plus actual/live Python 688/15, and evidence regressions 46/46. An initial liburing pytest omitted `K3X_TEST_IO_URING=1`, correctly failed the opposite capability test, and was replaced by the explicit `K3X_TEST_IO_URING=1 K3X_TEST_DIRECT=1` run.
- B-0032 has no token semantics, throughput, TTFT, quality, physical traffic, utilization, bandwidth, native-Linux, multi-layer, or default-policy claim. Host round trip and per-call validation remain defaults; device state and admission remain explicit experiments.
- Final self-review compared the complete M31 diff against opaque-token lifetime, mutation-time invalidation, downstream failure cleanup, host-default compatibility, explicit schema ownership, fixed evidence formulas, and production non-executability. No remaining Critical or Important finding was identified; single-backend concurrency, multi-session state, and exception-level allocation recovery remain explicitly outside M31 rather than silently claimed.

## 2026-08-11 — Milestone 31 public integration

- PR #54 branch correctness `31501537039`, pull-request correctness `31501569778`, and pull-request CodeQL `31501569789` all succeeded before merge.
- PR #54 rebase-merged at public implementation head `e1233891537f14785373f47e9f736fed43598c46`. Post-merge `main` correctness `31501949124` and CodeQL `31501949081` also succeeded.
- Workflow annotations were limited to the Node 20 and CodeQL Action v3 deprecation notices plus the existing C++ overlay-base fallback warning. Every recorded job conclusion was `success`.
- M31 is publicly complete. M32 will compare a wider device-resident residual/routing boundary with bounded multi-layer closure before code changes; host state and per-call validation remain defaults.

## 2026-08-11 — Milestone 31 publication reconciliation and M32 decision

- Publication PR #55 rebase-merged at `c46d6139673d442a83858e637d268e0d92a7784a`. Post-merge correctness `31503260596` and CodeQL `31503260599` succeeded; annotations remain the known Node 20, CodeQL v3, and C++ overlay-base fallback warnings.
- M32 compared a second official layer, a monolithic whole-layer call, and a two-stage device route-preparation boundary. D-070 selects the two-stage boundary because it directly targets the remaining CPU residual/router work without new official payload and preserves dynamic expert scheduling.
- The GPU will return raw router logits, while the existing canonical host rule retains sigmoid, correction, natural Top-16 tie breaking, and contribution normalization. This prevents the attribution experiment from silently changing routing policy.
- Prepared prefix/hidden vectors remain backend-owned behind one opaque single-use token and are consumed only by exact resident MXFP4 FFN. Host routing, host state, per-call validation, production non-executability, and all historical schemas remain defaults.
- The M32 implementation plan fixes five reviewable units: canonical raw-logit routing, CUDA preparation/token lifetime, wrapper/CLI ownership, strict B-0033 evidence, and formal verification/publication. Each unit has a witnessed RED, focused GREEN, self-review, and semantic commit boundary.

## 2026-08-11 — Milestone 32 Task 1 canonical routing

- The initial command used the stale distro label `Ubuntu` and failed before compilation with `WSL_E_DISTRO_NOT_FOUND`. The registered distro is `Ubuntu-24.04`; the plan commands were corrected before the actual RED.
- The controlled RED failed because `route_official_moe_logits` was undeclared. GREEN moves only sigmoid, correction, natural Top-K ordering/tie breaking, selected mass, and contribution normalization into the shared helper.
- The existing BF16 router matvec delegates to the helper and returns bit-identical route fields on the tiny fixture. Explicit tie, non-finite, length, Top-K, and zero-mass cases fail or pass as required. Portable official expert/MoE/KDA/layer CTest passes 4/4.

## 2026-08-12 — Milestone 32 Task 2 CUDA preparation

- Controlled RED produced 21 expected compile errors for the missing route view, opaque token, prepare/consume/discard API, and telemetry fields.
- The new CUDA boundary uses one deterministic single-thread BF16 residual/RMS preparation kernel plus a row-parallel raw-logit kernel. It retains exactly two hidden-width F32 vectors in a grow-only slot and returns no prepared activation or device pointer.
- Initial CTest exposed that the direct diagnostic command had hidden the program status. The real return code was 39. Narrow return codes and error messages localized the first failure to prepared FFN launch: byte count used the intentionally empty host span instead of token width. After that fix, output parity exposed a second instance where the prefix scratch offset used the empty host span and overwrote prepared hidden. Both source calculations now use the validated token width.
- Final focused verification passes CUDA official 3/3, CUDA MoE-layer 2/2, non-CUDA unavailable, portable official MoE, and Compute Sanitizer with `ERROR SUMMARY: 0 errors`. Host/default behavior, natural routing policy, and production capability remain unchanged.

## 2026-08-12 — Milestone 32 Task 3 wrapper and harness

- Controlled CUDA RED failed on the missing `OfficialMoeRoutePreparationMode` and extra wrapper argument. Focused Python RED passed 11 historical invalid cases and failed only the two new route-preparation cases because the CLI rejected the option generically.
- The wrapper keeps the omitted host path intact. Explicit device mode prepares residual/router work on CUDA, applies canonical host routing to raw logits, resolves exact experts, and consumes one opaque prepared token in the resident FFN.
- Downstream route, missing-expert, and FFN failures all discard the prepared token and invalidate the outstanding KDA token. Tiny CUDA tests and Compute Sanitizer pass with zero errors.
- The explicit CLI mode is restricted to AB incremental, device KDA state, resident weights, and admission validation. New JSON fields appear only when route preparation is explicit; historical implicit schema remains unchanged.
- One actual host smoke and one actual device-route smoke pass on the bounded 896-expert fixture. The device row returns exactly 7,168 logical router-logit bytes for two positions and zero warm weight H2D. No timing result or default-policy change is accepted before B-0033.

## 2026-08-12 — Milestone 32 Task 4 strict B-0033 evidence tool

- Controlled RED failed at import because `tools.ablate_official_moe_device_routing` did not exist. GREEN adds a fixed host-then-device two-row transaction with device KDA state, resident admission, canonical JSON, LF CSV, fsync, staged directory replacement, and strict hash binding.
- The verifier normalizes only the exact device-route deltas before delegating to the complete B-0032 contract: 12,888,064 resident/cold BF16 bytes, 7,168 logit D2H bytes per two-position sequence, two synchronizations, eight validation/cache hits, and the fixed prepare/consume counters.
- New and historical B-0030 through B-0033 evidence tests pass 65/65. Python compilation passes.
- An actual 0/1 smoke generated both rows and passed non-official strict verification. The host/device rows retained zero warm weight H2D and resident bytes 1,816,322,048/1,829,210,112; the device row recorded 7,168 logit bytes, two seeds/consumes, and zero discards/invalidations. The CLI then correctly rejected 0/1 as formal evidence through the 3/20 iteration gate.

## 2026-08-12 — Milestone 32 formal B-0033 and final verification

- Actual device-route Compute Sanitizer reports zero errors. Production `k3x_run` rejects the unchanged bounded artifact with `NON_EXECUTABLE_ARTIFACT` and creates no output.
- The sole fixed 3-warmup/20-sequence B-0033 transaction completed in 152.9 seconds and was sealed at `3d5d96c`. Host/device medians are 64.210407/63.767134 ms, a 0.690344% device reduction.
- Device route kernel time increases 27.294141%, while orchestration falls 9.279958 ms per sequence. It adds exactly 12,888,064 resident bytes and 7,168 logical D2H bytes per sequence. Exact routes, contributions, output/state digests, zero warm weight H2D, and `0.00048828125` maximum error are preserved.
- Strict verification, raw/summary hashes, aggregate recomputation, formulas, row order, forbidden fields, and LF CSV all pass. Host routing remains the default because the one-layer WSL2 wall change is small and mixed.
- Fresh final gates pass CPU CTest 19/19 plus Python 597/128, liburing/direct CTest 20/20 plus Python 603/122, ASan/UBSan CTest 20/20, CUDA CTest 34/34 plus Python 710/15, and evidence regressions 65/65.
- M33 should close a bounded multi-layer device-resident boundary before any full checkpoint or paid Cloud Run work. This is still not token throughput, quality, physical PCIe/NVMe traffic, native-Linux authority, or a production default.

## 2026-08-12 — Milestone 32 public integration

- Public ready PR #56 passed branch correctness `31510344481`, pull-request correctness `31510368444`, and pull-request CodeQL `31510368390`, then rebase-merged at `ab0ecb19ade01e7989bcba9f6dbcd1c853c43432`.
- Post-merge `main` correctness `31510749958` and CodeQL `31510749973` succeeded. Annotations remain the known Node 20 and CodeQL Action v3 deprecation notices; no new failure or security finding appeared.
- M32 is publicly integrated. Host routing remains default, and M33 bounded multi-layer closure is next before any full checkpoint or paid Cloud Run milestone.

## 2026-08-12 — Milestone 33 design

- The public M32 head is the clean starting point. B-0033 leaves a split boundary in which four added kernels and two router-logit synchronizations per sequence offset most of the host-orchestration reduction.
- The released schedule and synthetic reference both place KDA layers at decoder layers 1 and 2. Current official manufacturing, oracle, artifact naming, harness identity, and materialization checks are intentionally fixed to layer 1.
- A genuine two-layer A/B trace must interleave model order and retain independent KDA recurrence for each layer. The current backend's one global state slot cannot express that contract.
- D-071 selects a capacity-two layer-keyed state registry and an opaque ping-pong hidden/block token. Canonical natural Top-16 and dynamic expert resolution stay on the host; device Top-K is not bundled into M33.
- Replaying layer 1 twice was rejected because it cannot prove layer-2 tensor, state, or transition identity. A monolithic two-layer device-Top-K call was rejected because it hides the cache/rescue scheduling point and expands the correctness surface before the bounded boundary is measured.
- K3X v1 already stores canonical tensor names and per-layer directories. The proposed two-layer fixture can remain v1 and be assembled in execution order after dependency-ordered route discovery. No full checkpoint or paid resource is authorized or needed.

## 2026-08-12 — Milestone 33 implementation plan

- The plan separates layer-2 metadata authority, dependency-ordered manufacturing, portable interleaved execution, two-slot KDA state, opaque hidden ownership, the CUDA wrapper, evidence tooling, and formal measurement into semantic commits.
- Existing single-layer manufacture and CLI schemas remain frozen. The new two-layer path gets a dedicated orchestrator and benchmark executable so M29 through M32 evidence identities do not drift.
- The two-layer source remains `k3-official-moe-slice-v1`; its existing `tensor_order` authority already controls K3X physical order and K3X v1 already emits per-layer directories. A two-layer metadata envelope is additive and the production optional-feature guard remains unchanged.
- The first implementation task is payload-free planner TDD. New official payload is permitted only after the metadata-only dry-run, portable/tiny gates, and explicit bounded byte checks pass.

## 2026-08-12 — Milestone 33 Task 1 bounded layer-2 planning

- The controlled RED produced exactly two intended failures: layer-2 MoE and KDA plans were rejected by the literal layer-1 guards. Layer IDs 0 and 3 already failed closed.
- Commit `2a938ef` changes only the two planner guards to accept `(1, 2)`. Follow-up test commit `752972a` binds wrong-shard and missing-KDA-identity failures found during self-review. The existing layer-1 materializer, fixed inputs, manifests, artifact name, and production guard are unchanged.
- Final Task 1 verification passes 111 tests across official MoE/layer/source, source-manifest integrity, and converter resume. Python compilation and `git diff --check` pass.
- No tensor payload, complete shard, checkpoint, benchmark, or paid resource was used. Task 2 begins with an import-level RED for the dependency-ordered two-layer orchestrator.

## 2026-08-12 — Milestone 33 Task 2 plan and scheduler boundary

- The import-level RED failed because `k3x_converter.official_two_layer` did not exist. Commit `3665182` adds the exact `(1, 2)` composite plan with pinned source/index, nested MoE identity, and fixed byte-bound validation.
- Self-review found that matching non-official source blobs could pass the first GREEN. A focused RED reproduced the acceptance, and the composite boundary now checks the same pinned `OFFICIAL_KDA_SOURCE_BLOB_ID` as the single-layer planner.
- A second import RED failed on the missing trace state/execution API. Commit `481309e` adds a pure A1→A2→B1→B2 scheduler that preserves each layer's state chain, propagates the original block source, binds inter-layer activation digests, and records per-layer expert unions.
- Focused plan/scheduler plus historical official MoE/layer verification passes 33 tests. This is scheduling evidence only: the exact source-byte executor, materializer, artifact, CLI, runtime, and B-0034 do not exist yet, and no payload was read.

## 2026-08-12 — Milestone 33 Task 2 source-byte executor

- A controlled import RED failed on the missing source-byte tensor, MXFP4 expert, state, and executor APIs. The minimum implementation accepts BF16/F32 payload bytes plus native MXFP4 packed/scale bytes and reuses the official KDA, Attention Residual, and natural-routing references.
- The tiny exact A1→A2→B1→B2 trace keeps independent recurrent states, propagates layer-1 output into layer 2, preserves each position's block source, executes selected experts serially, and records contribution digests. A truncated dense payload fails closed before execution.
- Commit `0b7dc19` passes 10 focused tests and 45 focused plus historical official KDA/MoE/layer tests. Python compilation and Windows `git diff --check` pass. No official payload, artifact, benchmark, full checkpoint, or paid resource was used.

## 2026-08-12 — Milestone 33 Task 2 dependency-ordered manufacturer

- Commit `1be65f6` adds a tiny execution-ordered K3X v1 fixture with layer directories 1/2, complete expert records, strict source/tensor/root verification, optional non-executable feature bits, and interrupted extent resume.
- Commit `324822f` adds the real two-shard orchestration boundary and `--scope two-layer`. Dry-run reads only metadata and both headers. Materialization fetches both trunk sets before any expert, then prepares each A1→A2→B1→B2 route and fetches only that layer's first-use experts before exact completion.
- The external oracle stores both final outputs and both final official KDA states in a deterministic checked binary payload. The K3X source manifest binds its digest, while the final route-state manifest binds traffic, object ranges/hashes, trace digests, and artifact hashes.
- Focused plus historical official converter, format, source-integrity, resume, and discovery CLI verification passes 139 tests. Python compilation and `git diff --check` pass. The transport/materializer test is mocked and no official payload, complete shard, full checkpoint, benchmark, or paid resource was used.

## 2026-08-12 — Milestone 33 Task 3 portable two-layer runtime

- The controlled C++ RED failed because `k3x/official_two_layer.hpp` did not exist. Commit `3dd0a80` adds the minimum exact A1→A2→B1→B2 orchestrator and delegates all numerical work to the existing portable official-layer implementation.
- The runtime accepts exactly two inputs, layer IDs `(1, 2)`, and two caller-owned initial states. It keeps independent local state chains, preserves each input's block source, feeds layer-1 hidden output into layer 2, and publishes four attributed steps, two final states, and two final outputs.
- A fixture-lifetime failure initially corrupted the first expert ID because an `OfficialMoeWeights` span referenced a returned local vector. The test now owns both expert arrays for the fixture lifetime; focused CTest and direct execution pass without changing production code.
- Four official portable CTests and two focused cross-language tests pass. The new source and test pass `-Wall -Wextra -Wpedantic -Werror`; the repository-wide warning build stops on pre-existing `reader.cpp` warnings and was not broadened into an unrelated cleanup. Four ASan/UBSan official tests pass.
- This task downloaded no official payload, complete shard, or checkpoint, ran no performance benchmark, and provisioned no paid resource. The next implementation boundary is the capacity-two layer-keyed CUDA KDA state registry.

## 2026-08-12 — Milestone 33 Task 4 two-slot CUDA KDA state

- The controlled CUDA RED passed compilation and failed `cuda_official_kda` after seeding layer 1 and then layer 2 because the second seed replaced the backend's one global state slot.
- Commit `8580df8` replaces that single device-state buffer and metadata with exactly two grow-only slots keyed to layers 1 and 2. Owner identity remains backend-wide, generations remain globally unique, and host round trips use separate scratch storage.
- The ownership test interleaves two seeds, two continues, and two publishes against independent CPU oracles; verifies exact state/output parity and unchanged state H2D/D2H accounting; rejects live overwrite and layer 3; and proves a layer-1 host round trip invalidates only layer 1 while layer 2 remains publishable.
- CUDA official MoE/KDA/layer CTest passes 3/3, non-CUDA unavailable passes, and focused M31 device-state Python regressions pass 21/21. A CUDA `-Wall -Wextra -Werror` build passes after excluding one pre-existing missing-field initializer warning; `-Wpedantic` cannot be used because it rejects NVCC-generated line directives. Compute Sanitizer reports `ERROR SUMMARY: 0 errors`.
- No benchmark, official M33 payload, complete shard/checkpoint, production default change, or paid resource was created. The next boundary is the opaque inter-layer hidden/block token and CUDA front/tail bridge.

## 2026-08-12 — Milestone 33 Task 5 inter-layer activation bridge

- The controlled CUDA compile RED failed on the missing `OfficialLayerHiddenToken`, front, tail, and discard APIs. Commit `af762e6` adds only the bounded bridge required by D-071.
- Two grow-only activation slots retain hidden plus the original block source behind owner/generation/producer/width/slot tokens. Layer 1 is the only host-input front; layer 2 must consume the exact preceding token. Cross-backend, stale, wrong producer/consumer/width/slot, double consumption, unexpected host input, and failed-tail cleanup fail closed.
- The front reuses the existing exact Attention Residual, RMSNorm, KDA, prefix, and device route-preparation paths through internal device I/O overrides. The tail reuses the exact prepared native-MXFP4/shared FFN and either performs a device-to-device retain into the opposite slot or publishes only the final hidden vector. Canonical natural routing and expert resolution remain on the host.
- CUDA official MoE/KDA/layer CTest passes 3/3, non-CUDA unavailable passes 1/1, and two historical device-state/route Python regressions pass 2/2. The strict CUDA warning build passes with the previously recorded NVCC/pre-existing-warning exclusions. Compute Sanitizer reports zero errors for both official MoE and layer targets.
- The retained-path assertions bind logical activation H2D to the layer-2 KDA seed only, logical D2H to raw router logits only, and official KDA output D2H to zero. They are not physical PCIe measurements. No benchmark, official M33 payload, complete shard/checkpoint, production default change, or paid resource was created. Task 6 owns wrapper-level transaction cleanup and closed two-layer telemetry.

## 2026-08-12 — Milestone 33 Task 6 CUDA wrapper

- The first target RED reported no `test_cuda_official_two_layer`; after CMake registration, compilation failed on the missing CUDA weights, mode, and wrapper API. Commit `9265799` implements the exact bounded wrapper without changing historical one-layer interfaces.
- `host_round_trip` composes the existing device-state/device-route layer call. `device_closure` composes the front/tail bridge and keeps layer-1 hidden plus block on device. Both execute A1→A2→B1→B2, own independent layer state tokens, use canonical host routing, resolve exact experts after routing, publish two outputs and final states, and emit no token/TPS field.
- The CUDA test matches the portable oracle's four route ID lists, contribution vectors, two final outputs, and both final states. It rejects swapped layers, an invalid mode, a missing layer-2 expert, and insufficient residency, then proves a failed transaction leaves the backend reusable.
- CUDA official MoE/KDA/layer/two-layer CTest passes 4/4 and the portable two-layer CTest passes 1/1. Direct strict compilation passes for the changed C++ and CUDA test sources; the repository-wide warning build still stops on the recorded pre-existing `reader.cpp` warnings. Compute Sanitizer reports zero errors for the new two-layer target. Production `k3x_run` still reports `NON_EXECUTABLE_ARTIFACT` for the bounded official artifact.
- Logical telemetry separates state, KDA output, router logits, inter-layer hidden, final hidden, and generic backend deltas. Device closure reports zero inter-layer hidden H2D/D2H, but no physical PCIe claim or performance measurement is made. The dedicated artifact/manifest/oracle harness and Python schema RED remain the next Task 6 boundary.

## 2026-08-12 — Milestone 33 Task 6 dedicated harness

- The CLI RED first failed because `k3x_cuda_official_two_layer_bench` did not exist. The minimum parser then passed invalid-mode, duplicate-key JSON, layer-order, oracle-before-artifact, and verified-oracle-before-malformed-artifact boundaries before any CUDA construction.
- Commit `d13f88e` adds the complete benchmark-only harness. It binds the pinned official repository/revision/source/config/index and exact layer-1/layer-2 shards; checks A1→A2→B1→B2 route unions and state chains; validates the oracle header, size, filename, and digest; and verifies K3X root, layer IDs, physical tensor order, and every dense/MXFP4 packed/scale digest.
- The released-dimension dtype/shape/expert loader was extracted into one layer-ID-parameterized implementation and the historical one-layer harness now uses the same authority. The full historical/new harness matrix passes 36/36 against the existing actual M29 artifact, demonstrating no one-layer schema or numerical regression.
- The portable two-layer result now retains attributed KDA state and output for each of four steps. Before CUDA creation, the harness recomputes route IDs, contributions, contribution digest, state digest, KDA-output digest, final-output digest, final states, and sidecar parity. CUDA output exposes bounded wall/error/residency/VRAM and logical traffic fields only; it has no token/TPS field.
- Final focused verification passes six two-layer Python/C++ tests, CUDA official CTest 4/4, rebuilt portable CTest, strict changed-source compilation, production `NON_EXECUTABLE_ARTIFACT`, and Compute Sanitizer with zero errors. This task did not download or execute an official M33 payload and did not run B-0034, so performance and physical PCIe claims remain absent.

## 2026-08-12 — Milestone 33 Task 7 strict B-0034 tooling

- The witness test first failed with `ModuleNotFoundError` for `tools.ablate_official_two_layer_closure`, establishing the controlled RED before the publisher existed.
- The publisher fixes exactly two rows in host-round-trip/device-closure order. Strict verification requires one immutable artifact/manifest/oracle/runner transaction, three warmups, twenty measured A/B traces, 4 GiB resident admission, zero measured weight H2D, exact logical state/KDA/router/inter-layer/final transfer formulas, closed state/prepared lifetime counters, and route/contribution/output/final-state identities.
- The CUDA harness now includes final KDA state in its maximum-error gate and exposes cumulative state and prepared-slot lifetime counters. This closes the evidence contract without changing the production runtime or historical evidence schemas.
- Canonical raw JSON, LF-only CSV, summary and aggregate hashes, per-file/directory fsync, atomic directory replacement, and forbidden token/TPS/quality/physical-traffic fields are enforced. The verifier makes no ranking or performance interpretation.
- The new plus B-0030 through B-0033 evidence suites pass 87/87, CUDA two-layer CTest passes 2/2, the dedicated CUDA harness Python boundary passes 5/5, Python compile validation and strict changed-source compilation pass, and `git diff --check` passes. No official M33 payload was read and B-0034 was not run.

## 2026-08-13 — Milestone 33 actual-artifact correctness repair

- The completed 3,641,057,536-byte bounded fixture exposed two harness defects before B-0034. Its physical tensor order is KDA, pre-expert MoE trunk, selected experts, then post-expert MoE trunk; the original harness incorrectly expected every MoE trunk tensor before experts.
- Independent PyTorch and portable C++ accumulation does not preserve exact float digests across two layers. The real semantic envelope is exact expert sets, finite ID-keyed contributions within `2e-5`, final BF16 output within `2e-3`, BF16 convolution state within `8e-3`, and recurrent state within `5e-4`. The oracle file and its manifest-bound final-state digests remain exact provenance checks.
- Host-round-trip and device-closure cold and warm correctness smokes pass. Warm measured weight H2D is zero in both modes. Device closure reports zero inter-layer hidden H2D/D2H and a measured maximum error of `0.00195312`; its direct oracle recurrent error is `0.000492964`.
- Self-review found that B-0034 raw output/state identity fields were copied from the manifest rather than observed. The runner now publishes measured route IDs, contribution digests, final-output digests, and final-state digests. The evidence tool requires exact route/contribution parity across modes but preserves output/state digest divergence instead of claiming exact cross-mode equality.
- Focused verification passes official CUDA/portable CTest 5/5, harness boundaries 6/6, B-0030 through B-0034 evidence 89/89 before the added measured-identity mutation cases, and strict source compilation. The formal 3-warmup/20-iteration B-0034 transaction remains unrun.
- The first formal B-0034 attempt reached the host row but published nothing because the evidence formula omitted resident route-preparation weights. The exact correction adds `12,888,064` bytes per layer in both modes and a further three BF16 hidden-width vectors, `43,008` bytes per layer, for device closure. Mode-dependent formulas and their exact `86,016`-byte delta now pass 93 evidence regressions.
- The second formal attempt executed both rows but published nothing because the cross-row gate still required exact contribution digests. Each row already validates expert-ID-keyed contributions against the manifest within `2e-5`; host/device floating-point accumulation may legitimately produce distinct digests. The evidence now preserves that measured divergence while retaining exact route-ID parity and per-row semantic validation.
- The third formal transaction completed and atomically published B-0034. Strict `--verify-existing` and independent SHA-256 recomputation pass. Host/device medians are `96,102,951` and `109,388,034` ns, so device closure is `13.823803%` slower despite removing `57,344` logical inter-layer bytes in each direction per sequence. Host round trip remains the default; front/tail kernel and synchronization attribution is the next bottleneck boundary.
- Evidence commit `fa735fb` seals both raw JSON files plus canonical JSON/CSV summaries. The result contains no token throughput, TTFT, quality, utilization, bandwidth, or physical PCIe/NVMe claim, and no full checkpoint or paid cloud resource was used.
- Fresh final gates pass portable CTest 20/20, portable Python 652/134, liburing/direct CTest 17/17, ASan/UBSan CTest 17/17, CUDA CTest 36/36, actual two-layer harness 6/6, and evidence regressions 93/93. Actual device-closure Compute Sanitizer reports zero errors. Production execution remains fail-closed with `NON_EXECUTABLE_ARTIFACT` and no output.
- The complete CUDA-enabled Python suite exceeded both five- and ten-minute command limits with buffered output and is not claimed as passed. No failure was observed, but timeout is not success; the actual M33 CUDA boundary is instead covered by the focused harness, full CUDA CTest, evidence regressions, and sanitizer above.
- Public PR #58 rebase-merged M33 at `9ce513f9d79b6cee88b7bb2de176e6fbbb79f43b`. Branch correctness `31673610347`, pull-request correctness `31673636564`, pull-request CodeQL `31673636680`, post-merge correctness `31673888294`, and post-merge CodeQL `31673888289` all succeeded. The remaining workflow annotations are known deprecation and overlay-database fallback notices.
- Publication PR #59 rebase-merged the synchronized public state at `f46147f1b1f0c908fa1608f194e1305b328a0d2e`; post-merge correctness `31674457349` and CodeQL `31674457230` succeeded.
- M34 is bounded attribution-only work. Existing profiler snapshots around the exact front/tail calls were selected over new CUDA events or duplicated orchestration because they add no synchronization and preserve one cleanup authority. Default B-0034 JSON remains unchanged; an explicit attribution schema owns B-0035.
- M34 Task 1 RED failed compilation on the missing accumulator and wrapper signature. GREEN adds one optional caller-owned result, snapshots only existing successful profiler events, and publishes only after a complete successful transaction. CUDA and portable focused CTest pass, and Compute Sanitizer reports zero errors.
- M34 Task 2 parser RED returned `unknown argument` for `--attribution`; GREEN adds strict true/false parsing, creates a profiler only for the opt-in path, and preserves the historical schema without attribution timing keys. The actual default and attribution harness suite passes 8/8 in 232.46 seconds.
- Historical B-0034 strict verification now correctly rejects the rebuilt M34 runner SHA. The committed raw/summary evidence is not rewritten and no current-runner parity claim is made; default schema compatibility is verified by the actual harness test instead.
- M34 Task 3 tooling RED failed import on the missing B-0035 module. GREEN passes six new transaction/formula/mutation tests and 99 combined B-0030 through B-0035 evidence regressions. Actual attribution Compute Sanitizer reports zero errors, zero warm weight H2D, exact B-0034 route/output/state identities, and a closed wrapper timing formula.
- Formal B-0035 completed once and published atomically. Strict committed-evidence verification passes. Host/device medians are 99.316205/110.701472 ms. Device closure averages 69.822990 ms front wall, 0.039036 ms route wall, 41.060877 ms tail wall, and 0.023582 ms remainder; front and tail consume 62.934% and 37.010% of attributed wall time.
- B-0035 shows routing is not the useful next target. Existing-event front/tail CUDA time averages 52.571374/30.057734 ms, so the next bounded evidence task is operation-level attribution inside those regions before any fusion decision. Host round trip remains the default, and no token-throughput or physical-traffic claim is made.
- Final M34 local gates pass CPU CTest 20/20, liburing CTest 21/21, ASan/UBSan CTest 21/21, CUDA CTest 36/36, portable Python 658/136, and focused actual CUDA/attribution Python 14/14. Strict B-0035 rehash passes with the unchanged runner digest. The actual bounded artifact remains fail-closed and produces no output.
- Public PR #60 rebase-merged M34 at `a7ba52044e4d0ccbb23371cac54b5db87d4002f1`. Push correctness `31677396649`, pull-request correctness `31677408262`, pull-request CodeQL `31677408278`, post-merge correctness `31677651704`, and post-merge CodeQL `31677651706` all succeeded. The remote implementation branch was deleted.
- Publication PR #61 rebase-merged the synchronized M34 ledger at `acc14d0dad1796305161249001bfd24598086601`; post-merge correctness `31678280229` and CodeQL `31678280137` succeeded.
- M35 begins from sealed B-0035. D-073 selects classification of existing successful profiler events within the already-bounded front/tail snapshots. This preserves synchronization and execution behavior while distinguishing official KDA, device route preparation, and official MoE FFN device time. Unknown events remain visible through checked unclassified buckets.
- M35 ownership RED failed compilation on the five absent operation buckets. GREEN classifies existing successful events with checked arithmetic, closes front/tail device totals, preserves the caller accumulator on failure, and passes focused CUDA/portable CTest 2/2.
- The strict harness adds an independent operation-attribution switch and schema. Parser/conflict tests pass 2/2, fast harness regressions pass 8/8, and actual default/M34/M35 schema compatibility passes 3/3. M34 emits no M35 fields.
- B-0036 tooling RED failed import before the separate module existed. GREEN passes 6 new tests and 105 B-0030 through B-0036 evidence regressions. Actual operation-attribution Compute Sanitizer reports zero errors and exact regional closure.
- Formal B-0036 completed once and strict rehash passed. Host/device medians are 102.157295/116.049550 ms. Device existing-event time averages 36.345792 ms KDA, 19.075499 ms route preparation, and 31.698525 ms MoE FFN; both unclassified buckets are zero. KDA is largest at 41.719%, but no fusion/default change is accepted.
- Final M35 gates pass CPU CTest 20/20, liburing CTest 21/21, ASan/UBSan CTest 21/21, CUDA CTest 36/36, portable Python 664/139, evidence 105/105, strict B-0036 rehash, actual Compute Sanitizer zero errors, and the production non-executable guard.

## 2026-08-13 — Milestone 36 first-token pivot

- The user prioritized reaching an actual Kimi K3 execution quickly and explicitly requested no subagents, fewer repeated tests, and batched work.
- The shortest honest K3X path is selected-range execution, not a 1.56 TB full-checkpoint download and not a temporary third-party engine. One embedding row, always-active layer tensors, natural Top-16 experts, and LM-head chunks are sufficient for one exact text token.
- Development validation is limited to one focused test after the connected change and one final integration gate. Correctness failures remain visible and fail closed.
- Public M35 post-merge correctness run `31681318225` and CodeQL run `31681318273` both succeeded at `046a27f195ebf02cf0ce58c7da22d11f43a1b19f`.
- The live pinned metadata transaction inspected all 96 headers in 41.5 seconds with eight independent readers and zero tensor payload bytes. It found 497,220 checkpoint tensors and 1,560,860,324,864 tensor bytes; the text graph owns 497,052 tensors and 1,559,965,606,912 bytes.
- The manifest closes 69 zero-based KDA layers, 24 zero-based MLA layers, dense layer 0, and MoE layers 1 through 92. Its record SHA-256 is `0617a499fec4cbe9a0f35f67517ef0eb7470e28a13097cd4bf3515429ba8e596`.
- Existing exact component byte contracts imply 139,339,113,472 source tensor bytes for natural Top-16 one-token execution before replacing the full embedding matrix with one 14,336-byte row. This is an engineering byte model, not measured runtime traffic or TPS.
- The exact live manifest refines the selected-range one-token source bound to 136,990,317,568 bytes after replacing the 2,348,810,240-byte embedding with one 14,336-byte row.
- Layer 0 executed the released embedding row, KDA, and dense SiTU path on the RTX 5080. Its cold range download was 2,341,227,520 bytes in 517.196 seconds; compute was 1.049 seconds with 3,602,630,144 peak allocated CUDA bytes. The output/state digests are `05d2b350b34b0bab1c0a985b6a828a65935877daaebb37045c8aff46934e0273` and `d541668171aebade61736168161e4efecd3a96f0bb73eecabdc3095b4a19a557`.
- The existing M33 layer-1/layer-2 trunk objects were hard-linked into the M36 content-addressed cache and reverified before use. Actual layers 1 and 2 each downloaded only 280,756,224 routed-expert bytes and executed in 1.545/1.618 seconds.
- The first actual Gated MLA layer exposed a BF16 output-projection dtype omission in the synthetic reference. A focused BF16 regression failed at the projection, the gated activation is now rounded to the projection dtype, and the full MLA test file passes 5/5.
- Actual layer 3 then completed with natural route `[515,727,65,477,48,193,703,296,21,196,810,354,287,154,262,885]`, 280,756,224 new expert bytes, 1.593 seconds compute, 1,101,418,496 peak allocated CUDA bytes, and output digest `6d35fd9c01db90b2cb2473ccc1d1057f7143a4616c97e6b971005610995f1539`. No final token has been generated yet.

## 2026-08-13 — Milestone 37 local Foundry pivot

- The user selected local-only 1.28 TB manufacturing to avoid Cloud Storage internet egress. The interrupted first-token traversal was stopped after durable layer 24 state; its verified objects remain reusable.
- Windows HF CLI is authenticated as `rsb1813`; WSL and the existing urllib transport were anonymous and bypassed Xet. Authenticated `hf_xet 1.6.0` with `HF_XET_HIGH_PERFORMANCE=1` downloaded and assembled official shard 94, 4,697,664,072 bytes, in 56.02 seconds at 79.97 MiB/s and matched official SHA-256 `ad66e1cb96b86963e63d6a0a466b6a407b13c9815cb480fe612480cc6bb3b6e1`. The temporary payload was deleted.
- Live capacity is 1,607.88 GiB free on the P44 Pro and 829.24 GiB free on the HDD. A 1,280,000,000,000-byte output leaves 415.79 GiB on the NVMe; after a separate 200 GiB reserve, 215.79 GiB remains. The largest of 96 official shards is 15.82 GiB, so two-slot HDD staging fits.
- The 1.28 TB target requires a new 3-bit routed-expert representation; it cannot be published as runnable before the reference codec, metadata, runtime decode, and synthetic integration gate pass. Full source download is therefore gated rather than started prematurely.
- The authenticated 96-shard manifest is sealed locally with plan SHA-256 `1fb6b0b07534eda4f876fc3e55b62e6c5a73ad1b1bb420b40e6f7ef9bdce6fb9`. It accounts for 1,560,936,091,448 source bytes and a 16,990,916,912-byte maximum shard. The live `C:\K3X` plus `D:\K3X-staging` dry-run passes both reserves while retaining `official_launch_enabled=false`.
- Focused Local Foundry coverage passes 7 tests for the two-slot plan, disk rejection, checksum-bound ledger, 1.28 TB overflow, deterministic group-32 3-bit reference codec, reserved-code rejection, token-free Xet command construction, deletion gating, and non-publishing dry-run. The full download remains gated on runnable 3-bit reader/runtime support.
- The user approved continuing the local manufacture path. Quantization value 2 will be fail-closed behind a required feature bit, and this slice targets CPU/reference synthetic parity before any CUDA fused 3-bit path.
- The preserved untracked `results/b0043-official-first-token-progress/` directory remains outside this change.
- Commit `0b8a1e5` implements required-feature-negotiated quantization value 2, strict Python/C++ record validation, portable decoding, and complete synthetic layer/logit/token parity. The focused regression passes 28 tests and the CUDA-enabled binary builds and passes the same CPU execution gate.
- Commit `5f61c59` replaces CUDA-side host FP32 expansion with a scalar direct-packed 3-bit matvec. The literal CUDA test, complete quantized synthetic graph, native-MXFP4 regression, and Compute Sanitizer pass. CPU retains its explicit FP32-expanded oracle.
- Full manufacture remains disabled on bounded official MXFP4-to-3-bit conversion, exact recipe byte closure, and quality calibration. Grouped/resident/fused 3-bit FFN is also required for useful performance but is not misrepresented as part of the current correctness gate.
- The pinned 96-shard headers report 1,559,965,606,912 text bytes and 894,717,952 non-text bytes. All 82,432 routed experts occupy 1,446,456,066,048 native MXFP4 bytes; group-32 3-bit would occupy 1,191,199,113,216 bytes. Leaving every other text tensor unchanged would produce 1,305,603,372,032 bytes including non-text tensors, so expert-only 3-bit cannot satisfy the 1.28 TB budget.
- The smallest simple budget-closing candidate is group-32 3-bit for every routed expert plus conservative groupwise 8-bit for non-expert text tensors, while preserving the 894,717,952 non-text payload. Exact scale/alignment overhead must be included before launch. This is a proposed manufacturing recipe, not yet an accepted quality mode.
- Exact header-derived accounting preserves embeddings, LM head, one-dimensional tensors, norms, and router gate tensors at source precision; quantizes only the remaining non-expert BF16 matrices to group-128 8-bit; converts every routed expert to group-32 3-bit; and passes non-text tensors through. Payload is 1,252,654,054,352 bytes and the deliberately conservative K3X alignment/directory upper bound is 1,254,823,319,114 bytes, leaving 25,176,680,886 bytes below the 1.28 TB cap. This closes bytes only; the 8-bit codec/runtime and quality gates still block launch.
- The first released expert proxy showed that naive max-derived 3-bit scales produced 0.369519 relative L2 and 0.929302 cosine at the complete-expert output. Three least-squares group-scale iterations improved this to 0.325174 and 0.945909 without changing storage or decode ABI. This remains too divergent to unlock a quality-first full manufacture from one bounded proxy, so the 96-shard launch remains disabled under D-075.
- The user explicitly requested download, sharding, local manufacture, checksum-gated source cleanup, first-token generation, and optimization. D-076 therefore supersedes only D-075's launch lock and selects native MXFP4 experts plus group-128 8-bit trunk matrices rather than accepting measured 3-bit expert divergence.
- Official shards 1 and 2 matched their pinned hashes and produced Reader-valid fragments of 1,189,290,240 and 16,373,248,256 bytes. Their source files were deleted only after durable quality-ledger entries. Shard 3 onward is owned by `tools/run_local_foundry_quality.ps1`, with temporary microshards on D and final fragments on C.
- The active conductor process and its WSL child were rechecked while shard 3 was being converted. Runtime work must not stop or mutate that process. The next narrow gate is teaching the portable model loader to decode native BF16 and group-128 Q8 records, followed by a manifest-backed multi-fragment Reader so final assembly does not require a second 1.5 TB payload copy.
- The portable tensor decoder now accepts FP32, native BF16, and group-128 Q8 records and rejects unsupported encodings before model use. The focused C++ ops test and the synthetic runtime-session integration test pass. The initial bare `test_model_session` invocation returned 1 because that helper requires a generated artifact argument; the registered pytest integration supplied it and passed.
- Shard 2's 978.222-second conversion exposed per-extent durability overhead. With 250 synthetic extents the old writer published 251 resume manifests; the bounded 128-extent policy publishes three, forces a checkpoint on requested interruption, and retains source/partial validation on resume plus final root and Reader checks. Focused batching, resume, and corruption tests pass 3/3. Official timing remains pending on shard 4.
- The C++ Reader can now combine bounded K3X fragments without copying payload bytes. It reserves the high eight offset bits for the fragment index, rejects duplicate tensor IDs and inconsistent model configs, preserves request order across per-fragment batch reads, and aggregates counters. A two-fragment synthetic C++ integration passes; the checksum-bound set manifest remains the next gate.
- `K3XSET1` now seals the Local Foundry plan, ordered same-directory filenames, file lengths, each fragment's internal root SHA-256, and the manifest's canonical SHA-256. The finalizer refuses an incomplete ledger. The C++ Reader validates the seal and fragment identities before combining them; the synthetic set and tampered-seal paths pass. This replaces the proposed payload-copy assembler, but the official set cannot be published until all 96 units are complete.
- D-079 removes the remaining model-sized staging copy for shard 5 onward. The converter hardlinks the authenticated source shard into its bounded work directory, aliases only declared physical tensors to canonical K3X names, and writes only derived Q8 codes and scales. Focused local-shard and alias-manifest coverage passes 2/2; official timing remains pending.
- The first-token compatibility path now opens fragment records by canonical FNV-1a ID and supports dense BF16/F32/Q8 loads, bounded BF16 row slices, and native-MXFP4 matvecs. Focused local-shard decode and independent MXFP4 oracle parity pass 2/2. D-080 subsequently connected this store to every official graph entrypoint.
- D-080 connects layer 0, both official attention/MoE layer runners, resumed execution, and the chunked LM head to `--k3x-set`. The Python set parser rejects unsealed metadata before the strict fragment Reader. Focused set/store coverage passes 3/3, all modified entrypoints compile, and their CLI contracts expose the set flag. A real shard-1 strict open took 11.401 seconds and loaded finite official norm and Q8 q-projection tensors; this is a storage/decode diagnostic, not token throughput.
- Shard 5 is the first official conversion with both D-077 checkpoint batching and D-079 source-alias streaming plus emitted timing. It matched shard 2's source/output sizes and tensor composition, completed in 735.660 versus 978.222 seconds, and was ledger-published before source deletion. The 24.796% reduction is a combined-path result and does not isolate either change.
- D-081 keeps strict full-payload verification as the default and at conversion/deletion, but lets a sealed compatibility run trust the set's recorded roots when opening directories. The same shard-1 23-record artifact opened in 0.004774 versus 11.401 seconds under strict full verification. Focused strict/default and sealed-open coverage passes 4/4; the ratio is not token throughput.
- The reboot did not lose the reference prefix state: `artifacts/m36-official-first-token/state/state.json` remains digest-bound at completed layer 24 with 122 tensor records. `tools/run_official_reference_resume.sh` resumes layer 25 through the LM head in the background, stores new range objects on D to protect the C-drive final reserve, and publishes only the reference result JSON on C. This is the original-precision comparison oracle, not the K3X Q8 token.
- D-082 adds a Linux advisory lock around IMMORTAL ledger creation and completion read-modify-write, then parameterizes conductor staging, ledger, progress, and finalization. Focused ledger resume, two-process concurrent publication, and deletion gates pass 3/3; the PowerShell script parses. Three disjoint workers are planned only after the currently active shard 6 reaches its durable ledger gate.
- `run_k3x_first_token_when_ready.sh` is active as PID 25492. It waits for exactly 96 ledger units, seals `model.k3xset`, resumes an existing D-drive K3X state if present, and otherwise executes layer 0 through the chunked LM head under `--k3x-set`. Logs, range metadata objects, and prefix tensors stay on D; only compact result JSON is published in the worktree.
- D-083 removes only the duplicate CLI hash immediately before the converter's own authoritative source SHA-256. The independent post-ledger full hash before source deletion remains. Focused conversion/deletion coverage passes 2/2; shard 7 already loaded the earlier process code, so official timing evidence begins with later launches.
- D-084 propagates the canonical `K3XSET1` SHA-256 through every K3X layer result, prefix state, and head result. Resume and head execution reject a missing or different set identity. Focused identity/mismatch coverage passes 1/1 and the modified graph modules compile; no output-equivalence claim is made.
- Shard 6 reached its ledger and source-cleanup gates. After two fail-closed A-worker launches exposed a missing `-u` and then an out-of-scope `$target`, commits `f9c1131` and `de7cc67` corrected the exact path boundary. Workers A/B/C now concurrently convert shards 7/37/67 with the shared locked ledger; aggregate throughput remains unmeasured until publication.
- D-drive samples under three converters were 7.8–20.2 MB/s with queue length 5–14; pausing A and then B did not improve the remaining physical throughput. D-085 therefore uses the 23 GiB `/dev/shm` only for worker A's bounded conversion directory. Cross-device staging authenticates and inspects the RAM copy, while the original D source remains the post-ledger deletion authority. Focused coverage passes 1/1; official elapsed impact remains unmeasured.
- Shards 7, 37, 38, 67, and 68 subsequently reached durable ledger publication and checksum-gated source deletion. Shards 37/67 resumed partials in 471.855/408.273 seconds; fresh shards 38/68 completed in 697.560/694.020 seconds. The ledger therefore contains 11 fragments. No token or tok/s exists yet.
- D-086 combines final root comparison and artifact SHA-256 into one output pass. D-087 reuses a verified source identity only while device, inode, size, and nanosecond mtime remain unchanged. D-088 credits only structurally valid plan-bound resumable partial bytes in disk admission. Their focused regressions pass.
- WSL now has a 72 GiB cap, 16 GiB swap, and a 60 GiB `/dev/shm` mount, sufficient for two bounded RAM work roots. D-089 creates an atomic readiness marker only after D-to-RAM copy and official SHA verification; the next Xet prefetch waits for that marker. Focused coverage passes 1/1, Python compilation passes, and the PowerShell conductor parses.
- The destination reserve briefly rejected shard 69 because completed and resumable accounting left a small deficit. No model budget or 200 GiB reserve was weakened. Clearing only the reproducible `uv` package cache recovered 12.2 GiB; installed environments remained intact. Shard 69 then began through the new marker path.
- Fresh shards 39 and 69 completed in 452.074 and 449.727 seconds; shard 70 completed in 601.916 seconds after its RAM copy briefly overlapped worker B. The ledger now has 14 durable units. These are manufacture timings, not tok/s.
- Per-conductor markers did not prevent independent workers from copying shards 39 and 70 simultaneously. D-090 adds one shared advisory lock around only cross-device copy plus staged SHA verification. Focused ownership coverage passes 1/1; newly restarted B/C conductors use it for shards 40 and 71 onward.
- The original-precision reference remains durable at completed layer 46. Layer 47 failed once with `OFFICIAL_RANGE_LENGTH_MISMATCH` after a long process suspension; it can restart from layer 47 without recomputing prior layers.
- Reference retry succeeded and advanced through layer 54 while manufacture continued.
- Shard 8 completed in 398.151 seconds and raised the durable ledger to 17 units. A third bounded RAM worker is therefore active for shards 8–36; measured WSL memory before launch showed 52 GiB available and zero swap use.
- D-091 gives each conductor an independent Xet cache. The first two restarted conductors then ran active Python/Xet children simultaneously and host Ethernet received 2,427,845,614 bytes in 20 seconds, 121.39 MB/s or 971.14 Mb/s. This is link traffic, not effective payload throughput.
- Shards 40, 41, 71, and 72 raised the durable ledger to 19 units. Fresh 40/71/72 completed in 401.198/401.689/397.259 seconds. Shard 41 overlapped shard 72's final full-output audit, remained in DrvFS `p9_client_rpc`, and completed in 719.230 seconds. D-092 serializes only the final root/file-SHA audit; focused coverage passes 1/1 and conductors restarted at shards 9/42/73.
- The original-precision graph completed all layers 0–92 and the chunked LM head. Input token ID 1 generated greedy token ID 9689 at FP32 logit 8.307021141052246. Compact B-0044 SHA-256 is `e4b57d6dde9f59e205c2e6b40be8908b45d255ef9d29e8b4465fcb634c13beb9`; its schema explicitly records `throughput_measured=false`.
- D-093 caps full Xet download/assembly transactions at two with a named Windows semaphore while retaining three converters. Two active slots received 6,626,752,954 host Ethernet bytes in 55 seconds, 120.49 MB/s or 963.89 Mb/s, and the third conductor waited without spawning `hf.exe`.
- The semaphore count could leak when a background prefetch job was terminated after acquisition. D-094 replaces it with two named process-owned mutex slots and treats abandoned ownership as acquired. After cleaning only the confirmed old conductor/download trees, workers restarted at shards 11/44/75 with the ledger unchanged at 25/96 and the first-token waiter alive. Exactly two downloads ran; 20-second and 15-second host receive samples were 107.60 and 108.49 MiB/s. PowerShell parsing passes; a deliberate live abandonment injection was skipped to preserve useful in-flight work.
- Shard 44 was the first complete post-restart unit. It finished in 398.970 seconds, published a 16,157,917,440-byte fragment with SHA-256 `eaa3da6fbf5cb771c63f034d192ce9cf4a3eb858d971a2427e97591d07e80034`, deleted its verified source, and raised the durable ledger to 26/96. Because this remains in the prior clean 397–406 second band, no isolated speedup is claimed.
- Two later HF children stayed alive with zero CPU, network, and D I/O for roughly 10 and 15 minutes. D-095 adds a 600-second per-attempt limit and three partial-cache attempts. PowerShell 5.1 returned an empty `Start-Process` exit code despite HF printing and creating its final target, so transport completion now requires that target while the converter still performs the authoritative official SHA-256. Cached success and missing-target two-attempt failure checks pass. Shard 45 then completed in 407.314 seconds, published SHA-256 `dd96349e42b413c7d81a3c39bb9780e1095e7e4876fec9f0d3e449076533472f`, deleted its source, and raised the ledger to 29/96.
- `Start-Job` prefetch children still held zero CPU/network/write bytes after explicit Xet environment injection, while foreground downloads remained fast. D-096 moves marker waiting, mutex ownership, and bounded HF execution into a hidden child PowerShell process. Parser and cached-target helper checks pass. During shard 14/78 conversion plus shard 47/79 download, host receive reached 116.65 MiB/s. Shard 78 completed in 589.095 seconds with SHA-256 `8fac53b47f135c758f7151bb8159ae3b8617a1f64c1a9437e388988f9ccd2749`, source deletion, and ledger 35/96.
- The owned-process prefetch conductors continued without intervention through shards 17, 50, and 82. The durable quality ledger reached 47/96 fragments and 751,990,071,040 output bytes, covering contiguous ranges 1–17, 37–50, and 67–82. Active work advanced to shards 18, 51, and 83; the automatic first-token waiter remained alive and no sealed set or K3X token existed at this checkpoint.
- The same conductors continued through shards 22, 54, and 86. The durable ledger reached 60/96 fragments and 964,196,305,920 output bytes, covering contiguous ranges 1–22, 37–54, and 67–86. Active work advanced to shards 23, 55, and 87; no sealed set or K3X token existed at this checkpoint.
- The quality Local Foundry completed all 96 Reader-valid fragments with 1,507,512,467,456 payload bytes. `model.k3xset` seals them with manifest SHA-256 `f5c7443fd9ea9b4a2f0c95010f148182eefaedec4f29c094ca24e6bc4e61cefe`; every official source shard was deleted only after its ledger and checksum gates.
- The first sealed-set execution exposed that groupwise-Q8 decode returns its generic FP32 representation while the official graph requires the source contract's logical BF16 dtype. Commit `bd3bb5f` adds an optional load dtype, applies the pinned contract at every official graph entrypoint, and passes the focused regression. The restarted run completed layer 0 and continued from durable per-layer state without downloading weight payloads.
- After all workers stopped, one duplicate source and four abandoned conversion temporaries were confirmed under the bounded D-drive staging roots and removed explicitly, recovering about 52 GB. They are reproducible from the pinned official source but the deleted local copies are not directly recoverable.
- The full K3X execution completed all 93 layers and the LM head. Input token 1 generated greedy token 9689 at FP32 logit 8.290502548217773, exactly matching the original-precision token while differing from its 8.307021141052246 logit by 0.016518592834472656. The observed compatibility TTFT was 1,891 seconds; this is not steady decode tok/s.
- Verification checked all 93 embedded record digests, the monotonic layer chain, the sealed-set identity, zero downloaded model payload, all 449 final-state tensor SHA-256 values, and both head/reference records. The layer intervals sum to 563.791264 seconds load/attention/routing and 161.543706 seconds expert/dense compute, leaving repeated process/metadata/state orchestration as the largest observed gap.
- D-098 adds a SHA-256-validated persistent official HTTP metadata cache. The focused corruption/reuse test passes, and the real snapshot/index/config/shard-header boundary fell from 10.191660 seconds and 60,618,467 response bytes to 1.751957 seconds and zero HTTP bytes warm. This is an isolated 82.809896% boundary reduction, not a full token speedup.
- Milestone 39 starts from the measured B-0046 boundary rather than synthetic TPS. The selected bridge retains the verified Python graph and subprocess reference mode, adds callable stage entrypoints, and first removes interpreter restarts. Disk state publication remains until the one-process timing identifies its actual share; packed CUDA and expert-major speculation follow as separate measured changes.
- The first real persistent-driver resume used the completed official layer-92 state and sealed 96-fragment set. It generated token 9689 with the same 8.290502548217773 logit and final hidden digest as B-0046, downloaded zero payload bytes, and recorded 11.195065 seconds driver wall for the head-only resume. This validates the driver boundary but is not decode TPS or a full-token speedup.
- The uninterrupted B-0048 run used implementation head d367487 and finished all 93 layers plus the head in 1,156.152598 seconds versus B-0046's 1,891 seconds. All layer output/state digests, token 9689, logit 8.290502548217773, final hidden, and final state digest match. Load/decode and compute intervals sum to 529.554997/159.084610 seconds. A process-I/O sample at layer 82 was only a 79,236,866,016-byte lower bound, so complete physical NVMe and H2D traffic remain unmeasured.
- Milestone 40 targets only immutable topology/snapshot/index/config/set/header/store ownership. It intentionally leaves decoded weights and prefix tensors uncached so the next B-0049 wall delta isolates repeated metadata setup before packed CUDA work.
- The first shared-context head resume exposed that context creation preceded the driver total timer. The real output was correct, but the 6.179327-second record excluded context initialization and is diagnostic only. A witnessed call-order RED moved total_start ahead of context creation before the full B-0049 run.
- B-0049 used head 6a569c1 and measured 913.336487 seconds full wall, 21.002081% below B-0048 and 51.700873% below B-0046. All 93 output/state records and final token/logit/hidden/state match. Load/decode remains 430.579377 seconds and compute 159.572791 seconds, so packed CUDA is now the dominant actionable boundary.
- Milestone 41 isolates the first packed-transfer improvement: valid group-128 Q8 codes and BF16 scales cross PCIe in compressed form, CUDA reconstructs the same logical BF16 tensor, and the CPU decoder remains the reference fallback. It intentionally does not claim a fused matvec; B-0050 decides whether this bridge remains enabled while direct-packed Q8/MXFP4 kernels remain the next performance boundary.
- The released layer-0 q-projection microbenchmark is BF16 bit-exact and reduced median decode-plus-copy from 0.520519 to 0.441014 seconds, a 1.180276x speedup. Direct BF16 reconstruction reduced device peak allocation from the initial diagnostic 528,482,304 bytes to 177,537,024 bytes. A fresh official layer-0 execution retained the B-0049 output/KDA-state digests and the same 3,602,630,144-byte graph peak. Physical H2D traffic and token throughput remain unmeasured.
- B-0050 used implementation head `7cb9498` and completed all 93 layers plus the head in 583.658078 seconds, 36.096052% below B-0049 and 69.134951% below B-0046. All layer output/state records, token 9689, logit 8.290502548217773, final hidden, and final state match. Load/decode fell to 368.422976 seconds while compute remained 157.256886 seconds. This remains first-token TTFT, not steady decode tok/s.
- Milestone 42 selects a native `sm_120` PyTorch CUDA extension as the shortest reversible direct-packed Q8 bridge. The installed Torch 2.13.0+cu130 reports CUDA 13.0 and capability 12.0; CUDA 13.3 nvcc and Ninja are present. An explicit `--direct-q8` switch retains B-0050 as the default reference while released-tensor and layer-0 gates decide whether a full run is justified.
- B-0051 used implementation head `f06d078`. Its released q-projection cold path is 1.165607x faster and resident kernel is 40.298662x faster, with 57.48% BF16 exact elements and 0.03125 maximum error. Full wall is 566.002323 seconds, only 3.025017% below B-0050. Token 9689 remains, final-hidden cosine is 0.9999314, but nine of 92 MoE layers change Top-16 sets. Direct Q8 therefore remains experimental/non-default; packed residency is the next measured boundary.
- Milestone 43 selects explicit-budget stable first admission for packed Q8 L0/L1 residency. Sequential full-model LRU can produce complete scan thrash when capacity is below the working set; stable admission guarantees repeat hits for the admitted subset. One OfficialRuntimeContext will own the shared cache, and zero-byte defaults preserve prior behavior.
- B-0052 proves shared packed-Q8 residency on official layer 0: three dense-MLP matrices occupy 738,017,280 bytes, cold/warm wall is 11.149552/2.638550 seconds, and the second pass records 3/3 L0 hits with identical output and KDA-state digests.
- The remaining B-0052 warm wall traced to materialized KDA Q8 projections. The reference projector now accepts a validated matvec-capable weight without changing its tensor path, and only layer-0 self-attention is selected for the new direct path. Focused KDA/store/runtime tests pass.
- Superseded single-warm diagnostics varied from 0.214740 to 0.275639 seconds, so they were not promoted. B-0055 uses committed implementation `7f8fc05`, committed median harness `300fb75`, and five warm samples.
- B-0055 admits all eleven layer-0 Q8 projections into 1,188,528,640 device bytes. Cold wall is 7.345563 seconds and the five-run warm median is 0.213557 seconds, a 34.3963x ratio, with 11/11 hits per warm run. Versus B-0052, final-hidden cosine is 0.9999964 and maximum absolute error is 0.000244141; full-token routing and coding quality remain unmeasured.
- Milestone 44 selects a thin native `sm_120` PyTorch extension for the already verified E2M1/E8M0 CUDA arithmetic. A complete C++ backend binding would duplicate ownership, while subprocess invocation cannot preserve residency. The first cache is explicitly matrix-granular; atomic whole-expert hot-bank admission remains a later policy step.
- The native MXFP4 Python boundary preserves the portable store's FP32 matvec output rather than BF16-rounding at the extension boundary. Shared explicit RAM/VRAM byte budgets default to zero, so the previous portable path remains the reference mode. The released expert benchmark uses layer 1 expert 0, a deterministic 3,584-element input, one CPU oracle, one cold CUDA call, and five warm CUDA calls.
- B-0056 measured 13.732 ms warm median for one resident expert with cosine effectively 1.0 and 0.0000014 maximum absolute error. Warp reduction plus bitwise E8M0 measured 13.952 ms, and isolated bitwise E8M0 measured 13.976 ms, so both scalar experiments were rejected and reverted. The current C++ resident-grid control reproduced a 10.848 ms median for the complete natural Top-16 MoE FFN with zero maximum error. This evidence changes the next optimization from scalar-kernel tuning to expert-major batching.
